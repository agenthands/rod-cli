---
phase: 22-example-plugins
plan: 05
type: execute
wave: 2
depends_on: [22-01, 22-02]
files_modified:
  - docs/plugins/examples/recipes.md
  - docs/plugins/examples/starter.md
autonomous: true
requirements: [PEX-02, PEX-03]
tags: [plugin, docs, examples, recipes, starter]

must_haves:
  truths:
    - "A reader can open docs/plugins/examples/recipes.md and, per hook, see the recipe script, what it does, and how to run it (`plugin run <getter>`) to see the hook fire"
    - "A reader can open docs/plugins/examples/starter.md and follow it to copy the starter, load it unchanged, and confirm it runs without errors"
    - "Both docs cross-link the Phase 21 reference pages with the correct `../` relative paths"
  artifacts:
    - path: "docs/plugins/examples/recipes.md"
      provides: "One section per lifecycle-hook recipe: what it does + how to run it"
      contains: "plugin run"
    - path: "docs/plugins/examples/starter.md"
      provides: "How to copy, load, and run the starter template"
      contains: "starter.js"
  key_links:
    - from: "docs/plugins/examples/recipes.md"
      to: "docs/plugins/lifecycle-hooks.md"
      via: "cross-link with ../ relative path for hook payload shapes"
      pattern: "\\.\\./lifecycle-hooks\\.md"
    - from: "docs/plugins/examples/starter.md"
      to: "docs/plugins/cli-reference.md"
      via: "cross-link with ../ relative path for plugin load/run"
      pattern: "\\.\\./cli-reference\\.md"
---

<objective>
Document the per-hook recipes (PEX-02) and the copyable starter (PEX-03) under `docs/plugins/examples/`. The recipes doc has one section per lifecycle hook describing the recipe script, what it collects, and how to run it (`plugin load` the recipe, drive the browser, `plugin run <getter>` to see that hook fired). The starter doc explains how to copy `starter.js`, load it unchanged, and confirm it runs without errors. Both cross-link the Phase 21 reference pages instead of duplicating them.

Purpose: PEX-02 and PEX-03 each require a short note alongside their runnable scripts so a reader can run them and discover them.
Output: recipes.md + starter.md under docs/plugins/examples/.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-example-plugins/22-CONTEXT.md
@.planning/phases/22-example-plugins/22-PATTERNS.md
@docs/plugins/lifecycle-hooks.md
@docs/plugins/cli-reference.md
@docs/plugins/state-api.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/plugins/examples/recipes.md (one section per hook)</name>
  <files>docs/plugins/examples/recipes.md</files>
  <read_first>
    - plugins/examples/recipes/on_request.js, on_response.js, on_load.js, on_dom_node_inserted.js (the four scripts this doc documents — their hook + getter names: getRequestLog, getResponseLog, getLoadLog, getInsertedNodes)
    - docs/plugins/lifecycle-hooks.md lines 26-86 ("Worked Snippets" tone/structure to mirror) and the `## See Also` / `## Source` footer style
    - docs/plugins/cli-reference.md (the `plugin load` / `plugin run` flow to reference)
    - .planning/phases/22-example-plugins/22-PATTERNS.md lines 204-215 (doc structure + `../` cross-link rule for the deeper directory)
  </read_first>
  <action>
    Write `docs/plugins/examples/recipes.md`:
    - Opening prose: these are minimal one-hook recipes, one per lifecycle hook, each loadable and readable via `plugin run <getter>`.
    - One `###` section per hook (onRequest, onResponse, onLoad, onDOMNodeInserted). Each section: a one-line description of what the recipe collects; the recipe file path (`plugins/examples/recipes/<file>.js`); a `bash` block showing `rod-cli plugin load ./plugins/examples/recipes/<file>.js`, a "drive the browser" note (navigate / let a response arrive / insert a DOM node), then `rod-cli plugin run <getter>` and what JSON it returns. Link the hook's payload shape to `../lifecycle-hooks.md` rather than restating it; for onLoad link `../state-api.md` for `api.GetSnapshot()`.
    - A `## See Also` section: `../lifecycle-hooks.md`, `../state-api.md`, `../cli-reference.md`, and `./xss-scanner.md`.
    - A `## Source` footer pointing at `../../plugins/examples/recipes/` (repo-relative `../../`).
    Getter names must match the scripts exactly: getRequestLog, getResponseLog, getLoadLog, getInsertedNodes. Do not duplicate the reference pages — link them.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && test -f docs/plugins/examples/recipes.md && grep -q 'getRequestLog' docs/plugins/examples/recipes.md && grep -q 'getInsertedNodes' docs/plugins/examples/recipes.md && grep -q '\.\./lifecycle-hooks\.md' docs/plugins/examples/recipes.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/plugins/examples/recipes.md` exists.
    - Contains a section per hook and references all four getters (getRequestLog, getResponseLog, getLoadLog, getInsertedNodes).
    - Cross-links `../lifecycle-hooks.md`, `../state-api.md`, `../cli-reference.md`.
    - Has `## See Also` and `## Source` sections.
  </acceptance_criteria>
  <done>recipes.md documents all four hook recipes with run instructions and correct cross-links.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/plugins/examples/starter.md</name>
  <files>docs/plugins/examples/starter.md</files>
  <read_first>
    - plugins/examples/starter.js (the template this doc documents — its four stubs + getResults accessor)
    - docs/plugins/cli-reference.md (structure/tone to mirror; the `plugin load` / `plugin run` flow to reference)
    - docs/plugins/lifecycle-hooks.md lines 18-24 (hooks are optional / no-op if absent — restate the copy-and-fill model)
    - .planning/phases/22-example-plugins/22-PATTERNS.md lines 204-215 (doc structure + `../` cross-link rule)
  </read_first>
  <action>
    Write `docs/plugins/examples/starter.md`:
    - Opening prose: this is a copyable scaffold defining empty stubs for all four hooks plus a results accessor; copy it, fill in the hooks you need, and the rest no-op.
    - A `bash` block: copy `plugins/examples/starter.js` to your own file, then `rod-cli plugin load ./your-plugin.js`, confirming it loads without errors, then `rod-cli plugin run getResults` returns an empty JSON array until you populate `results`.
    - A short "next steps" note linking `../lifecycle-hooks.md` (which hooks exist + payloads), `../state-api.md` (the `api` global), and `./recipes.md` (per-hook recipes to crib from).
    - A `## See Also` section with the `../` cross-links and `./xss-scanner.md`.
    - A `## Source` footer pointing at `../../plugins/examples/starter.js`.
    The accessor name must match the script exactly: getResults. Do not duplicate the reference pages — link them.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && test -f docs/plugins/examples/starter.md && grep -q 'starter.js' docs/plugins/examples/starter.md && grep -q 'plugin run getResults' docs/plugins/examples/starter.md && grep -q '\.\./cli-reference\.md' docs/plugins/examples/starter.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/plugins/examples/starter.md` exists.
    - Contains `starter.js`, `plugin run getResults`, and cross-links `../cli-reference.md` (plus `../lifecycle-hooks.md`, `../state-api.md`).
    - Has `## See Also` and `## Source` sections.
  </acceptance_criteria>
  <done>starter.md explains copy -> load unchanged -> `plugin run getResults`, with correct cross-links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → documentation | Docs are read-only Markdown; no executable trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | Information Disclosure | Recipe/starter docs describing how to read collected state | accept | Docs describe reading the operator's own session state via `plugin run`; no exploit logic, live targets, or sensitive data embedded. Example scripts stay in user space. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (docs only). N/A. |
</threat_model>

<verification>
- `docs/plugins/examples/recipes.md` exists with a section per hook, all four getters, and `../` cross-links.
- `docs/plugins/examples/starter.md` exists with `plugin run getResults` and `../` cross-links.
- Both docs cross-link the Phase 21 reference pages rather than duplicating them.
</verification>

<success_criteria>
- PEX-02: each per-hook recipe is documented with run instructions so a reader can see the hook fire.
- PEX-03: the starter is documented so a reader can copy it, load it unchanged, and confirm it runs.
</success_criteria>

<output>
Create `.planning/phases/22-example-plugins/22-05-SUMMARY.md` when done.
</output>