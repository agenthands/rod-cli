---
phase: 22-example-plugins
plan: 04
type: execute
wave: 2
depends_on: [22-01, 22-03]
files_modified:
  - docs/plugins/examples/xss-scanner.md
  - docs/plugins/cli-reference.md
autonomous: true
requirements: [PEX-01]
tags: [plugin, docs, examples, xss, cli-reference]

must_haves:
  truths:
    - "A reader can follow docs/plugins/examples/xss-scanner.md to load the scanner, drive a vulnerable page, and run `plugin run getFindings` to observe findings end-to-end"
    - "docs/plugins/cli-reference.md documents `plugin run <name>` accurately as invoking the named JS function and returning its result, with the missing-function error"
    - "The xss-scanner doc cross-links the Phase 21 reference pages with the correct `../` relative paths"
  artifacts:
    - path: "docs/plugins/examples/xss-scanner.md"
      provides: "Worked example doc walking load -> drive page -> plugin run getFindings"
      contains: "plugin run getFindings"
    - path: "docs/plugins/cli-reference.md"
      provides: "Corrected `plugin run` section reflecting the now-functional RunFunc behavior"
      contains: "invokes the JS function"
  key_links:
    - from: "docs/plugins/examples/xss-scanner.md"
      to: "docs/plugins/lifecycle-hooks.md"
      via: "cross-link with ../ relative path (examples dir is one level deeper)"
      pattern: "\\.\\./lifecycle-hooks\\.md"
    - from: "docs/plugins/cli-reference.md"
      to: "actions/plugin.go"
      via: "documents the rewritten PluginRun -> RunFunc behavior"
      pattern: "plugin run"
---

<objective>
Document the flagship XSS scanner as a complete worked example (PEX-01) and update the Phase 21 CLI reference so `plugin run` is documented truthfully now that the engine fix (Plan 01) made it functional.

1. Write `docs/plugins/examples/xss-scanner.md`: a reader follows it to `plugin load` the scanner, drive a vulnerable page, and `plugin run getFindings` to observe findings end-to-end — the doc's reason to exist.
2. Update the `plugin run <name>` section of `docs/plugins/cli-reference.md`: replace the "current stub (known limitation)" text and the `Triggered plugin <name>` output with accurate behavior ("invokes the JS function named `<name>` in the loaded plugin VM and returns its result"), including the missing-function error from RunFunc. Keep all other cli-reference content intact (no Phase 21 regression).

Purpose: PEX-01's success criterion is observing findings end-to-end; the CLI reference must stay truthful after the fix.
Output: new xss-scanner.md; surgically-updated cli-reference.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-example-plugins/22-CONTEXT.md
@.planning/phases/22-example-plugins/22-PATTERNS.md
@plugins/examples/xss_scanner.js
@docs/plugins/cli-reference.md
@docs/plugins/lifecycle-hooks.md
@docs/plugins/state-api.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/plugins/examples/xss-scanner.md (worked example)</name>
  <files>docs/plugins/examples/xss-scanner.md</files>
  <read_first>
    - plugins/examples/xss_scanner.js (the polished script this doc walks through — its hooks and the getFindings/getRequestLog accessors)
    - docs/plugins/lifecycle-hooks.md (tone/structure to mirror: intro prose, javascript/bash fenced blocks, `## See Also`, `## Source`; note its sibling cross-links use a single `./`)
    - docs/plugins/cli-reference.md lines 5-28 (the `plugin load` flow this doc invokes) and lines 79-86 (See Also + Source footer style)
    - .planning/phases/22-example-plugins/22-PATTERNS.md lines 204-215 (doc structure + the one-level-deeper `../` cross-link rule)
  </read_first>
  <action>
    Create `docs/plugins/examples/` and write `xss-scanner.md`:
    - Opening prose paragraph: what the scanner does and that this page walks loading and observing its findings end-to-end.
    - A `bash` block for the load step: `rod-cli plugin load ./plugins/examples/xss_scanner.js`.
    - A short walkthrough of the script's hooks (onRequest logging requests, onResponse checking for reflected payloads, onLoad scanning the DOM snapshot) — reference, do not duplicate, the hook payload shapes by linking `../lifecycle-hooks.md` and the `api.GetSnapshot()` usage by linking `../state-api.md`.
    - A "drive a vulnerable page" step describing navigating to a page that reflects a payload (use a generic example URL; do not bake exploit infra into the doc).
    - A `bash` block showing `rod-cli plugin run getFindings` returning the findings JSON, and `rod-cli plugin run getRequestLog` for the request log — explain that `plugin run <name>` invokes that JS accessor and prints its JSON (link `../cli-reference.md`).
    - A `## See Also` section cross-linking `../lifecycle-hooks.md`, `../state-api.md`, `../cli-reference.md` (all with the extra `../` because this file is one level deeper than the existing docs).
    - A `## Source` footer pointing at `../../plugins/examples/xss_scanner.js` and the relevant `internal/plugin/` files (repo-relative `../../`).
    Do not duplicate the reference pages' content — link to them.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && test -f docs/plugins/examples/xss-scanner.md && grep -q 'plugin run getFindings' docs/plugins/examples/xss-scanner.md && grep -q '\.\./lifecycle-hooks\.md' docs/plugins/examples/xss-scanner.md && grep -q '\.\./cli-reference\.md' docs/plugins/examples/xss-scanner.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/plugins/examples/xss-scanner.md` exists.
    - Contains the literal `plugin run getFindings`.
    - Cross-links `../lifecycle-hooks.md`, `../state-api.md`, and `../cli-reference.md` (extra `../` for the deeper directory).
    - Has a `## See Also` and a `## Source` section.
  </acceptance_criteria>
  <done>The xss-scanner worked-example doc walks load -> drive page -> `plugin run getFindings` and cross-links the Phase 21 references with correct relative paths.</done>
</task>

<task type="auto">
  <name>Task 2: Update the `plugin run` section of cli-reference.md</name>
  <files>docs/plugins/cli-reference.md</files>
  <read_first>
    - docs/plugins/cli-reference.md (file being edited; the `plugin run <name>` section lines 53-73 to rewrite; the `plugin load` section lines 5-28 as the correct "functional command" template — Behavior / Output / Error conditions; the Thin-Client section line 77 and Source footer line 86 which mention the stub)
    - actions/plugin.go (the rewritten PluginRun from Plan 01 — delegates to RunFunc, guards empty name)
    - internal/plugin/engine.go (the RunFunc errors: not found / not callable / nil VM)
    - .planning/phases/22-example-plugins/22-PATTERNS.md lines 219-223 (which parts to rewrite, keep the rest intact)
  </read_first>
  <action>
    Surgically rewrite ONLY the `plugin run <name>` section (lines ~53-73) and the few clauses elsewhere that assert it is a stub:
    - Replace the "Behavior — current stub (known limitation)" paragraph with accurate behavior: `plugin run <name>` invokes the JS function named `<name>` in the loaded plugin's VM (via the daemon's `PluginRun` -> `engine.RunFunc`), calls it with no arguments, and returns its result stringified. Note the canonical use: `rod-cli plugin run getFindings` returns the example scanner's collected findings JSON.
    - Replace the `Triggered plugin <name>` Output block with the real behavior: the printed output is the function's returned value (e.g. the JSON string an accessor produces); a function returning undefined/null prints empty.
    - Update Error / exit conditions: a missing/undefined function name yields `function "<name>" not found`; a name bound to a non-callable value yields `"<name>" is not a callable function`; an empty name is guarded with `plugin function name is required`; runtime errors inside the function are wrapped as `error calling "<name>"`. Each causes a non-zero exit.
    - Remove the "The real execution path today is `plugin load`" stub-era caveat if it now misleads (keep a brief note that hooks still fire automatically after load; `plugin run` is for invoking named accessors/functions).
    - Update the Thin-Client paragraph and the Source footer ONLY where they describe `PluginRun` as a stub — make them say it delegates to `engine.RunFunc`.
    - Keep `plugin load`, `plugin list`, See Also, and all other content byte-for-byte intact. Do NOT reflow unrelated sections.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && grep -q 'invokes the JS function' docs/plugins/cli-reference.md && grep -q 'plugin run getFindings' docs/plugins/cli-reference.md && ! grep -q 'Triggered plugin' docs/plugins/cli-reference.md && ! grep -q 'not yet fully implemented' docs/plugins/cli-reference.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/plugins/cli-reference.md` contains `invokes the JS function` and `plugin run getFindings`.
    - The literals `Triggered plugin` and `not yet fully implemented` are GONE.
    - The missing-function error `function "<name>" not found` is documented.
    - `plugin load` and `plugin list` sections are unchanged (spot-check: their first lines still match).
  </acceptance_criteria>
  <done>The cli-reference `plugin run` section accurately documents RunFunc behavior + errors; the rest of the page is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → documentation | Docs are read-only Markdown; no executable trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Information Disclosure | Worked-example doc describing payload-driving | accept | The doc references a generic vulnerable page and the user-space example script; no exploit infrastructure or live targets are embedded. Consistent with REQUIREMENTS Out of Scope (exploit logic stays in example scripts, not docs or binary). |
| T-22-09 | Repudiation | cli-reference accuracy after the fix | mitigate | Updating the `plugin run` docs to match the shipped RunFunc behavior keeps the reference truthful, preventing a Phase 21 regression where the page would mis-describe the command. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (docs only). N/A. |
</threat_model>

<verification>
- `docs/plugins/examples/xss-scanner.md` exists, contains `plugin run getFindings`, and cross-links `../lifecycle-hooks.md`, `../state-api.md`, `../cli-reference.md`.
- `docs/plugins/cli-reference.md` documents the functional `plugin run` (`invokes the JS function`, `plugin run getFindings`) and no longer contains `Triggered plugin` or `not yet fully implemented`.
- `plugin load` / `plugin list` sections of cli-reference.md remain intact.
</verification>

<success_criteria>
- PEX-01: a reader can follow xss-scanner.md to load the scanner, drive a page, and observe findings via `plugin run getFindings`.
- The CLI reference truthfully documents the now-functional `plugin run` without regressing the rest of the page.
</success_criteria>

<output>
Create `.planning/phases/22-example-plugins/22-04-SUMMARY.md` when done.
</output>