---
phase: 21-reference-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/plugin/api.go
  - internal/plugin/integ_test.go
autonomous: true
requirements: [PDOC-03]
must_haves:
  truths:
    - "A plugin author can call api.GetLocalStorage() from a loaded plugin and receive the page's localStorage as a key/value object"
    - "GetLocalStorage() returns (nil, nil) when no page is bound, mirroring GetCookies/GetSnapshot"
    - "go build ./... and go test ./internal/plugin/... pass after the change"
  artifacts:
    - path: "internal/plugin/api.go"
      provides: "PluginAPI.GetLocalStorage() accessor"
      contains: "func (a *PluginAPI) GetLocalStorage()"
    - path: "internal/plugin/integ_test.go"
      provides: "Integration test for GetLocalStorage on a real page"
      contains: "func TestPluginAPI_GetLocalStorage_RealPage"
  key_links:
    - from: "internal/plugin/api.go"
      to: "internal/plugin/lifecycle.go"
      via: "GetLocalStorage is a method on PluginAPI, which BindLifecycle sets as the 'api' global — no new wiring needed"
      pattern: "func \\(a \\*PluginAPI\\) GetLocalStorage"
---

<objective>
Add a `GetLocalStorage()` accessor to `PluginAPI` in `internal/plugin/api.go` so that localStorage becomes a first-class, documentable method on the `api` global, alongside the existing `GetSnapshot()` and `GetCookies()`. Add an integration test mirroring the existing `TestPluginAPI_*` tests.

This is the sanctioned small corrective engine fix from CONTEXT.md (per D — "small corrective fix, user-approved"). It surfaces existing browser state; it is NOT a new engine capability. It must land before `state-api.md` (Plan 04) so that page documents a real, shipped method.

Purpose: Make `state-api.md` able to cite a method that actually exists in source (Success Criterion #4 — no invented surfaces).
Output: `PluginAPI.GetLocalStorage()` method + `TestPluginAPI_GetLocalStorage_RealPage` test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-reference-documentation/21-CONTEXT.md
@.planning/phases/21-reference-documentation/21-PATTERNS.md

@internal/plugin/api.go
@internal/plugin/lifecycle.go
@internal/plugin/integ_test.go
@internal/plugin/scanner/scanner.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GetLocalStorage() accessor to PluginAPI</name>
  <files>internal/plugin/api.go</files>
  <read_first>
    - internal/plugin/api.go (the file being modified — study the exact shape of GetCookies() lines 17-27 and GetSnapshot() lines 29-35)
    - internal/plugin/scanner/scanner.go (page.Eval pattern, ~lines 180-205 — page.Eval takes a JS arrow-function string and returns result, err; result.Value.Export() yields a Go value)
    - internal/plugin/lifecycle.go (confirms PluginAPI is set as the 'api' global via e.vm.Set("api", NewPluginAPI(page)) — no new wiring needed)
  </read_first>
  <behavior>
    - Calling GetLocalStorage() on a PluginAPI with a real page returns a non-nil value and nil error.
    - Calling GetLocalStorage() on NewPluginAPI(nil) returns (nil, nil) — the established no-op-when-page-nil pattern.
    - When a key is seeded in the page's localStorage, the returned value contains that key/value pair.
  </behavior>
  <action>
    Add method `func (a *PluginAPI) GetLocalStorage() (interface{}, error)` to internal/plugin/api.go, placed after GetCookies()/GetSnapshot(). Mirror the existing accessors exactly:
    - First line guard: if a.page == nil, return nil, nil (identical pattern to GetCookies/GetSnapshot).
    - Read window.localStorage by calling a.page.Eval with a no-argument JS arrow function that iterates localStorage keys into a plain object and returns it (iterate index 0..localStorage.length-1 using localStorage.key(i) and localStorage.getItem(k)).
    - On the page.Eval error, return nil, err.
    - On success return result.Value.Export(), nil so goja hands back a map[string]interface{} (most consistent with GetCookies returning a structured interface{}, per Claude's Discretion in CONTEXT.md — map preferred over a JSON string).
    Do NOT add any new import (rod is already imported; page.Eval needs nothing else). Do NOT change the signatures or bodies of GetCookies or GetSnapshot (CONTEXT specifics constraint). Do NOT alter NewPluginAPI or the PluginAPI struct.
  </action>
  <verify>
    <automated>go build ./... && grep -q "func (a \*PluginAPI) GetLocalStorage() (interface{}, error)" internal/plugin/api.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/plugin/api.go contains the exact signature `func (a *PluginAPI) GetLocalStorage() (interface{}, error)`.
    - internal/plugin/api.go contains a nil-page guard returning `nil, nil` inside GetLocalStorage.
    - internal/plugin/api.go references `a.page.Eval` and `.Export()` inside GetLocalStorage.
    - The import block of internal/plugin/api.go is unchanged (still only `github.com/go-rod/rod`).
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>GetLocalStorage() exists on PluginAPI, mirrors the GetCookies nil-guard + (value, error) shape, reads window.localStorage via page.Eval, and the package compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TestPluginAPI_GetLocalStorage_RealPage integration test</name>
  <files>internal/plugin/integ_test.go</files>
  <read_first>
    - internal/plugin/integ_test.go (the file being modified — reuse TestMain lines 19-39, openIntegPage lines 41-51, and copy the shape of TestPluginAPI_GetCookies_RealPage lines 110-122)
    - internal/plugin/api.go (the GetLocalStorage method added in Task 1 — confirm its (interface{}, error) return shape)
  </read_first>
  <behavior>
    - Opening the integ test page, seeding a localStorage key, then calling api.GetLocalStorage() returns nil error and a non-nil value containing the seeded key.
    - GetLocalStorage() on NewPluginAPI(nil) returns (nil, nil) with no browser and no panic.
  </behavior>
  <action>
    Add `func TestPluginAPI_GetLocalStorage_RealPage(t *testing.T)` to internal/plugin/integ_test.go, placed alongside the other TestPluginAPI_* tests. Reuse the existing harness — do NOT add a second TestMain, do NOT recreate the server or browser:
    - Open the page with `page := openIntegPage(t, integServerURL)` and `defer page.MustClose()`.
    - The default served HTML has NO localStorage, so seed one key before reading: call page.Eval with a JS arrow function that does localStorage.setItem with a fixed key/value (e.g. theme/dark).
    - Construct `api := NewPluginAPI(page)`, then `data, err := api.GetLocalStorage()`; t.Fatalf on err; t.Fatal if data == nil.
    - Optionally assert the seeded key is present in the returned map.
    Also add a cheap nil-page unit assertion (either as a second small test func like TestPluginAPI_GetLocalStorage_NilPage or inline): `NewPluginAPI(nil).GetLocalStorage()` returns (nil, nil) — matches the established no-op pattern and needs no browser.
    Do NOT modify the existing tests, TestMain, or openIntegPage.
  </action>
  <verify>
    <automated>go test ./internal/plugin/... -run TestPluginAPI_GetLocalStorage -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/plugin/integ_test.go contains `func TestPluginAPI_GetLocalStorage_RealPage`.
    - The new test calls `openIntegPage` and `api.GetLocalStorage()` and seeds localStorage via page.Eval before reading.
    - A nil-page assertion exists: `NewPluginAPI(nil).GetLocalStorage()` is checked to return nil/nil.
    - `go test ./internal/plugin/... -run TestPluginAPI_GetLocalStorage -count=1` exits 0.
    - `go test ./internal/plugin/...` (full package) exits 0 — no existing test regressed.
  </acceptance_criteria>
  <done>The new real-page test seeds localStorage, reads it back via GetLocalStorage(), asserts non-nil; the nil-page case returns (nil,nil); the full plugin package test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| goja plugin VM → browser page state | A user-supplied plugin script (already trusted, loaded by the operator) calls api.GetLocalStorage() to read window.localStorage of the controlled page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | PluginAPI.GetLocalStorage() exposes localStorage to the plugin VM | accept | Consistent with the already-shipped GetSnapshot()/GetCookies() exposure. Plugins are operator-supplied trusted scripts loaded via `plugin load`; the operator already controls the page and its storage. No new trust boundary is crossed — same VM, same page, same access class as cookies/HTML. |
| T-21-02 | Tampering | window.localStorage read via page.Eval JS string | accept | The JS arrow function is a fixed literal authored in-repo (no user input is interpolated into the eval string), so there is no injection surface. Read-only; does not mutate page state. |
</threat_model>

<verification>
- `go build ./...` exits 0.
- `go test ./internal/plugin/...` exits 0 (all existing + new tests pass).
- `grep -c "func (a \*PluginAPI) Get" internal/plugin/api.go` returns 3 (GetCookies, GetSnapshot, GetLocalStorage).
</verification>

<success_criteria>
- PluginAPI.GetLocalStorage() exists with signature `(interface{}, error)`, nil-page guard, page.Eval read of window.localStorage, returns Export()'d map.
- TestPluginAPI_GetLocalStorage_RealPage passes against the shared integ harness; nil-page case returns (nil, nil).
- Existing GetCookies/GetSnapshot signatures and tests are unchanged.
</success_criteria>

<output>
Create `.planning/phases/21-reference-documentation/21-01-SUMMARY.md` when done.
</output>
