---
phase: 21-reference-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/plugins/lifecycle-hooks.md
autonomous: true
requirements: [PDOC-02]
must_haves:
  truths:
    - "A reader can find each of onRequest, onResponse, onLoad, onDOMNodeInserted with its JS handler name, the CDP event it maps to, and key payload fields"
    - "A reader can see a worked snippet for each hook"
    - "A reader can confirm handlers are optional and the 'api' global is bound on plugin load"
    - "Every documented hook name and proto type matches internal/plugin/lifecycle.go"
  artifacts:
    - path: "docs/plugins/lifecycle-hooks.md"
      provides: "Authoritative reference for the four lifecycle hooks"
      contains: "onDOMNodeInserted"
  key_links:
    - from: "docs/plugins/lifecycle-hooks.md"
      to: "internal/plugin/lifecycle.go"
      via: "doc cites lifecycle.go as the backing source for the hook→event mapping"
      pattern: "internal/plugin/lifecycle.go"
---

<objective>
Create `docs/plugins/lifecycle-hooks.md` — the authoritative reference for the four plugin lifecycle hooks, grounded in `internal/plugin/lifecycle.go`. It documents the JS handler name → CDP event (proto type) → key payload fields mapping for each hook, with one worked snippet per hook drawn from the shipped `xss_scanner.js`.

Purpose: Satisfy ROADMAP Success Criterion #1 and PDOC-02 — a plugin author can look up every lifecycle hook with its signature and payload shape.
Output: `docs/plugins/lifecycle-hooks.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-reference-documentation/21-CONTEXT.md
@.planning/phases/21-reference-documentation/21-PATTERNS.md

@internal/plugin/lifecycle.go
@plugins/examples/xss_scanner.js
@skills/rod-cli/references/storage-state.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/plugins/lifecycle-hooks.md</name>
  <files>docs/plugins/lifecycle-hooks.md</files>
  <read_first>
    - internal/plugin/lifecycle.go (source of truth — BindLifecycle lines 20-34 maps each proto event to its JS handler name via invokeJSFunc; invokeJSFunc lines 36-49 silently no-ops when the handler is undefined; line 22 sets the 'api' global; LifecycleEmitter interface lines 12-17 uses uppercase Go names — document the lowercase JS names instead)
    - plugins/examples/xss_scanner.js (worked snippets: onRequest lines 17-24, onResponse lines 27-41, onLoad lines 44-65)
    - skills/rod-cli/references/storage-state.md (tone/structure analog: H1 title, short intro, H2 sections, language-tagged fenced blocks, concise imperative prose)
  </read_first>
  <action>
    Create docs/plugins/lifecycle-hooks.md (the docs/ and docs/plugins/ directories do not exist yet — create them). Follow the shared reference template established by storage-state.md: H1 title, one-paragraph intro, H2 sections, fenced blocks tagged ```javascript for handler snippets.
    Required content, all grounded in internal/plugin/lifecycle.go (do NOT invent hooks beyond the four wired in BindLifecycle):
    - A mapping table with columns: JS handler name | CDP event (proto type) | key payload fields. Rows, exactly as wired in BindLifecycle:
        onRequest -> proto.NetworkRequestWillBeSent -> event.Request.URL, event.Request.Method, headers
        onResponse -> proto.NetworkResponseReceived -> event.Response.URL, event.Response.Status, headers
        onLoad -> proto.PageLoadEventFired -> load timestamp
        onDOMNodeInserted -> proto.DOMChildNodeInserted -> parent node id / inserted node
      Document the lowercase JS handler names (what authors write), and note the uppercase Go LifecycleEmitter interface names exist internally.
    - Payload depth: document only the commonly-used fields above and link to the go-rod CDP proto type for the full field set (keep the page token-efficient — do NOT dump full proto field lists inline). Reference each proto type by its Go name so it is greppable.
    - One concise worked snippet per hook, adapted from xss_scanner.js (onRequest reading event.Request.URL/.Method; onResponse reading event.Response.URL/.Status; onLoad calling api.GetSnapshot()). Keep onDOMNodeInserted minimal since the example file has no DOM-insert handler — show a stub that reads the inserted node.
    - A note that handlers are OPTIONAL: the engine's invokeJSFunc silently no-ops if the JS function is not defined.
    - A note that the api global is only bound after BindLifecycle runs, i.e. after `plugin load`.
    - A cross-link line to state-api.md (network context is reached through these hook payloads, not an api method) and to cli-reference.md (how to load a plugin).
    - A "Source" footer line citing internal/plugin/lifecycle.go as the backing file.
    Do NOT place a literal that any acceptance grep negative-checks; all greps below are positive presence checks.
  </action>
  <verify>
    <automated>test -f docs/plugins/lifecycle-hooks.md && grep -q "onRequest" docs/plugins/lifecycle-hooks.md && grep -q "onResponse" docs/plugins/lifecycle-hooks.md && grep -q "onLoad" docs/plugins/lifecycle-hooks.md && grep -q "onDOMNodeInserted" docs/plugins/lifecycle-hooks.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/plugins/lifecycle-hooks.md exists.
    - Contains all four JS handler names: onRequest, onResponse, onLoad, onDOMNodeInserted.
    - Contains all four proto type names: proto.NetworkRequestWillBeSent, proto.NetworkResponseReceived, proto.PageLoadEventFired, proto.DOMChildNodeInserted.
    - Contains the key payload field references: event.Request.URL, event.Response.Status.
    - States handlers are optional (mentions the no-op / undefined-handler behavior).
    - Cites internal/plugin/lifecycle.go in a Source reference.
    - Contains a cross-link to state-api.md and to cli-reference.md.
  </acceptance_criteria>
  <done>lifecycle-hooks.md documents all four hooks with handler name → proto type → payload fields, one worked snippet each, the optional-handler note, the api-bound-on-load note, cross-links, and a lifecycle.go source citation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation only) | This plan creates a Markdown file with no runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-D2 | Information Disclosure | docs/plugins/lifecycle-hooks.md | accept | A reference doc carries no runtime security surface. Content is sourced from already-public in-repo code; no secrets, endpoints, or credentials are introduced. No threats above low. |
</threat_model>

<verification>
- docs/plugins/lifecycle-hooks.md exists and contains all four hook names and all four proto type names.
- The page cites internal/plugin/lifecycle.go.
- No invented hooks beyond the four in BindLifecycle.
</verification>

<success_criteria>
- A reader can find each hook with its JS handler name, CDP proto type, and key payload fields, plus one worked snippet each.
- Optional-handler and api-bound-on-load notes are present.
- Source citation and cross-links present; every surface matches lifecycle.go.
</success_criteria>

<output>
Create `.planning/phases/21-reference-documentation/21-02-SUMMARY.md` when done.
</output>
