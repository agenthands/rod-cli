---
phase: 21-reference-documentation
plan: 04
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - docs/plugins/state-api.md
autonomous: true
requirements: [PDOC-03]
must_haves:
  truths:
    - "A reader can find the api global's GetSnapshot() and GetCookies() documented with return shapes and a worked usage snippet"
    - "A reader can find the newly-added GetLocalStorage() documented with its return shape"
    - "A reader learns network context is reached via lifecycle hook payloads, not an api method, with a cross-link to lifecycle-hooks.md"
    - "Every documented api method exists in internal/plugin/api.go"
  artifacts:
    - path: "docs/plugins/state-api.md"
      provides: "Authoritative reference for the api global's state/context accessors"
      contains: "GetLocalStorage"
  key_links:
    - from: "docs/plugins/state-api.md"
      to: "internal/plugin/api.go"
      via: "doc cites api.go as the backing source for GetSnapshot/GetCookies/GetLocalStorage"
      pattern: "internal/plugin/api.go"
    - from: "docs/plugins/state-api.md"
      to: "docs/plugins/lifecycle-hooks.md"
      via: "network-context section cross-links lifecycle-hooks.md because network state arrives via hook payloads"
      pattern: "lifecycle-hooks.md"
---

<objective>
Create `docs/plugins/state-api.md` — the authoritative reference for the `api` global's state/context accessors, grounded in `internal/plugin/api.go`. It documents `GetSnapshot()`, `GetCookies()`, AND the newly-added `GetLocalStorage()` (from Plan 01) with their return shapes and a worked snippet, and explains that network context is reached via lifecycle hook payloads (cross-linking lifecycle-hooks.md).

This plan depends on Plan 01 (the GetLocalStorage() engine fix) so the page documents a method that actually exists in source — Success Criterion #4, no invented surfaces.

Purpose: Satisfy ROADMAP Success Criterion #2 and PDOC-03 — a plugin author can look up the state/context API (snapshot, cookies, localStorage, network context) with usage examples.
Output: `docs/plugins/state-api.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-reference-documentation/21-CONTEXT.md
@.planning/phases/21-reference-documentation/21-PATTERNS.md

@internal/plugin/api.go
@internal/plugin/lifecycle.go
@plugins/examples/xss_scanner.js
@skills/rod-cli/references/storage-state.md
@.planning/phases/21-reference-documentation/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/plugins/state-api.md</name>
  <files>docs/plugins/state-api.md</files>
  <read_first>
    - internal/plugin/api.go (source of truth AFTER Plan 01 lands — GetCookies() lines 17-27 returns the CDP cookie array as interface{}; GetSnapshot() lines 29-35 returns (string HTML, error) via page.HTML(); the new GetLocalStorage() returns (interface{}, error), a key/value map of window.localStorage. Confirm the exact signatures before documenting them — do NOT document a method that is not present in this file.)
    - .planning/phases/21-reference-documentation/21-01-SUMMARY.md (confirms the GetLocalStorage() return representation chosen in Plan 01 — document whatever shape actually shipped)
    - internal/plugin/lifecycle.go (line 22: the api global is set via e.vm.Set("api", ...) inside BindLifecycle, i.e. only after `plugin load`)
    - plugins/examples/xss_scanner.js (worked snippet: api.GetSnapshot() call inside onLoad, lines 46-48)
    - skills/rod-cli/references/storage-state.md (tone/structure analog)
  </read_first>
  <action>
    Create docs/plugins/state-api.md following the shared reference template (H1 title, intro, H2 section per accessor, ```javascript blocks for usage). The docs/plugins/ directory already exists from Wave 1 — only this file is owned here.
    Required content, grounded in internal/plugin/api.go as it stands after Plan 01 (do NOT invent api methods beyond the three that exist: GetSnapshot, GetCookies, GetLocalStorage):
    - `api.GetSnapshot()` -> returns the full-page HTML as a string (backed by page.HTML()). Show the worked snippet adapted from xss_scanner.js onLoad: call api.GetSnapshot() and scan the returned HTML.
    - `api.GetCookies()` -> returns the page's cookie array (the CDP cookie objects, as a structured value). Describe the common fields a plugin reads and link to the go-rod cookie proto type for the full shape rather than dumping all fields inline (token-efficient depth per CONTEXT).
    - `api.GetLocalStorage()` -> returns the page's localStorage as a key/value object/map. Document the exact return shape that Plan 01 shipped (read 21-01-SUMMARY.md / api.go to confirm — map vs string). Show a short worked snippet reading a key.
    - A note that the api global is ONLY available after BindLifecycle runs, i.e. after `plugin load`; before that it is undefined (mirror the guard `typeof api !== "undefined"` used in xss_scanner.js).
    - A "Network context" section: state explicitly that network request/response data is NOT an api method — it arrives through the onRequest/onResponse lifecycle hook payloads (event.Request.URL/.Method, event.Response.URL/.Status). Cross-link to lifecycle-hooks.md for the payload shapes.
    - Cross-link to cli-reference.md (how to load the plugin that binds api).
    - A "Source" footer citing internal/plugin/api.go.
    All acceptance greps are positive presence checks.
  </action>
  <verify>
    <automated>test -f docs/plugins/state-api.md && grep -q "GetSnapshot" docs/plugins/state-api.md && grep -q "GetCookies" docs/plugins/state-api.md && grep -q "GetLocalStorage" docs/plugins/state-api.md && grep -q "lifecycle-hooks.md" docs/plugins/state-api.md && grep -q "func (a \*PluginAPI) GetLocalStorage" internal/plugin/api.go</automated>
  </verify>
  <acceptance_criteria>
    - docs/plugins/state-api.md exists.
    - Documents all three api methods: GetSnapshot, GetCookies, GetLocalStorage.
    - Each method's return shape is described (HTML string; cookie array; localStorage key/value map).
    - Contains a worked usage snippet (api.GetSnapshot() inside a handler).
    - States the api global is bound only after plugin load.
    - Has a Network context section that defers to lifecycle hook payloads and cross-links lifecycle-hooks.md.
    - Cites internal/plugin/api.go in a Source reference.
    - GUARD: internal/plugin/api.go actually contains `func (a *PluginAPI) GetLocalStorage` (the documented method exists in source — Plan 01 dependency satisfied).
  </acceptance_criteria>
  <done>state-api.md documents GetSnapshot/GetCookies/GetLocalStorage with return shapes and a worked snippet, notes api is bound on load, defers network context to lifecycle hooks with a cross-link, and cites api.go — and every documented method exists in source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation only) | This plan creates a Markdown file. The underlying GetLocalStorage() runtime surface is threat-modeled in Plan 01 (T-21-01/T-21-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-D4 | Information Disclosure | docs/plugins/state-api.md | accept | A reference doc carries no runtime security surface. It documents accessors (snapshot/cookies/localStorage) already exposed to the trusted plugin VM; the runtime exposure is accepted in Plan 01's register. No secrets are introduced in the doc. No threats above low. |
</threat_model>

<verification>
- docs/plugins/state-api.md exists and documents all three api methods.
- internal/plugin/api.go contains GetLocalStorage (Plan 01 must have landed).
- Network-context section cross-links lifecycle-hooks.md.
- Cites internal/plugin/api.go.
</verification>

<success_criteria>
- A reader can find GetSnapshot/GetCookies/GetLocalStorage with return shapes and a worked snippet, plus how localStorage and network context are reached.
- api-bound-on-load note present; network context deferred to hooks with cross-link.
- Every documented method exists in source after Plan 01.
</success_criteria>

<output>
Create `.planning/phases/21-reference-documentation/21-04-SUMMARY.md` when done.
</output>
