---
phase: 21-reference-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/plugins/cli-reference.md
autonomous: true
requirements: [PDOC-04]
must_haves:
  truths:
    - "A reader can find plugin load <path>, plugin list, and plugin run <name> documented with arguments, behavior, output format, and error/exit conditions"
    - "A reader learns that plugin load requires a path (client error 'plugin path is required') and that a missing file surfaces the daemon 'failed to open script file' error with non-zero exit"
    - "A reader learns plugin run is currently a stub and that hooks firing after plugin load is the real execution path"
    - "Every documented command matches actions/plugin.go and cmd.go"
  artifacts:
    - path: "docs/plugins/cli-reference.md"
      provides: "Authoritative reference for the plugin CLI commands"
      contains: "plugin path is required"
  key_links:
    - from: "docs/plugins/cli-reference.md"
      to: "actions/plugin.go"
      via: "doc cites actions/plugin.go and cmd.go as the backing sources for command behavior"
      pattern: "actions/plugin.go"
---

<objective>
Create `docs/plugins/cli-reference.md` — the authoritative reference for the three plugin CLI commands (`plugin load <path>`, `plugin list`, `plugin run <name>`), grounded in `cmd.go` and `actions/plugin.go`. Document each command's arguments, behavior, output format, and error/exit conditions. Critically, document `plugin run` HONESTLY as a current stub that needs a registry.

Purpose: Satisfy ROADMAP Success Criterion #3 and PDOC-04 — a user can look up every plugin CLI command with its flags and exit/error conditions.
Output: `docs/plugins/cli-reference.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-reference-documentation/21-CONTEXT.md
@.planning/phases/21-reference-documentation/21-PATTERNS.md

@cmd.go
@actions/plugin.go
@daemon/daemon.go
@skills/rod-cli/references/storage-state.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/plugins/cli-reference.md</name>
  <files>docs/plugins/cli-reference.md</files>
  <read_first>
    - cmd.go (the plugin command + load/list/run subcommands, ~lines 398-429: the load subcommand returns the client-side "plugin path is required" error when no path arg is given, line ~408; load/list/run dispatch daemon.Request commands "plugin-load"/"plugin-list"/"plugin-run", lines ~410, 417, 425)
    - actions/plugin.go (the daemon-side behavior: PluginLoad lines 11-28 returns "Plugin loaded successfully from %s" on success and surfaces the engine's "failed to open script file %s" error on a missing file; PluginList lines 31-42 returns "No active plugins loaded." or a JSON array; PluginRun lines 45-49 is a STUB returning "Triggered plugin %s" with the source comment that a registry is needed)
    - daemon/daemon.go (~lines 201-206: dispatch of plugin-load/plugin-list/plugin-run to the actions functions — confirms the thin-client → daemon mapping)
    - skills/rod-cli/references/storage-state.md (tone/structure analog; CLI invocations use ```bash fenced blocks)
  </read_first>
  <action>
    Create docs/plugins/cli-reference.md following the shared reference template (H1 title, intro paragraph, H2 section per command, ```bash blocks for invocations). The docs/plugins/ directory may already exist from a sibling plan in this wave — create it only if absent; only this file is owned here.
    Required content, grounded in cmd.go + actions/plugin.go (do NOT invent flags or commands beyond load/list/run):
    - `plugin load <path>`: takes one positional path argument. Behavior: loads and executes the script in the goja VM and binds lifecycle hooks. Success output: the string "Plugin loaded successfully from <path>". Error/exit conditions: (a) missing path -> client-side error "plugin path is required" with non-zero exit; (b) missing/unreadable file -> daemon error "failed to open script file <path>" with non-zero exit. Show a ```bash example invocation.
    - `plugin list`: takes no arguments. Output: "No active plugins loaded." when none, otherwise a JSON array of loaded plugin paths. Show a ```bash example and note the two output forms.
    - `plugin run <name>`: takes one positional name argument. Document HONESTLY: it is currently a STUB — actions.PluginRun returns "Triggered plugin <name>" without re-executing a registry-resolved plugin. Note (per the source comment) that real per-name execution needs a plugin registry, which is a known limitation. State clearly that the real execution path today is hooks firing continuously after `plugin load`. Cross-link to lifecycle-hooks.md for that.
    - A short note that these CLI commands are thin clients: they send daemon.Request{Command: "plugin-load"|"plugin-list"|"plugin-run"} to the daemon and print the response; a failed command yields a non-zero exit.
    - Cross-links to lifecycle-hooks.md (hooks are the live execution path) and state-api.md (what a loaded plugin can read).
    - A "Source" footer citing cmd.go and actions/plugin.go.
    Document error strings exactly as they appear in source so a verifier can grep them. All acceptance greps are positive presence checks.
  </action>
  <verify>
    <automated>test -f docs/plugins/cli-reference.md && grep -q "plugin load" docs/plugins/cli-reference.md && grep -q "plugin list" docs/plugins/cli-reference.md && grep -q "plugin run" docs/plugins/cli-reference.md && grep -q "plugin path is required" docs/plugins/cli-reference.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/plugins/cli-reference.md exists.
    - Documents all three commands: "plugin load", "plugin list", "plugin run".
    - Contains the literal client error string "plugin path is required".
    - Contains the daemon error reference "failed to open script file".
    - Contains the "No active plugins loaded." list output and notes the JSON-array form.
    - Documents plugin run as a stub and references the registry limitation, and states hooks-after-load is the real execution path.
    - Cites cmd.go and actions/plugin.go in a Source reference.
    - Contains cross-links to lifecycle-hooks.md and state-api.md.
  </acceptance_criteria>
  <done>cli-reference.md documents all three commands with arguments, behavior, output, and error/exit conditions, honestly flags plugin run as a stub, and cites cmd.go + actions/plugin.go.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation only) | This plan creates a Markdown file with no runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-D3 | Information Disclosure | docs/plugins/cli-reference.md | accept | A CLI reference doc carries no runtime security surface. It documents already-shipped commands; no secrets or credentials are introduced. Documenting `plugin run` as a stub is a transparency benefit, not a risk. No threats above low. |
</threat_model>

<verification>
- docs/plugins/cli-reference.md exists and documents all three commands.
- Contains exact error strings "plugin path is required" and "failed to open script file".
- plugin run is documented as a stub with the registry limitation.
- Cites cmd.go and actions/plugin.go.
</verification>

<success_criteria>
- A reader can find plugin load/list/run with arguments, behavior, output format, and exit/error conditions.
- plugin run is honestly documented as a current stub; hooks-after-load described as the real execution path.
- Source citation and cross-links present; every command matches source.
</success_criteria>

<output>
Create `.planning/phases/21-reference-documentation/21-03-SUMMARY.md` when done.
</output>
