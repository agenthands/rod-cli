---
phase: 27-canvas-webgl-webrtc-hardening
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - internal/detect/detect.js
  - tests/detection_test.go
autonomous: true
requirements: [HARDEN-01, HARDEN-02]
must_haves:
  truths:
    - "The webrtc_ice subtest is now required-GREEN: it asserts the live page leaks no real host IP (webrtcIce is empty, no-RTCPeerConnection, or a masked .local hostname — never a routable IPv4/IPv6), proving EvadeWebRTC + WithWebRTCLeakProtection fired. The KNOWN-RED wrapper is gone."
    - "A canvas_noise_stable subtest draws a fixed canvas, reads toDataURL twice in the same session, and asserts the two reads are byte-identical — proving stable-per-session noise (HARDEN-02 success criterion 2)."
    - "The canvas stability assertion still fails loudly if the canvas surface is unobservable (empty/undefined output), so a blanked probe cannot masquerade as a pass."
    - "The whole TestDetectionHarness suite passes against a freshly built binary with default toggles on."
  artifacts:
    - path: "internal/detect/detect.js"
      provides: "a canvasHash probe (deterministic-content canvas -> toDataURL) recorded into window.__detect for traceability"
      contains: "canvasHash"
    - path: "tests/detection_test.go"
      provides: "webrtc_ice flipped to required-green (no leaked host IP) + canvas_noise_stable subtest (two reads identical)"
      contains: "canvas_noise_stable"
  key_links:
    - from: "tests/detection_test.go (webrtc_ice)"
      to: "types/context.go (EvadeWebRTC wiring)"
      via: "asserts the live webrtcIce signal is not a routable IP — the observable effect of Plan 03 EvadeWebRTC + WithWebRTCLeakProtection"
      pattern: "webrtc_ice"
    - from: "tests/detection_test.go (canvas_noise_stable)"
      to: "../godoll/stealth/webgl.go (seeded canvas noise)"
      via: "two toDataURL reads in one session are identical — the seeded-PRNG stability contract from Plan 02 wired by Plan 03"
      pattern: "toDataURL"
---

<objective>
Flip the harness to assert both hardening features on the live page:
- **HARDEN-01:** replace the `webrtc_ice_known_red` baseline-record subtest with a required-green `webrtc_ice` assertion — no routable host IP leaks (empty / `no-RTCPeerConnection` / masked `.local` only).
- **HARDEN-02:** add a `canvas_noise_stable` subtest that reads the same canvas's `toDataURL` twice in one session and asserts byte-identical output (stable-per-session noise), plus a `canvasHash` probe in detect.js for traceability.

Purpose: This is the gate that proves Plans 02+03 actually work against a real browser. It is the "done" assertion for the phase per the harness-first milestone discipline.
Output: `canvasHash` probe in detect.js; flipped `webrtc_ice` subtest + new `canvas_noise_stable` subtest in detection_test.go.
</objective>

<context>
@.planning/phases/27-canvas-webgl-webrtc-hardening/27-CONTEXT.md
@.planning/phases/27-canvas-webgl-webrtc-hardening/27-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a canvasHash probe to detect.js</name>
  <files>internal/detect/detect.js</files>
  <read_first>
    - internal/detect/detect.js (the probe() helper :21-27; the webglVendor canvas-creation pattern :51-66; the async settle/ready structure :122-end)
  </read_first>
  <action>
    Add a synchronous `probe("canvasHash", function () { ... })` in the table-stakes block. It must: create a canvas, get the 2d context, draw deterministic fixed content (a fillText + a fillRect with hardcoded coordinates/colors — NO Date/random), then return `canvas.toDataURL()` (or a short hash of it). This is informational/traceability — it lets a human inspect the noised data URL. Keep it wrapped by the existing probe() try/catch. Do NOT make the drawn content depend on time or random so the only variability is the injected noise.
  </action>
  <acceptance_criteria>
    - `grep -n 'canvasHash' internal/detect/detect.js` matches
    - `grep -n 'toDataURL' internal/detect/detect.js` matches
    - the drawn content uses no `Date` / `Math.random` (`grep -n 'Math.random\|Date.now\|new Date' internal/detect/detect.js` returns nothing inside the canvasHash probe)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Flip webrtc_ice to a required-green no-leak assertion</name>
  <files>tests/detection_test.go</files>
  <read_first>
    - tests/detection_test.go (webrtc_ice_known_red subtest :351-373 — the current baseline-record version and its detailed comment about the SDP candidate connection-address field; evalDetect helper :219-230)
  </read_first>
  <action>
    Rename the subtest from `webrtc_ice_known_red` to `webrtc_ice` and change it to a required-green assertion. Read `ice := evalDetect(t, "webrtcIce")`. PASS conditions (no real host-IP leak):
      - `ice == ""` (no candidate gathered — the clean WithWebRTCLeakProtection effect), OR
      - `ice == "no-RTCPeerConnection"`, OR
      - every comma-separated token ends in `.local` (modern-Chrome mDNS masking — not a real IP).
    FAIL conditions: `ice == "undefined"` (surface unobservable — keep the loud-fail guard), an `error:` string, OR any token that parses as a routable IPv4/IPv6 address (use net.ParseIP on each token; if ParseIP succeeds and it is not a .local hostname → it is a leaked real IP → t.Errorf). Update the comment block to say HARDEN-01 is now wired and asserted (remove the "flips in Phase 27" KNOWN-RED language). Add `"net"` to the imports if not already present.
  </action>
  <acceptance_criteria>
    - `grep -n 'webrtc_ice_known_red' tests/detection_test.go` returns NOTHING (renamed/flipped)
    - `grep -n 't.Run("webrtc_ice"' tests/detection_test.go` matches
    - `grep -n 'net.ParseIP' tests/detection_test.go` matches (real-IP detection)
    - the subtest still fails on `ice == "undefined"` (loud-fail guard retained)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add the canvas_noise_stable subtest</name>
  <files>tests/detection_test.go</files>
  <read_first>
    - tests/detection_test.go (runCli usage + evalResultPrefix parsing in evalDetect :219-230; the t.Run subtest pattern; the suite setup that navigates to the fixture and the deterministic close/close around it)
  </read_first>
  <action>
    Add a `t.Run("canvas_noise_stable", func(t *testing.T) { ... })` subtest inside TestDetectionHarness (after the webrtc_ice subtest). It must eval a single self-contained JS expression via runCli("eval", expr) that:
      - creates a canvas, gets 2d context, draws the SAME deterministic content twice into two equal canvases (or draws once and calls toDataURL twice on the same canvas),
      - returns a string like `r1 === r2 ? "stable:" + r1.length : "UNSTABLE"`.
    Parse the eval result (reuse the evalResultPrefix slicing pattern from evalDetect, or call a small local helper). Assertions:
      - result must START WITH `"stable:"` → identical across the two reads (HARDEN-02 stability). If it is `"UNSTABLE"` → t.Errorf("canvas noise is not stable-per-session: two reads differ").
      - the trailing length must be > 0 (the data URL is non-empty) → guards against a blanked/unobservable canvas surface; if length is 0 or the output is empty/undefined → t.Errorf.
    Keep the eval expression free of Date/Math.random so the only source of variation would be a (bug) per-call-random noise implementation.
  </action>
  <acceptance_criteria>
    - `grep -n 't.Run("canvas_noise_stable"' tests/detection_test.go` matches
    - `grep -n 'UNSTABLE' tests/detection_test.go` matches (instability path errors)
    - `go vet ./tests/` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Rebuild first (tests exec the prebuilt binary): `go build -o rod-cli .`.
- Kill stale daemons with `pkill -9 -x rod-cli` (NEVER `pkill -f` — it self-matches the shell). Do NOT touch the user's interactive Chromium.
- `go test ./tests/ -run TestDetectionHarness -count=1` exits 0 with `webrtc_ice` and `canvas_noise_stable` both PASS.
- `go test ./tests/ -run TestNetworkEvasionHeaders -count=1` still passes (no header regression).
- Do NOT run the full `go test ./...` (browser-load timeout + the pre-existing flaky daemon_more_test.go).
</verification>
