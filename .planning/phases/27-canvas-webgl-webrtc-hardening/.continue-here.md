---
context: phase
phase: 27-canvas-webgl-webrtc-hardening
task: 8
total_tasks: 12
status: blocked
last_updated: 2026-06-24T14:07:38.661Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was hit through actual failure this session. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **anvil close-gate blocks phase/milestone complete** — `anvil-cc phase complete 27` (and `milestone complete`) fails closed: `manifest_unresolved[gsd/workflows/execute-phase.md]`. The project has no `gsd/` dir (gsd→anvil migration); the anvil §13.2 close-gate expects an in-project workflow manifest that does not exist. The `no_records_section` ledger findings are WARNINGS, not the blocker. Mitigation: resolve the manifest (or bypass with manual ROADMAP/STATE bookkeeping) — do NOT assume `phase complete` works.
- [ ] CONSTRAINT: **Stale prebuilt binary** — the `tests/` suite execs a PREBUILT `../rod-cli` binary, NOT freshly-compiled code. ALWAYS `go build -o rod-cli .` before any `runCli`-based test.
- [ ] CONSTRAINT: **`pkill -f` self-match** — `pkill -f "rod-cli ..."` kills the running shell (pattern is in its own argv). Use `pkill -9 -x rod-cli` (exact comm) only.
- [ ] CONSTRAINT: **godoll is a separate repo** — `../godoll` changes are uncommitted-by-design (consumed via `replace => ../godoll`). NEVER `git add ../godoll/*` from rod-cli.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-executing a done phase | Phase 27's 4 plans all have SUMMARYs and are qa-verified — re-running execute would redo finished work | blocking | Treat 27 as execution-complete; the ONLY remaining work is completion bookkeeping (task 8/9), blocked by the gate |
| Assuming `anvil-cc phase complete` works | It fails closed on the §13.2 manifest gate | blocking | Check `anvil-cc phase close-gate` first; resolve the manifest or use manual bookkeeping |
| Stale-binary test | `go test ./tests/...` without rebuilding `./rod-cli` first → false pass/fail | blocking | `go build -o rod-cli .` immediately before any runCli test |
| pkill self-match | `pkill -f "rod-cli ..."` kills the orchestrator shell | blocking | `pkill -9 -x rod-cli` (exact process name) |
| Killing the user's browser | Blanket chromium kill terminates the user's own interactive Chromium (`/usr/lib/chromium`) | advisory | Only kill `-x rod-cli` / `-x headless_shell` / `-x chrome` test browsers |
| Full `go test ./...` | Exceeds Go's 10-min timeout under browser load; includes pre-existing flaky daemon_more_test.go | advisory | Run targeted: `-run TestDetectionHarness`, `-run TestNetworkEvasionHeaders`, `./types ./actions`, godoll `./stealth` |

<current_state>
Phase 27 (Canvas/WebGL/WebRTC Hardening) is **fully executed and qa-verified PASSED**, but **not marked complete** because `anvil-cc phase complete 27` is blocked by the anvil §13.2 close-gate (missing in-project manifest `gsd/workflows/execute-phase.md`). The `/anvil-autonomous --from 27` run was stopped by the user to resolve the gsd→anvil migration; an `anvil-up` attempt errored (Claude CLI `--print` needs a prompt — unrelated to the gate). All targeted test suites are green. godoll edits remain uncommitted-by-design.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: 27-01 config + flags (WebRTCLeakProtection/CanvasNoise *bool, default-on, --webrtc-protection/--canvas-noise) — Done (7f2e162)
- Task 2: 27-02 godoll seeded canvas/audio noise + SpoofCanvas gate — Done (bd08cbc; godoll uncommitted)
- Task 3: 27-03 runtime wiring (session seed, gated EvadeWebRTC + WithWebRTCLeakProtection + SpoofCanvas/Audio) — Done (bd08cbc)
- Task 4: 27-04 harness (webrtc_ice required-green, canvas_noise_stable, audio_noise_stable, canvasHash) — Done (de3ff3a)
- Task 5: Code-review fixes CR-01..CR-05 — Done (09fe94a)
- Task 6: CR-02 *bool tri-state + round-trip test; CR-01 audio regression guard — Done (1e5a7f6, bab65d8, 7236c87)
- Task 7: qa goal verification PASSED 6/6 must-haves — Done (a357942)
</completed_work>

<remaining_work>

- Task 8: Resolve the anvil §13.2 close-gate (manifest setup / fix anvil-up / authorize manual bypass) — BLOCKED
- Task 9: Mark Phase 27 complete (ROADMAP [x] + date, advance STATE → 28, HARDEN-01/02 → validated)
- Task 10: Phase 28 — Human-Behavior Tuning (HUMANIZE-01)
- Task 11: Phase 29 — Best-Effort Live Validation (LIVEWAF-01)
- Task 12: Milestone v1.6 lifecycle (audit → complete → cleanup) — same close-gate will block here too
</remaining_work>

<decisions_made>

- Hardening toggles are `*bool` (tri-state), so a yaml-persisted `false` survives ResolveStealth (CR-02); precedence: explicit flag > non-nil yaml value (incl. false) > default true.
- The CanvasNoise toggle gates BOTH canvas and audio noise (profileFromStealth sets SpoofCanvas + SpoofAudioContext).
- Session noise seed lives on the Context (crypto/rand, once in NewContext) — launchBrowser takes Config by value so a seed there can't propagate; Context-level gives stable per-session noise.
- godoll changes stay uncommitted in ../godoll (replace pattern; CR-01 audio fix is in ../godoll/stealth/js.go).
- Stopped at the close-gate rather than bypassing — user's choice to resolve the migration.
</decisions_made>

<blockers>
- anvil §13.2 close-gate: `anvil-cc phase complete`/`milestone complete` fails closed on `manifest_unresolved[gsd/workflows/execute-phase.md]` (no `gsd/` dir in project). Workaround: create the gsd/workflows manifest(s), fix anvil-up, or do manual ROADMAP/STATE/REQUIREMENTS bookkeeping.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full machine-readable state (commit hashes, decisions, uncommitted files).
2. `.planning/phases/27-canvas-webgl-webrtc-hardening/27-VERIFICATION.md` — qa goal verification (status: passed, 6/6 must-haves).
3. `.planning/phases/27-canvas-webgl-webrtc-hardening/27-0{1,2,3,4}-SUMMARY.md` — per-plan build records.
4. Memory `anvil-close-gate-blocker` (in the project memory dir) — the gate root cause + resolution options.

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: re-running `/anvil-execute-phase 27` → it would redo finished, verified work. Mitigation: phase 27 execution is DONE; only completion bookkeeping remains.
- ANTI-PATTERN: assuming `anvil-cc phase complete` succeeds → it's gate-blocked. Mitigation: clear the gate first or bypass manually.

## Infrastructure State
- Branch: `main` (branching_strategy=none). rod-cli commits through `e49a1c3` (stop-point) — the latest is the pause WIP commit after this file.
- godoll sibling repo at `../godoll`, branch main, intentionally-uncommitted working-tree changes (phase 25/26/27).
- No rod-cli daemons should be running after the pause. Rebuild `./rod-cli` on resume before any test.
- anvil tooling: `anvil-cc` CLI on PATH (~/.local/bin/anvil-cc); anvil workflows in ~/.claude/anvil/workflows/; the close-gate looks for `./gsd/workflows/*.md` which does not exist.

<context>
The gsd→anvil migration happened mid-session (skills/agents → anvil-*, model → Opus 4.8). The engineering for phase 27 is complete, reviewed, and independently verified — the work is trustworthy. The only thing standing between here and a green Phase 27 (and the rest of the milestone) is the anvil bookkeeping close-gate, which is mis-pointed at a non-existent in-project manifest. This is an environment/setup issue, not a code issue.
</context>

<next_action>
Start with: confirm how the user wants the anvil §13.2 close-gate cleared (set up the gsd/workflows manifest, fix `anvil-up`, or authorize manual ROADMAP/STATE/REQUIREMENTS bookkeeping). Once unblocked, mark Phase 27 complete and resume `/anvil-autonomous --from 28`. Do NOT re-execute Phase 27 — verify with `ls .planning/phases/27-canvas-webgl-webrtc-hardening/*-SUMMARY.md` and `27-VERIFICATION.md` (status: passed) that the work is already done.
</next_action>
