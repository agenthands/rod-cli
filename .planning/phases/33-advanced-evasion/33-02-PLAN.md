---
phase: 33-advanced-evasion
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - internal/detect/probe.js
  - tests/detection_test.go
  - docs/stealth-config.md
autonomous: true
requirements: [EVAD-02, EVAD-03]
must_haves:
  truths:
    - "internal/detect/probe.js gains probes that read each hardened vector back from the LIVE page: fonts (a measureText/font-availability signature), media devices (navigator.mediaDevices.enumerateDevices() count/kinds), battery (navigator.getBattery present + plausible values), codecs (a canPlayType signature for a few types). probe.js is the shared source for harness + stealth-check."
    - "A harness test asserts, with the toggles ON (default), each vector reads back as COHERENT/non-headless-default (e.g. enumerateDevices is non-empty/plausible; fonts signature matches the profile OS; getBattery exists; codecs plausible) — read from the live page, never a Go field (validate-live-not-source). At least the 2+ EVAD-02 surfaces are asserted; cover all 4 if affordable, else a representative subset + note it."
    - "A STABILITY assertion proves a vector is consistent on re-read within a session (EVAD success criterion 4): read a vector's signature twice (or across an in-session re-read) and assert identical — matching the v1.6 canvas stability pattern."
    - "A TOGGLE-OFF assertion proves independence for at least one vector: with its toggle disabled, the harness shows that vector reverts to the un-hardened/default reading (so the toggle is provably effective, not cosmetic)."
    - "docs/stealth-config.md documents the 4 new toggles (names, default ON, CLI>profile>default precedence) with on/off guidance and a brief performance note (EVAD-03), and notes they are coherent per-profile-OS and real-Chrome (no TLS)."
    - "The harness additions are offline/deterministic and run under the default `go test ./...`; `go build ./...` clean."
  artifacts:
    - path: "internal/detect/probe.js"
      provides: "live-page probes for fonts/mediaDevices/battery/codecs"
      contains: "enumerateDevices"
    - path: "tests/detection_test.go"
      provides: "coherence + stability + toggle-off assertions for the new vectors"
      contains: "Spoof"
    - path: "docs/stealth-config.md"
      provides: "documentation of the 4 new hardening toggles (EVAD-03)"
      contains: "font-spoof"
  key_links:
    - from: "tests/detection_test.go"
      to: "internal/detect/probe.js"
      via: "harness reads the new vector probes back from the live page to assert coherence + stability"
      pattern: "probe"
---

<objective>
Prove the Phase 33 hardening (EVAD-02 "asserted by harness") and document it (EVAD-03).
Add live-page probes for fonts/mediaDevices/battery/codecs, then assert (a) coherence
when ON, (b) stability on re-read, and (c) toggle-off effectiveness — all offline and
deterministic. Document the 4 toggles in stealth-config.md.

Purpose: turn the 33-01 wiring into a regression-proof, honestly-documented guarantee.
Without the harness assertion, "hardened" is a claim; with it, a regression that
re-leaks a vector turns the build red.
</objective>

<context>
@.planning/phases/33-advanced-evasion/33-CONTEXT.md
@internal/detect/probe.js
@tests/detection_test.go
@docs/stealth-config.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add live-page probes for the 4 vectors</name>
  <files>internal/detect/probe.js</files>
  <read_first>
    - internal/detect/probe.js (the `probe(name, fn)` pattern; existing pluginsLength/webglVendor probes for the shape).
  </read_first>
  <action>
  Add probes (using the existing probe() helper): `fonts` — a stable signature of font
  availability (e.g. measureText widths for a handful of probe fonts, or a hash); 
  `mediaDevices` — `navigator.mediaDevices.enumerateDevices()` resolved to a count + the
  set of kinds (guard for promise/absence); `battery` — whether `navigator.getBattery`
  exists + level/charging (guard for absence/promise); `codecs` — `canPlayType` results
  for a few representative MIME types (e.g. mp4/h264, webm/vp9, ogg). Keep each defensive
  (try/catch, return a sentinel on error) so the probe page never throws. These are read
  by both the harness and stealth-check.
  </action>
  <verify>grep -n "fonts\|mediaDevices\|battery\|codecs\|enumerateDevices" internal/detect/probe.js</verify>
</task>

<task type="auto">
  <name>Task 2: Coherence + stability + toggle-off assertions</name>
  <files>tests/detection_test.go</files>
  <read_first>
    - tests/detection_test.go (harness boot on 127.0.0.1, runCli with stealth flags, reading probe values back via eval; the existing canvas-stability assertion pattern from Phase 27 if present).
    - types/config.go (the toggle flag names from 33-01 to drive on/off).
  </read_first>
  <action>
  Add harness subtests: (1) COHERENCE — with default (toggles ON), drive a built-in
  profile and assert each new probe reads back coherent/non-headless-default
  (enumerateDevices non-empty/plausible kinds; fonts signature present + OS-appropriate;
  getBattery exists; codecs plausible). Cover ≥2 vectors (all 4 if affordable; if not,
  assert a representative subset and document that the consistency/stability legs cover
  the rest). (2) STABILITY — read a vector's signature twice within a session and assert
  identical (EVAD criterion 4). (3) TOGGLE-OFF — for at least one vector, run with its
  --*-spoof=false and assert the probe reverts to the un-hardened default reading,
  proving the toggle is effective. Offline/deterministic; part of the default suite.
  Keep runtime reasonable (tests/ is slow — use a minimal profile set, run targeted).
  </action>
  <verify>cd /home/john/go/src/github.com/agenthands/rod-cli && go test ./tests/ -run "Spoof|Evasion|Dimension|Advanced" -count=1 2>&1 | tail -20</verify>
</task>

<task type="auto">
  <name>Task 3: Document the new toggles</name>
  <files>docs/stealth-config.md</files>
  <read_first>
    - docs/stealth-config.md (the toggles/precedence section; how canvas-noise/webrtc-protection are documented).
  </read_first>
  <action>
  Add the 4 new toggles (font-spoof, media-devices-spoof, battery-spoof, codec-spoof) to
  the config surface docs: each default ON, CLI>profile>default precedence, a one-line
  on/off guidance, and a brief performance note (script injection at page setup; cheap).
  State they are coherent per the profile's OS (Windows fonts on Windows profiles, etc.)
  and real-Chrome (no TLS spoofing — cross-ref stealth-validation.md). Keep it concise.
  </action>
  <verify>grep -n "font-spoof\|media-devices-spoof\|battery-spoof\|codec-spoof" docs/stealth-config.md</verify>
</task>

</tasks>

<must_have_truths>
See frontmatter. Load-bearing: live-page probes + coherence + STABILITY + a TOGGLE-OFF
assertion (the toggle is provably effective), all offline/deterministic in the default
suite, plus the EVAD-03 docs. This is what makes the 33-01 hardening real and durable.
</must_have_truths>
