---
phase: 33-advanced-evasion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types/context.go
  - types/config.go
  - cmd.go
  - ../godoll/stealth/evasion.go
autonomous: true
requirements: [EVAD-01, EVAD-02, EVAD-03]
must_haves:
  truths:
    - "rod-cli activates godoll's fingerprint-dimension injectors: createPage generates a fingerprint CONSTRAINED to the active profile's OS + locale (fingerprint.FPWithOS + FPWithLocales, OS mapped from profile.Platform: Win32→windows, MacIntel→macos) and calls em.SetFingerprint(fp) before Apply(), so applyFingerprintDimensions() actually runs (today em.fingerprint is nil and it is skipped)."
    - "The injected dimensions are OS-COHERENT with the profile (Windows fonts on a Windows profile, macOS fonts on a Mac profile) — NOT an unconstrained random fingerprint. (coherent-not-random lens.)"
    - "Four per-vector hardening toggles exist as `*bool` in StealthConfig following the CanvasNoise/WebRTCLeakProtection pattern — fonts, media devices, battery, codecs — each resolved CLI>profile>default via ResolveStealth, default ON (boolVal(x,true))."
    - "The toggles are INDEPENDENT and effective: turning a vector's toggle OFF prevents that dimension from being injected (≥2 must be independently provable; we ship 4). Gating is done either by extending godoll applyFingerprintDimensions to accept per-dimension options (godoll is vendored — allowed) or by zeroing the corresponding fingerprint fields before SetFingerprint."
    - "CLI flags for the toggles are forwarded into the daemon argv like the existing canvas/webrtc flags; precedence CLI>profile>default is preserved."
    - "Evasion-dimension failures log-and-continue (never abort the daemon), matching the existing em.Apply() discipline."
    - "`go build ./...` and `go vet ./...` pass (rod-cli AND ../godoll if modified); existing v1.6 evasion/harness tests still pass."
  artifacts:
    - path: "types/context.go"
      provides: "OS/locale-constrained fingerprint generation + em.SetFingerprint(fp) wiring + per-toggle gating"
      contains: "SetFingerprint"
    - path: "types/config.go"
      provides: "FontSpoof/MediaDevicesSpoof/BatterySpoof/CodecSpoof *bool toggles resolved via the precedence funnel"
      contains: "Spoof"
    - path: "cmd.go"
      provides: "CLI flags for the 4 toggles forwarded into daemon argv"
      contains: "spoof"
  key_links:
    - from: "types/context.go createPage"
      to: "../godoll/stealth/evasion.go applyFingerprintDimensions"
      via: "em.SetFingerprint(OS-constrained fp) activates the dormant injector path"
      pattern: "SetFingerprint"
---

<objective>
Activate the dormant godoll fingerprint-dimension hardening (fonts, media devices,
battery, codecs) in rod-cli, COHERENTLY (constrained to the profile's OS/locale) and
under per-vector CLI>profile>default toggles (EVAD-01/02/03). The one missing call —
`em.SetFingerprint(fp)` with an OS-constrained fingerprint — turns on injectors godoll
already ships but rod-cli never invokes.

Purpose: harden 4 real fingerprint vectors that currently leak headless-Chrome
defaults, without inventing new JS (godoll has it) and without breaking coherence
(the whole v1.6 ethos). The harness assertions + docs land in 33-02.
</objective>

<context>
@.planning/phases/33-advanced-evasion/33-CONTEXT.md
@types/context.go
@types/config.go
@../godoll/stealth/evasion.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: OS/locale-constrained fingerprint + SetFingerprint wiring</name>
  <files>types/context.go</files>
  <read_first>
    - types/context.go createPage (~617-655): the existing fp generation (`rodfingerprint.NewFingerprintGenerator(FPWithBrowserNames("chrome")).Generate()`), `ctx.fingerprint = fp`, SetProfile/SetNoiseSeed/Apply — and confirm SetFingerprint is NOT currently called.
    - ../godoll/stealth/evasion.go Apply (81) + SetFingerprint (74) + applyFingerprintDimensions (173).
    - ../godoll/fingerprint/generator.go FPWithOS (531), FPWithLocales (538), FPWithBrowserNames (529).
  </read_first>
  <action>
  In createPage, change the fingerprint generation to constrain by the active profile's
  OS and locale: map prof.Platform → OS string (Win32→"windows", MacIntel→"macos", else
  lowercase platform / "windows" fallback), and pass `fingerprint.FPWithBrowserNames("chrome")`,
  `fingerprint.FPWithOS(os)`, `fingerprint.FPWithLocales(prof.Locale)` (when locale set)
  to the generator. After generating, call `em.SetFingerprint(fp)` BEFORE `em.Apply()`
  so applyFingerprintDimensions runs with a coherent fingerprint. Keep the
  log-and-continue discipline (a generation/apply failure warns to stderr, never aborts).
  For the no-profile/default path, an unconstrained-but-single-draw fingerprint is
  acceptable (coherent-not-random lens override).
  </action>
  <verify>cd /home/john/go/src/github.com/agenthands/rod-cli && go build ./... && grep -n "SetFingerprint\|FPWithOS" types/context.go</verify>
</task>

<task type="auto">
  <name>Task 2: Per-vector toggles in StealthConfig + precedence</name>
  <files>types/config.go,cmd.go</files>
  <read_first>
    - types/config.go: CanvasNoise/WebRTCLeakProtection `*bool` fields (106-113) + how boolVal + ResolveStealth resolve them; StealthFlags (235+).
    - cmd.go: the --canvas-noise / --webrtc-protection flag definitions + daemon-argv forwarding (the template).
  </read_first>
  <action>
  Add four `*bool` StealthConfig fields — FontSpoof, MediaDevicesSpoof, BatterySpoof,
  CodecSpoof — with yaml/json tags, resolved through ResolveStealth exactly like
  CanvasNoise (CLI>profile>default, default ON via boolVal(x,true)). Add matching CLI
  flags in cmd.go (e.g. --font-spoof, --media-devices-spoof, --battery-spoof,
  --codec-spoof, all default true, allow =false to disable) and forward them into the
  daemon argv like the canvas/webrtc flags. Thread the resolved values into createPage
  so Task 3 can gate on them.
  </action>
  <verify>cd /home/john/go/src/github.com/agenthands/rod-cli && go build ./... && grep -n "FontSpoof\|MediaDevicesSpoof\|BatterySpoof\|CodecSpoof" types/config.go</verify>
</task>

<task type="auto">
  <name>Task 3: Per-dimension gating (independent toggles)</name>
  <files>../godoll/stealth/evasion.go,types/context.go</files>
  <read_first>
    - ../godoll/stealth/evasion.go applyFingerprintDimensions (173) — the all-or-nothing bundle to make selectable.
    - types/context.go (where the resolved toggles from Task 2 are available before Apply).
  </read_first>
  <action>
  Make the four dimensions individually gateable. Preferred: extend godoll with a
  per-dimension options struct (e.g. an exported `DimensionOptions{Fonts,MediaDevices,
  Battery,Codecs bool}` + a setter like `em.SetDimensionOptions(opts)` consulted inside
  applyFingerprintDimensions) — godoll is vendored (replace => ../godoll) so this is
  allowed; default all-true to preserve existing godoll callers. Then rod-cli passes the
  resolved FontSpoof/MediaDevicesSpoof/BatterySpoof/CodecSpoof values. Alternative if you
  prefer zero godoll change: in rod-cli, zero out fp.Fonts / fp.MediaDevices / fp.Battery
  / the codec fields before SetFingerprint when a toggle is off (verify each injector
  no-ops on empty input). Either way: a toggle OFF must prevent that dimension's
  injection, provably (33-02 asserts it). Keep ≥2 genuinely independent.
  </action>
  <verify>cd /home/john/go/src/github.com/agenthands/rod-cli && go build ./... && go vet ./... && go test ./types/ -run "Stealth|Resolve|Profile" -count=1 2>&1 | tail -15</verify>
</task>

</tasks>

<must_have_truths>
See frontmatter. Load-bearing: SetFingerprint(OS-constrained fp) activates the
dormant injectors COHERENTLY; 4 independent CLI>profile>default toggles (default on);
a toggle OFF provably suppresses its vector. No TLS, real Chrome. Harness proof in 33-02.
</must_have_truths>
