---
phase: 29-best-effort-live-validation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/stealth-validation.md
  - README.md
autonomous: true
requirements: [LIVEWAF-01]
must_haves:
  truths:
    - "docs/stealth-validation.md exists and states what Tier-1 (offline harness, blocking CI) deterministically proves vs what the Tier-2 live suite is best-effort about."
    - "The doc explicitly names the TLS-fingerprint (JA3/JA4), IP-reputation, and CDP-footprint layers a JS-injecting CLI CANNOT control, and makes NO 'undetectable' guarantee."
    - "The doc documents how to run the live suite manually: `go test -tags detection_live ./tests/ ...`."
    - "The new doc is linked from README.md (and/or ARCHITECTURE.md) so it is discoverable."
  artifacts:
    - path: "docs/stealth-validation.md"
      provides: "the honest Tier-1-vs-Tier-2 validation-ceiling doc (LIVEWAF-01 criterion 3)"
      contains: "undetectable"
    - path: "README.md"
      provides: "a link to docs/stealth-validation.md in the Documentation list"
      contains: "stealth-validation"
  key_links:
    - from: "docs/stealth-validation.md"
      to: "docs/cdp-footprint.md"
      via: "cross-reference the CDP-layer honesty doc as one of the layers the CLI cannot control"
      pattern: "cdp-footprint"
---

<objective>
Author the honest-ceiling documentation (LIVEWAF-01 criterion 3): a doc that
states plainly what rod-cli's stealth validation proves (Tier-1 offline, the
JS layer) vs what it cannot (the TLS/IP/CDP layers), with no "undetectable"
overclaim, plus how to run the opt-in live suite.

Purpose: the honesty deliverable is the point of this phase — it encodes the
project's validate-live-not-source / no-overclaim ethos so a user is not misled
into thinking a passing live check means undetectable.
Output: docs/stealth-validation.md + a discoverable link from README.md.
</objective>

<context>
@.planning/phases/29-best-effort-live-validation/29-CONTEXT.md
@.planning/phases/29-best-effort-live-validation/29-01-PLAN.md
@docs/cdp-footprint.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/stealth-validation.md (the honest ceiling)</name>
  <files>docs/stealth-validation.md</files>
  <read_first>
    - docs/cdp-footprint.md (existing CDP-layer honesty doc — match tone, cross-ref).
    - .planning/phases/29-best-effort-live-validation/29-CONTEXT.md (the Tier-1/Tier-2 framing).
  </read_first>
  <action>
  Write docs/stealth-validation.md with: (1) Tier-1 (offline harness, blocking CI) — what it deterministically proves: the JS-layer fingerprint signals read back from the live page (UA/Client-Hints/platform/WebGL/canvas/WebRTC/webdriver/plugins), per Phases 24-27. (2) Tier-2 (the opt-in live suite from Plan 01) — best-effort informational signal vs real WAFs, explicitly subject to the TLS-fingerprint (JA3/JA4), IP-reputation, and CDP-footprint layers a JS-injecting CLI cannot control (cross-ref docs/cdp-footprint.md). (3) A plain statement of the ceiling: NO "undetectable" guarantee. (4) How to run: `go test -tags detection_live ./tests/ -run ... -v`, noting failures are informational/non-blocking.
  </action>
  <verify>test -f docs/stealth-validation.md && grep -iE "tier|undetectable|tls|ja3|cdp|best-effort" docs/stealth-validation.md</verify>
</task>

<task type="auto">
  <name>Task 2: Link the doc from README</name>
  <files>README.md</files>
  <read_first>
    - README.md (the Documentation list ~:18 where ARCHITECTURE.md is linked).
  </read_first>
  <action>
  Add a link to docs/stealth-validation.md in the README Documentation list with a one-line honest description (e.g. "Stealth Validation: what the offline harness proves vs the best-effort live ceiling").
  </action>
  <verify>grep -n "stealth-validation" README.md</verify>
</task>

</tasks>

<must_have_truths>
See frontmatter. Load-bearing: the doc must NOT overclaim — a doc implying
"passes Cloudflare ⇒ undetectable" FAILS criterion 3. Name the uncontrollable
layers explicitly.
</must_have_truths>
