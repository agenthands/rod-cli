---
phase: 29-best-effort-live-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/detection_live_test.go
  - .github/workflows/test.yml
autonomous: true
requirements: [LIVEWAF-01]
must_haves:
  truths:
    - "tests/detection_live_test.go exists with //go:build detection_live as its first line and compiles ONLY under -tags detection_live."
    - "A plain `go build ./...` / `go test ./...` (no tag) does NOT compile or run the live file — it is invisible without the tag."
    - "The suite drives the REAL binary (runCli) against Cloudflare, DataDome, and CreepJS as independently-skippable subtests, reporting per-target verdicts INFORMATIONALLY (t.Logf / t.Skip) and NEVER t.Fatal/t.Errorf on a live detection or a network error (non-blocking discipline)."
    - ".github/workflows/test.yml runs plain `go test ./... -count=1` with NO -tags detection_live, and carries a comment documenting the deliberate exclusion so it is not accidentally re-enabled."
    - "`go build -tags detection_live ./...` compiles the live file successfully (it is valid Go, not just excluded dead code)."
  artifacts:
    - path: "tests/detection_live_test.go"
      provides: "build-tagged opt-in live smoke check vs Cloudflare/DataDome/CreepJS via runCli, informational/non-blocking reporting"
      contains: "//go:build detection_live"
    - path: ".github/workflows/test.yml"
      provides: "explanatory comment that the live suite is deliberately excluded (no -tags detection_live); gate command unchanged"
      contains: "detection_live"
  key_links:
    - from: "tests/detection_live_test.go"
      to: "tests/server.go"
      via: "runCli helper drives the prebuilt ../rod-cli binary against live target URLs"
      pattern: "runCli"
---

<objective>
Add the opt-in, non-blocking live smoke check behind `//go:build detection_live`
(LIVEWAF-01 criteria 1 + 2): a build-tagged test that drives the real binary
against Cloudflare / DataDome / CreepJS and reports per-target verdicts
informationally, excluded from the default test run and the push CI job by the
build tag alone.

Purpose: the Tier-2 best-effort counterpart to the Tier-1 offline harness. The
verifiable bar is EXCLUSION (tag-gated, CI never opts in) + NON-BLOCKING
reporting (log/skip, never hard-fail) — NOT a green live result (flaky by nature).
Output: tests/detection_live_test.go + an explanatory comment in test.yml.
</objective>

<context>
@.planning/phases/29-best-effort-live-validation/29-CONTEXT.md
@tests/detection_test.go
@.github/workflows/test.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build-tagged live smoke test driving the real binary, informational/non-blocking</name>
  <files>tests/detection_live_test.go</files>
  <read_first>
    - tests/detection_test.go (runCli("goto",url)+eval pattern, ready-gating, subtest shape).
    - tests/server.go (the runCli helper + how the binary is driven).
  </read_first>
  <action>
  Create tests/detection_live_test.go with `//go:build detection_live` as the FIRST line (Go 1.25 build-tag form), then `package tests`. Pin the three target URLs as named constants with a "third-party, best-effort, may change" comment. Add a subtest per target (Cloudflare, DataDome, CreepJS): goto the URL via runCli, read a verdict via eval/page state, and report it with t.Logf. Use t.Skip/t.Skipf when the target is unreachable or there is no network egress. NEVER t.Fatal/t.Errorf on a live detection or a network error — a flaky third-party challenge must not produce a red build. Each subtest independently skippable. Respect the binary-driving traps (the suite execs the prebuilt binary).
  </action>
  <verify>go build ./... ; go build -tags detection_live ./... ; go vet -tags detection_live ./tests/</verify>
</task>

<task type="auto">
  <name>Task 2: Document the deliberate CI exclusion in test.yml</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml (the "Run tests (headless)" step running `go test ./... -count=1`).
  </read_first>
  <action>
  Add a short comment above the test step stating the live suite (tests/detection_live_test.go, //go:build detection_live) is DELIBERATELY excluded from this blocking gate — run manually with `go test -tags detection_live ./tests/`. Do NOT change the gate command (no -tags). The exclusion is by construction; the comment prevents accidental re-enabling.
  </action>
  <verify>grep -n "detection_live" .github/workflows/test.yml</verify>
</task>

</tasks>

<must_have_truths>
See frontmatter. Load-bearing: the live file is INVISIBLE without the tag (plain
build/test excludes it) AND non-blocking within the suite (no hard-fail on a live
result). A future `go test ./...` and the CI job must be provably unaffected.
</must_have_truths>
