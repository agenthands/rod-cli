---
phase: 30-cdp-footprint-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types/context.go
  - types/config.go
  - cmd.go
  - docs/stealth-config.md
autonomous: true
requirements: [CDP-01]
must_haves:
  truths:
    - "A plain session (daemon spawned with no capture/log flags, then `goto`) installs NEITHER the `proto.RuntimeConsoleAPICalled` subscription NOR the `proto.NetworkRequestWillBeSent` subscription NOR an enabled `network.Interceptor` — so neither `Runtime.enable` nor `Network.enable` nor `Fetch.enable` is sent for the baseline path."
    - "HTTP↔JS identity coherence is preserved WITHOUT the interceptor: `createPage` calls `proto.EmulationSetUserAgentOverride` built from the active `stealth.Profile` (UserAgent, AcceptLanguage, Platform, and UserAgentMetadata.Brands derived via parseChromeMajor) so UA / Accept-Language / Sec-Ch-Ua / navigator.userAgentData all still agree with the JS identity (FINGERPRINT-01/02 invariant intact)."
    - "WIRE-VERIFY (the C→B fallback gate): the engineer captures actual outgoing request headers (via the offline harness / a request capture) and confirms `Sec-Ch-Ua` and the spoofed `Accept-Language` appear on the wire under Emulation override with NO Network.enable. If they do NOT, the plan falls back to Option B (`proto.NetworkSetUserAgentOverride` + `proto.NetworkSetExtraHTTPHeaders`, accepting `Network.enable`) and that choice is recorded in the SUMMARY. Either way header coherence MUST hold."
    - "Console capture is opt-in: a new global flag (e.g. `--console-capture`) threaded through `types.Config` (CLI>profile>default precedence, default OFF) gates the `RuntimeConsoleAPICalled` subscription; the `console` command documents that it requires the daemon spawned with capture on."
    - "Request logging is opt-in: a new global flag (e.g. `--request-capture`) gates the `NetworkRequestWillBeSent` subscription, default OFF, same precedence; the `requests` command documents the requirement."
    - "Mock routes still work: `AddRoute` lazily creates + `.Enable()`s the interceptor when none exists (footprint follows the feature), and identity headers continue to be applied; `RemoveRoute` of the last route may disable it. A session that never adds a route never enables Fetch."
    - "Plugins that hook OnRequest/OnResponse continue to function (they enable Network/Fetch on demand via the plugin engine, not via the baseline path)."
    - "`go build ./...` and `go test ./...` pass; existing v1.6 stealth-check / harness assertions for UA/CH coherence still pass."
  artifacts:
    - path: "types/context.go"
      provides: "minimal-baseline createPage: Emulation identity override (or Network fallback), opt-in console/request subscriptions, lazy interceptor via AddRoute/RemoveRoute"
      contains: "EmulationSetUserAgentOverride"
    - path: "types/config.go"
      provides: "ConsoleCapture / RequestCapture config fields + resolution through the existing precedence resolver"
      contains: "Capture"
    - path: "cmd.go"
      provides: "--console-capture and --request-capture global flags forwarded into the daemon spawn argv like the other stealth flags"
      contains: "console-capture"
    - path: "docs/stealth-config.md"
      provides: "documents the new opt-in capture flags and the minimal-by-default CDP behavior"
      contains: "console-capture"
  key_links:
    - from: "types/context.go createPage"
      to: "stealth.Profile (ctx.profile)"
      via: "EmulationSetUserAgentOverride built from the active profile so HTTP identity == JS identity"
      pattern: "EmulationSetUserAgentOverride"
    - from: "cmd.go global flags"
      to: "types.Config (ResolveStealth path)"
      via: "capture flags resolved once at daemon spawn, CLI>profile>default"
      pattern: "Capture"
---

<objective>
Drive the baseline CDP surface to the minimum (CDP-01): a plain `goto` session
must enable NO Runtime, Network, or Fetch domain. Make the three currently
unconditional footprint sources in `types/context.go createPage()` —
console-capture (`RuntimeConsoleAPICalled`), request-logging
(`NetworkRequestWillBeSent`), and the always-on `network.Interceptor` — lazy /
opt-in, WITHOUT regressing the v1.6 HTTP↔JS header-coherence invariant.

Identity coherence moves off the interceptor and onto
`proto.EmulationSetUserAgentOverride` (Emulation domain has no `enable`, so it
costs zero CDP footprint; recorded design decision C+D in CONTEXT D-09). The
interceptor becomes lazy — enabled only when a mock route (or strict-header mode)
needs it. Console + request capture become opt-in flags (CLI>profile>default,
default OFF) — "nothing always-on" (CONTEXT D-02).

Purpose: this is the actual reduction the phase headline promises. Verifiable bar:
a plain session sends none of {Runtime.enable, Network.enable, Fetch.enable} AND
the spoofed UA/Accept-Language/Sec-Ch-Ua still agree with the JS identity.
</objective>

<context>
@.planning/phases/30-cdp-footprint-reduction/30-CONTEXT.md
@types/context.go
@types/config.go
@docs/stealth-config.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move identity coherence to Emulation.setUserAgentOverride (verify wire propagation; B fallback)</name>
  <files>types/context.go</files>
  <read_first>
    - types/context.go (createPage ~517-585: profile build at 536-537 `prof := profileFromStealth(...)`; em.Apply() at 542; the catch-all identity rule in updateInterceptorRules ~625-676; defaultChromeMajor const ~447; parseChromeMajor usage ~657-669).
    - go-rod proto: /home/john/go/pkg/mod/github.com/go-rod/rod@v0.116.2/lib/proto/emulation.go (EmulationSetUserAgentOverride + EmulationUserAgentMetadata / EmulationUserAgentBrandVersion) and network.go (NetworkSetUserAgentOverride, NetworkSetExtraHTTPHeaders) for the fallback.
  </read_first>
  <action>
  After `em.Apply()` in createPage (around line 544), set the HTTP identity natively via Chrome's Emulation domain so the per-request interceptor identity rule is no longer required. Call `proto.EmulationSetUserAgentOverride{ UserAgent: prof.UserAgent, AcceptLanguage: prof.AcceptLanguage, Platform: prof.Platform, UserAgentMetadata: &proto.EmulationUserAgentMetadata{ Brands: <derived from prof>, Platform: <chPlatform mapping copied from updateInterceptorRules: Win32→"Windows", MacIntel→"macOS", else prof.Platform>, Mobile: false } }.Call(page)` — derive the Chrome major via the existing `parseChromeMajor(prof.UserAgent)` (fallback `defaultChromeMajor`) and build Brands as `{"Not A(Brand","99"}, {"Google Chrome",major}, {"Chromium",major}` mirroring the exact Sec-Ch-Ua tuple currently produced at context.go ~671. Only set UA-metadata when `prof.SpoofClientHints` (match current gating). Log-and-continue on error (same discipline as em.Apply, do not abort the daemon).
  Then REMOVE the always-on identity application: the catch-all `URLPattern:"*"`, `network.Continue` ModifiedHeaders rule must no longer be installed on the baseline path (it moves to the lazy-interceptor path only if still needed for mock routes). Do NOT delete `updateInterceptorRules`'s mock-route handling.
  WIRE-VERIFY before trusting this: build the binary, drive a request against the offline harness (or any 127.0.0.1 capture) and confirm the OUTGOING request carries the spoofed `Sec-Ch-Ua` and `Accept-Language` with Network NOT enabled. If they are absent, FALL BACK to Option B: use `proto.NetworkSetUserAgentOverride{...same fields...}` plus `proto.NetworkSetExtraHTTPHeaders` for residual headers (this accepts `Network.enable` on the baseline) and record the fallback + reason in the SUMMARY. Coherence MUST hold either way.
  </action>
  <verify>go build ./... && go vet ./types/</verify>
</task>

<task type="auto">
  <name>Task 2: Gate console + request subscriptions behind opt-in config (default OFF)</name>
  <files>types/config.go,types/context.go</files>
  <read_first>
    - types/config.go (Config / StealthConfig / StealthFlags structs and ResolveStealth — the single precedence resolver, CLI>profile>default; boolVal helper).
    - types/context.go (the two `go page.EachEvent(...)` blocks ~558 RuntimeConsoleAPICalled and ~570 NetworkRequestWillBeSent; ctx.config access).
  </read_first>
  <action>
  Add two opt-in fields resolved through the existing precedence machinery: `ConsoleCapture` and `RequestCapture` (pointer-bool or the same shape the other resolved toggles use), default OFF. In createPage, wrap the `RuntimeConsoleAPICalled` subscription so it is installed ONLY when console capture is enabled (`boolVal(ctx.config.<...>.ConsoleCapture, false)`), and the `NetworkRequestWillBeSent` subscription ONLY when request capture is enabled. When OFF, the subscription is not registered at all (so the domain is never enabled). Keep the existing append-to-slice bodies unchanged when ON.
  </action>
  <verify>go build ./... && grep -n "ConsoleCapture\|RequestCapture" types/config.go types/context.go</verify>
</task>

<task type="auto">
  <name>Task 3: Lazy interceptor lifecycle for mock routes</name>
  <files>types/context.go</files>
  <read_first>
    - types/context.go (createPage interceptor setup ~577-582; updateInterceptorRules ~603-676; AddRoute/RemoveRoute ~679-695; ctx.interceptor field ~228, ctx.routes).
    - ../godoll/network/ (NewInterceptor, Enable, and whether a Disable/Stop exists; InterceptRule, Mock/Continue actions).
  </read_first>
  <action>
  Remove the unconditional interceptor create+Enable from createPage's baseline. Instead, make it lazy: in `AddRoute`, if `ctx.interceptor == nil`, create it (`network.NewInterceptor(ctx.page)`), then `updateInterceptorRules()` + `Enable()`. In `RemoveRoute`, when the last route is removed, optionally disable/clear the interceptor if godoll exposes a teardown (else clear rules). `updateInterceptorRules` keeps applying mock-route rules; the identity catch-all rule moved to Emulation (Task 1) so it is no longer needed here unless a future strict-header mode re-adds it. A session that never calls AddRoute must never enable Fetch.
  </action>
  <verify>go build ./... && go test ./types/ ./actions/ 2>&1 | tail -20</verify>
</task>

<task type="auto">
  <name>Task 4: Wire --console-capture / --request-capture flags + document</name>
  <files>cmd.go,docs/stealth-config.md</files>
  <read_first>
    - cmd.go (global flags block + runClientCommand which forwards non-secret stealth flags into the daemon spawn argv; the existing `console` ~699 and `requests` ~706 subcommands).
    - docs/stealth-config.md (the precedence-chain documentation + flag table).
  </read_first>
  <action>
  Add two global bool flags `--console-capture` and `--request-capture` (default false) and forward them into the daemon spawn argv exactly like the other non-secret stealth flags, so they resolve once at spawn. Update the `console` and `requests` subcommand Usage strings to note they require the session started with the matching capture flag. In docs/stealth-config.md add a short "CDP footprint / capture" subsection: minimal-by-default (no Runtime/Network/Fetch on a plain session), and the two opt-in flags with their CLI>profile>default precedence. No dedicated --cdp-stealth flag (CONTEXT D-07).
  </action>
  <verify>go build ./... && grep -n "console-capture\|request-capture" cmd.go docs/stealth-config.md</verify>
</task>

</tasks>

<must_have_truths>
See frontmatter. Load-bearing: (1) a plain `goto` enables none of Runtime/Network/
Fetch; (2) UA/Accept-Language/Sec-Ch-Ua/userAgentData still agree with the JS
identity (the WIRE-VERIFY gate decides C vs the B fallback — coherence is
non-negotiable); (3) capture is opt-in, default OFF; (4) mock routes + plugins
still work via lazy on-demand enabling. The reduction is real, not just documented.
</must_have_truths>
