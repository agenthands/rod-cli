---
phase: 30-cdp-footprint-reduction
plan: 03
type: execute
wave: 3
depends_on: ["30-01", "30-02"]
files_modified:
  - docs/cdp-footprint.md
  - docs/stealth-validation.md
  - tests/detection_live_test.go
autonomous: true
requirements: [CDP-02, CDP-03]
must_haves:
  truths:
    - "docs/cdp-footprint.md is rewritten from the Phase-24 findings note into a CURRENT inventory: a per-command/per-domain table where every CDP domain rod-cli can enable (Runtime, Network, Fetch, Page, DOM, Emulation, plus any others actually used) has columns: when it is sent / what triggers it / mitigation status (one of reduced | obfuscated | accepted-visible)."
    - "The table reflects the 30-01 reality: Runtime/Network/Fetch are `reduced` (now opt-in / lazy, off on a plain session); Emulation identity override is noted as zero-enable; whatever the WIRE-VERIFY outcome was (C vs B fallback) is documented accurately."
    - "An explicit Honest Ceiling section names what remains detectable and what cannot be fixed without browser-patching / MITM / alternate transport, and defers that to v2 CDP-DEEP-01 by name. No 'undetectable' claim."
    - "docs/cdp-footprint.md cross-links docs/stealth-validation.md (Tier-1 offline blocking vs Tier-2 live opt-in), and stealth-validation.md links back to the updated cdp-footprint.md."
    - "Live measurement (CDP-03) reuses the Phase-29 Tier-2 suite: tests/detection_live_test.go gains an informational, non-blocking CDP-footprint observation (or a documented note) against the live targets — t.Logf/t.Skip only, NEVER t.Fatal/t.Errorf, still excluded from the CI gate by the //go:build detection_live tag."
    - "Every claim in cdp-footprint.md is true against HEAD (no overclaiming); `go build -tags detection_live ./...` still compiles."
  artifacts:
    - path: "docs/cdp-footprint.md"
      provides: "current per-command CDP inventory with mitigation status + honest ceiling, cross-linked to stealth-validation"
      contains: "accepted-visible"
    - path: "docs/stealth-validation.md"
      provides: "back-link to the updated cdp-footprint.md Tier framing"
      contains: "cdp-footprint"
    - path: "tests/detection_live_test.go"
      provides: "informational, non-blocking CDP-footprint observation against live targets (Tier-2), tag-gated"
      contains: "//go:build detection_live"
  key_links:
    - from: "docs/cdp-footprint.md"
      to: "docs/stealth-validation.md"
      via: "Tier-1/Tier-2 cross-reference"
      pattern: "stealth-validation"
---

<objective>
Deliver the inventory + honest-ceiling documentation (CDP-02) and the live-target
measurement (CDP-03 live leg). Rewrite docs/cdp-footprint.md from the Phase-24
"Answer: No" findings note into a current per-command table (command/domain → when
sent → status reduced|obfuscated|accepted-visible) reflecting the 30-01 reduction
and the 30-02 instrumentation, with an explicit honest-ceiling section deferring
deeper obfuscation to v2 CDP-DEEP-01. Reuse the Phase-29 non-blocking live suite
for an informational CDP-footprint observation against real targets.

Purpose: close CDP-02 (inventory with mitigation status) and the CDP-03 "measured
against live targets" leg, honestly — matching the project's validate-live-not-source,
no-overclaim ethos.
</objective>

<context>
@.planning/phases/30-cdp-footprint-reduction/30-CONTEXT.md
@docs/cdp-footprint.md
@docs/stealth-validation.md
@tests/detection_live_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/cdp-footprint.md into a current inventory + honest ceiling</name>
  <files>docs/cdp-footprint.md</files>
  <read_first>
    - docs/cdp-footprint.md (the Phase-24 findings note being superseded).
    - 30-01-PLAN.md and its SUMMARY, 30-02-PLAN.md and its SUMMARY (the actual reduction + instrumentation that landed, incl. the C-vs-B WIRE-VERIFY outcome).
    - types/context.go (HEAD — confirm which domains are enabled, when, after 30-01/30-02).
    - docs/stealth-validation.md (Tier-1/Tier-2 framing to cross-link).
  </read_first>
  <action>
  Replace the note with a current inventory. Include a table with a row per CDP
  domain rod-cli can enable — Runtime, Network, Fetch, Page, DOM, Emulation, and any
  other actually used — columns: Domain/Command | When sent / trigger | Status
  (reduced | obfuscated | accepted-visible). Mark Runtime/Network/Fetch as `reduced`
  (opt-in/lazy, off on a plain session, with the triggering flag/feature named);
  note Emulation.setUserAgentOverride as zero-enable identity; mark structurally-
  required domains (e.g. Page) honestly as accepted-visible with the reason. Apply
  the CONTEXT `earn-accepted-visible` lens: do not rubber-stamp — each accepted-
  visible row states WHY reduction is infeasible. Add an "Honest Ceiling" section:
  what remains detectable, and that full CDP obfuscation needs browser-patching/MITM
  → deferred to v2 CDP-DEEP-01 (name it). State no "undetectable" claim. Cross-link
  docs/stealth-validation.md (Tier-1 offline blocking harness asserts the baseline
  per 30-02; Tier-2 live is informational).
  </action>
  <verify>grep -n "reduced\|accepted-visible\|CDP-DEEP-01\|stealth-validation" docs/cdp-footprint.md</verify>
</task>

<task type="auto">
  <name>Task 2: Back-link stealth-validation.md and reconcile its CDP statements</name>
  <files>docs/stealth-validation.md</files>
  <read_first>
    - docs/stealth-validation.md (its current CDP-footprint mention / Tier framing).
    - docs/cdp-footprint.md (the rewritten doc from Task 1).
  </read_first>
  <action>
  Add/refresh a link from stealth-validation.md to docs/cdp-footprint.md, and update
  any statement that implied the CDP footprint is purely an unavoidable ceiling
  (Phase-24 framing) to reflect that Runtime/Network/Fetch are now reduced/opt-in and
  asserted by the offline harness baseline (30-02). Keep the Tier-1/Tier-2 honesty
  framing intact.
  </action>
  <verify>grep -n "cdp-footprint" docs/stealth-validation.md</verify>
</task>

<task type="auto">
  <name>Task 3: Informational CDP-footprint observation in the live (Tier-2) suite</name>
  <files>tests/detection_live_test.go</files>
  <read_first>
    - tests/detection_live_test.go (Phase-29 structure: //go:build detection_live first line, per-target subtests, t.Logf/t.Skip non-blocking discipline, runCli).
    - 30-CONTEXT.md (D-06: reuse this suite, non-blocking, NOT in CI gate).
  </read_first>
  <action>
  Extend the existing live suite with an INFORMATIONAL CDP-footprint observation
  against the live targets (e.g. log the enabled-domain ledger / cdpTell verdict per
  target for the plain-session path), reported with t.Logf and t.Skip/t.Skipf only —
  NEVER t.Fatal/t.Errorf. Keep the //go:build detection_live tag as the first line so
  it stays excluded from the blocking CI gate (do not add the tag to test.yml). This
  is the CDP-03 "measured against live targets" leg, honestly best-effort. If a clean
  live measurement is impractical, add a documented note in cdp-footprint.md instead
  and keep the suite change minimal — but prefer a real informational observation.
  </action>
  <verify>go build ./... && go build -tags detection_live ./... && grep -n "//go:build detection_live" tests/detection_live_test.go</verify>
</task>

</tasks>

<must_have_truths>
See frontmatter. Load-bearing: the inventory is CURRENT and TRUE against HEAD (no
overclaiming), every accepted-visible row earns it, the honest ceiling names
v2 CDP-DEEP-01, and the live measurement stays informational/non-blocking and out
of the CI gate. CDP-02 + the CDP-03 live leg close here; the deterministic CDP-03
gate lives in 30-02.
</must_have_truths>
