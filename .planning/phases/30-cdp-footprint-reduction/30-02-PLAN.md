---
phase: 30-cdp-footprint-reduction
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - types/context.go
  - internal/detect/server_test.go
  - tests/detection_test.go
autonomous: true
requirements: [CDP-01, CDP-03]
must_haves:
  truths:
    - "The daemon records, per session, which CDP domains it enabled (at minimum a boolean/ledger for Runtime, Network, Fetch) via a deterministic instrumentation point — readable by a test (e.g. exposed through an existing inspection command, a daemon field, or a documented log line)."
    - "An offline, deterministic harness/test asserts the BASELINE: a plain `goto` session (no capture flags, no routes, no plugins) enabled ZERO of {Runtime, Network, Fetch}. This is the falsifiable CDP-01/CDP-03 reduction gate and runs under plain `go test ./...` (no special tag)."
    - "A complementary positive assertion proves the instrumentation is real (not vacuously zero): a session started WITH --console-capture records Runtime enabled (or WITH a mock route records Fetch enabled), so the test distinguishes reduced from broken."
    - "The assertion is wired into the existing offline harness ethos (internal/detect + tests/) — no network egress, deterministic green/red — consistent with Phase 24."
    - "`go test ./...` passes including the new baseline assertion."
  artifacts:
    - path: "types/context.go"
      provides: "per-session CDP domain-enable ledger / instrumentation hook the harness can read"
      contains: "cdp"
    - path: "internal/detect/server_test.go"
      provides: "or tests/detection_test.go — the deterministic baseline assertion (plain goto enables zero of Runtime/Network/Fetch) + a positive control"
      contains: "Runtime"
  key_links:
    - from: "internal/detect or tests harness"
      to: "types/context.go instrumentation"
      via: "reads the per-session enabled-domain ledger to assert the baseline is minimal"
      pattern: "cdp"
---

<objective>
Make the CDP-footprint reduction MEASURABLE and asserted (CDP-01 "measured",
CDP-03 "harness asserts the reduction baseline"). Instrument the daemon to record
which CDP domains a session enables, then add a deterministic offline assertion
that a plain `goto` session enables ZERO of {Runtime, Network, Fetch}, plus a
positive control proving the instrumentation actually fires when a footprint
feature is turned on.

Purpose: turn the 30-01 reduction from a claim into a regression-proof gate, in
the Phase-24 harness style (offline, deterministic, runs on every push). The
instrumented command-count approach is the chosen measurement (CONTEXT D-04); the
existing informational `window.__detect.cdpTell` JS probe stays as-is (D-05).
</objective>

<context>
@.planning/phases/30-cdp-footprint-reduction/30-CONTEXT.md
@types/context.go
@internal/detect/server_test.go
@tests/detection_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument per-session CDP domain enablement</name>
  <files>types/context.go</files>
  <read_first>
    - types/context.go (createPage; the points where Runtime/Network subscriptions and the lazy interceptor get enabled AFTER 30-01; ctx struct fields ~226-232; existing accessors like GetConsoleLogs for the accessor pattern).
    - 30-01-PLAN.md (so the ledger is set exactly where each domain is actually enabled).
  </read_first>
  <action>
  Add a per-session ledger on Context recording which CDP domains were enabled —
  at minimum bools for Runtime, Network, Fetch (or a `map[string]bool` /
  `[]string`). Set Runtime=true where the RuntimeConsoleAPICalled subscription is
  installed (console capture on), Network=true where NetworkRequestWillBeSent is
  installed (request capture on) OR where the B-fallback Network override path runs,
  Fetch=true where the lazy interceptor is Enabled (AddRoute path). Expose it for
  tests via a small accessor (e.g. `GetEnabledCDPDomains()`) AND/OR surface it
  through the daemon's existing inspection surface so a test driving the real binary
  can read it (engineer's discretion: an accessor used by an internal test is
  sufficient; if the harness drives the binary out-of-process, add a tiny readback
  e.g. via an existing eval/debug path or a documented stderr line). Keep it cheap
  and lock-safe (reuse stateLock).
  </action>
  <verify>go build ./... && grep -ni "fetch\|runtime\|network" types/context.go | grep -i "enabl\|ledger\|domains" | head</verify>
</task>

<task type="auto">
  <name>Task 2: Deterministic baseline assertion + positive control</name>
  <files>internal/detect/server_test.go,tests/detection_test.go</files>
  <read_first>
    - internal/detect/server_test.go and tests/detection_test.go (how the offline harness boots 127.0.0.1, drives rod-cli, reads back state; the runCli helper; ready-gating).
    - types/context.go (the accessor from Task 1).
  </read_first>
  <action>
  Add a test (in whichever harness layer can read the Task-1 ledger most directly —
  prefer an in-process types test if the ledger is a Context accessor, else a
  binary-driven tests/ test). BASELINE: start a plain session (no --console-capture,
  no --request-capture, no routes/plugins), `goto` the offline fixture, assert the
  enabled-domain ledger contains NONE of Runtime/Network/Fetch (allow Page/DOM which
  go-rod needs structurally). POSITIVE CONTROL: start a second session WITH
  --console-capture (assert Runtime recorded) and/or add a mock route (assert Fetch
  recorded) — proving the assertion is not vacuously passing. Keep it offline and
  deterministic (no network egress); it must run under plain `go test ./...`.
  </action>
  <verify>go test ./internal/detect/ ./tests/ -run "CDP|Footprint|Baseline" -count=1 2>&1 | tail -20</verify>
</task>

</tasks>

<must_have_truths>
See frontmatter. Load-bearing: the baseline assertion is deterministic, offline,
and FALSIFIABLE (a positive control proves it can go red), so a future regression
that re-enables Runtime/Fetch on a plain session turns the build red. This is the
CDP-03 "harness asserts the reduction baseline" deliverable.
</must_have_truths>
