---
phase: 22-example-plugins
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/examples/xss_scanner.js
autonomous: true
requirements: [PEX-01]
tags: [plugin, examples, xss, javascript]

must_haves:
  truths:
    - "The polished xss_scanner.js loads via `plugin load` with no errors"
    - "getFindings and getRequestLog accessors remain and return clean JSON for `plugin run`"
    - "onRequest, onResponse, and onLoad hooks are preserved and well-commented"
  artifacts:
    - path: "plugins/examples/xss_scanner.js"
      provides: "Polished flagship XSS scanner example: hooks + findings/requestLog accessors"
      contains: "function getFindings"
  key_links:
    - from: "plugins/examples/xss_scanner.js"
      to: "internal/plugin/engine.go"
      via: "getFindings accessor invoked by `plugin run getFindings` via RunFunc"
      pattern: "function getFindings"
    - from: "plugins/examples/xss_scanner.js"
      to: "internal/plugin/api.go"
      via: "onLoad reads api.GetSnapshot under typeof guard"
      pattern: "api.GetSnapshot"
---

<objective>
Polish the EXISTING flagship example `plugins/examples/xss_scanner.js` (PEX-01) so it reads as a complete, well-structured worked example: clearer comments and section structure, a clean `plugin load`, correct `api` usage under a guard, and preserved `getFindings`/`getRequestLog` accessors so `plugin run getFindings` returns the collected findings JSON. Polish in place — do NOT rewrite from scratch and do NOT remove the accessors.

XSS payloads and exploit logic stay in this user-space example script, never in the core binary (REQUIREMENTS Out of Scope).

Purpose: PEX-01 needs a polished, runnable flagship the xss-scanner doc (Plan 04) walks through end-to-end.
Output: polished xss_scanner.js (same file, improved).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-example-plugins/22-CONTEXT.md
@.planning/phases/22-example-plugins/22-PATTERNS.md
@plugins/examples/xss_scanner.js
@docs/plugins/lifecycle-hooks.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish xss_scanner.js in place</name>
  <files>plugins/examples/xss_scanner.js</files>
  <read_first>
    - plugins/examples/xss_scanner.js (the file being polished — preserve: module-level `findings`/`requestLog` arrays lines 5-6, `onRequest`/`onResponse`/`onLoad` hooks lines 17-65, `getFindings`/`getRequestLog` accessors lines 67-75, and the `typeof api !== "undefined"` guard line 46)
    - docs/plugins/lifecycle-hooks.md lines 22-24 (the typeof api guard requirement) and lines 9-16 (payload field names)
    - .planning/phases/22-example-plugins/22-PATTERNS.md lines 155-163 (polish constraints: do not rewrite, keep accessors)
  </read_first>
  <action>
    Improve the existing script without changing its demonstrable behavior:
    - Add a clear top-of-file comment block: what the plugin does, how to run it (`plugin load ./plugins/examples/xss_scanner.js`, drive a vulnerable page, then `plugin run getFindings` / `plugin run getRequestLog`), and a one-line note that payloads live here in user space, not in the binary.
    - Group the file into commented sections: state, payloads, hooks, accessors.
    - Keep all three existing hooks (`onRequest`, `onResponse`, `onLoad`) and both accessors (`getFindings`, `getRequestLog`) returning `JSON.stringify(...)`. Keep the `typeof api !== "undefined"` guard and the try/catch around `api.GetSnapshot()`.
    - Tighten comments so each hook states the CDP event and the payload fields it reads. Optionally add an `onDOMNodeInserted` hook ONLY if it strengthens the example without bloating it (keep it minimal, push the node name into findings or a dedicated array with its own accessor); leaving it out is acceptable per CONTEXT.
    - Do NOT introduce console, fs, require, or import. Do NOT rename the accessors. Do NOT remove payloads.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && go build -o /tmp/rod-cli-22 . && grep -q 'function getFindings' plugins/examples/xss_scanner.js && grep -q 'function getRequestLog' plugins/examples/xss_scanner.js && grep -q 'typeof api' plugins/examples/xss_scanner.js</automated>
  </verify>
  <acceptance_criteria>
    - `plugins/examples/xss_scanner.js` still defines `getFindings` and `getRequestLog` (grep both succeed).
    - The `typeof api` guard remains present.
    - No `console.`, `require(`, or leading `import ` present.
    - Script loads without a goja execute error; `go build -o /tmp/rod-cli-22 .` exits 0.
  </acceptance_criteria>
  <done>xss_scanner.js is restructured and well-commented, loads clean, and retains both JSON accessors and the api guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → example script | The operator chooses to load the XSS scanner example; it runs in the goja sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | Tampering | XSS payloads embedded in the example | accept | Payloads live in a user-space example script (`plugins/examples/xss_scanner.js`), never compiled into the core binary — explicitly in scope per REQUIREMENTS Out of Scope (exploit logic stays in example scripts). The script only reads page state already surfaced via the sandboxed `api` global. |
| T-22-07 | Information Disclosure | getFindings/getRequestLog return collected data | accept | Returns only what the operator's own session collected, read back by the same operator via `plugin run`. No new exposure. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (single JS file edit). N/A. |
</threat_model>

<verification>
- xss_scanner.js retains `getFindings` and `getRequestLog` accessors and the `typeof api` guard.
- No sandbox-forbidden globals (`console.`, `require(`, leading `import `).
- Script loads without a goja execute error; `go build` exits 0.
</verification>

<success_criteria>
- PEX-01: the flagship XSS scanner is a polished, clearly-structured worked example that loads cleanly and exposes JSON accessors readable via `plugin run getFindings` / `plugin run getRequestLog`.
</success_criteria>

<output>
Create `.planning/phases/22-example-plugins/22-03-SUMMARY.md` when done.
</output>