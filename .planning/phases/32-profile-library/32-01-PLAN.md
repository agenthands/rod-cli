---
phase: 32-profile-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - profiles/
  - types/profiles_embed.go
  - types/config.go
  - cmd.go
  - actions/actions.go
autonomous: true
requirements: [PROF-01, PROF-03, PROF-04]
must_haves:
  truths:
    - "A `profiles/` directory contains 6 coherent Chrome-only desktop profile JSON files matching the godoll stealth.Profile schema: windows-11-chrome, windows-11-desktop-1440p, windows-10-chrome, windows-10-laptop, macos-applesilicon-chrome, macos-intel-chrome (count within the PROF-01 5-10 range; families per CONTEXT D-01)."
    - "The profiles are embedded in the binary via `//go:embed` (a Go file embeds the profiles/ dir) — they ship with the binary, no external files needed."
    - "Bare `--profile=<name>` resolves a BUILT-IN profile first (by name, no path/extension), then falls back to the existing ~/.rod-cli/profiles/<name>.json / ./profiles/<name>.json behavior. A value containing a path separator or .json suffix, or that exists on disk, is still used verbatim (custom profiles — PROF-04)."
    - "`--profile=list` prints the built-in profile names (human-readable; honors --raw/--json for a token-efficient machine form) and does NOT attempt to load a profile named 'list'."
    - "Precedence is unchanged (PROF-04): CLI flags override profile fields override defaults; built-in profiles overlay at the same Tier 2 as custom profile files in ResolveStealth. A missing/malformed built-in is still a LOUD failure."
    - "Each shipped profile is internally coherent enough to pass `deriveAndValidateFingerprint` (the hard per-profile gate is asserted in 32-02, but the files authored here must already pass it — UA major ↔ Client-Hints ↔ platform, timezone ↔ locale, plausible screen/hardware)."
    - "`go build ./...` and `go vet ./...` pass; existing --profile (custom path) behavior still works."
  artifacts:
    - path: "profiles/windows-11-chrome.json"
      provides: "a coherent Chrome-on-Windows-11 built-in profile (representative of the set)"
      contains: "userAgent"
    - path: "types/profiles_embed.go"
      provides: "//go:embed of the profiles/ dir + built-in lookup/list API"
      contains: "go:embed"
    - path: "types/config.go"
      provides: "resolveProfilePath extended: built-in name resolved before user-dir fallback"
      contains: "list"
    - path: "cmd.go"
      provides: "--profile=list handling + --profile flag Usage updated for built-in names"
      contains: "profile"
  key_links:
    - from: "cmd.go --profile flag"
      to: "types built-in profile lookup (profiles_embed.go)"
      via: "bare name resolves an embedded built-in before the user-dir path"
      pattern: "profile"
---

<objective>
Ship the built-in Chrome-only profile library (PROF-01) with by-name selection and
`--profile=list` discovery (PROF-03), without breaking custom profiles or the v1.6
precedence chain (PROF-04). Author 6 coherent desktop profiles (Windows 11/10,
macOS Apple-Silicon/Intel + resolution/hardware variants per CONTEXT D-01),
embed them via `//go:embed`, and resolve a bare `--profile=<name>` to a built-in
first then the existing user-dir path.

Purpose: out-of-the-box vetted identities so an agent picks a coherent profile by
name instead of hand-writing JSON. The vetting GATE (each profile passes the
consistency validator + harness) lands in 32-02; this plan authors the set + the
selection mechanism, with files that already pass the validator.
</objective>

<context>
@.planning/phases/32-profile-library/32-CONTEXT.md
@types/config.go
@../godoll/stealth/profile.go
@cmd.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 6 coherent Chrome-only profile JSON files</name>
  <files>profiles/windows-11-chrome.json,profiles/windows-11-desktop-1440p.json,profiles/windows-10-chrome.json,profiles/windows-10-laptop.json,profiles/macos-applesilicon-chrome.json,profiles/macos-intel-chrome.json</files>
  <read_first>
    - ../godoll/stealth/profile.go (the exact JSON schema: userAgent, platform, acceptLanguage, languages, timezone, locale, screen{width,height,deviceScaleFactor}, hardwareConcurrency, deviceMemory, vendor, spoofClientHints, spoofCanvas, spoofAudioContext).
    - types/config.go deriveAndValidateFingerprint (535) — the coherence rules each file must satisfy (UA major drives CH; OS token drives platform; timezone↔locale).
    - .planning/phases/32-profile-library/32-CONTEXT.md D-01/D-01a (the set + the MacIntel-for-both-Macs nuance).
  </read_first>
  <action>
  Create the 6 JSON files (godoll stealth.Profile shape). Use a CURRENT Chrome major
  in every userAgent (keep all six on the SAME major for consistency) and set
  spoofClientHints:true, spoofCanvas:true, spoofAudioContext:true (match v1.6 default
  hardening). Per-file concrete values:
  - windows-11-chrome: UA Windows NT 10.0 Win64 x64 Chrome/<major>, platform "Win32", screen 1920×1080 @1.0, en-US, America/New_York, vendor "Google Inc.", HW 8, mem 8, languages ["en-US","en"], acceptLanguage "en-US,en;q=0.9".
  - windows-11-desktop-1440p: same Win11 UA/platform, screen 2560×1440 @1.0, HW 16, mem 16.
  - windows-10-chrome: Win10 UA (also "Windows NT 10.0"), platform "Win32", 1920×1080 @1.0, HW 8, mem 8.
  - windows-10-laptop: Win10, 1366×768 @1.0, HW 4, mem 8.
  - macos-applesilicon-chrome: UA "Macintosh; Intel Mac OS X 10_15_7" Chrome/<major>, platform "MacIntel", screen 2560×1600 @2.0, America/Los_Angeles, HW 8, mem 16.
  - macos-intel-chrome: same macOS UA token, platform "MacIntel", 1920×1080 @1.0, HW 8, mem 16, America/Los_Angeles.
  Ensure each is internally coherent so deriveAndValidateFingerprint accepts it (32-02 will assert this for all).
  </action>
  <verify>ls profiles/*.json | wc -l; for f in profiles/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f'))" || echo "BAD JSON: $f"; done</verify>
</task>

<task type="auto">
  <name>Task 2: Embed the profiles dir + built-in lookup/list API</name>
  <files>types/profiles_embed.go</files>
  <read_first>
    - types/config.go (package name, how stealth.Profile JSON is currently loaded from a file path, error discipline).
    - ../godoll/stealth/profile.go (the struct to unmarshal into).
  </read_first>
  <action>
  Create types/profiles_embed.go with `//go:embed profiles/*.json` (embed.FS) — note
  the embed path must be reachable from the package; if profiles/ is at repo root and
  the embedding file is in types/, either move the embedding file to package main/root
  or embed via a root-level file (engineer's discretion — keep ONE embed site). Expose:
  (a) `BuiltinProfileNames() []string` (sorted) for --profile=list; (b)
  `LoadBuiltinProfile(name string) (*stealth.Profile, bool, error)` returning ok=false
  when the name isn't a built-in (so callers fall back to the user-dir path); malformed
  embedded JSON is a hard error (it ships in the binary, must never be malformed).
  </action>
  <verify>go build ./... && grep -n "go:embed" types/profiles_embed.go</verify>
</task>

<task type="auto">
  <name>Task 3: Resolve built-in first; wire --profile=list</name>
  <files>types/config.go,cmd.go,actions/actions.go</files>
  <read_first>
    - types/config.go resolveProfilePath (264-286) and ResolveStealth (288+, Tier-2 profile overlay).
    - cmd.go --profile flag (152) + forwarding (50); the subcommand pattern (e.g. how `console`/`requests` dispatch) for a list command.
    - actions/actions.go (an existing simple action returning a string, to model a list output).
  </read_first>
  <action>
  In the profile resolution path: when --profile is a bare name (no separator/.json,
  not an existing file), check BuiltinProfileNames/LoadBuiltinProfile FIRST; if it's a
  built-in, use the embedded profile (overlay at Tier 2 exactly like a profile file);
  otherwise keep the existing ~/.rod-cli/profiles/<name>.json fallback. Handle
  `--profile=list` as a special value that prints BuiltinProfileNames() and exits
  (honor --raw/--json for machine output) WITHOUT launching a daemon/loading a profile.
  Update the --profile flag Usage to mention `list` and that bare names select built-ins.
  Keep ResolveStealth's loud-failure discipline. Do NOT change CLI>profile>default order.
  </action>
  <verify>go build ./... && ./rod-cli --profile=list 2>&1 | head; grep -n "list" cmd.go</verify>
</task>

</tasks>

<must_have_truths>
See frontmatter. Load-bearing: built-in resolves before user-dir, custom paths +
precedence unchanged (PROF-04), `--profile=list` works and 'list' is reserved, and
the 6 authored profiles are coherent (the hard all-profiles gate is 32-02). No
spoofed TLS — these are real-Chrome identities only.
</must_have_truths>
