---
phase: 26-configurable-fingerprint-consistency-validator
plan: 03
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - types/context.go
autonomous: true
requirements: [FINGERPRINT-01, FINGERPRINT-03]
must_haves:
  truths:
    - "The resolved cfg.Stealth identity (pinned profile / flags) becomes the active stealth.Profile that feeds JS injection and the network interceptor — the single source of truth, not a freshly-randomized fingerprint."
    - "The rod-cli interceptor's Sec-Ch-Ua header version is derived from the active profile UA's Chrome major (no hardcoded 121)."
    - "When no fingerprint is pinned, behavior matches the prior default (no regression)."
  artifacts:
    - path: "types/context.go"
      provides: "config-pinned stealth.Profile wired into createPage's evasion + interceptor; UA-derived Sec-Ch-Ua at the former :509 literal"
      contains: "cfg.Stealth"
  key_links:
    - from: "types/context.go createPage"
      to: "godoll stealth.EvasionManager"
      via: "the pinned profile from cfg.Stealth is set on the EvasionManager so injected JS reflects the pinned identity"
      pattern: "SetProfile|em\\.profile|stealth\\.Profile"
    - from: "types/context.go updateInterceptorRules"
      to: "active profile UA"
      via: "Sec-Ch-Ua built from Chrome major parsed off prof.UserAgent"
      pattern: "Chrome/"
---

<objective>
Wire the resolved single source of truth into the live browser: make `createPage` apply the config-pinned `stealth.Profile` (from `cfg.Stealth`, resolved by Plan 01) as the active profile that drives godoll's evasion JS injection and rod-cli's network interceptor — instead of silently generating a random fingerprint and ignoring the config. Then replace the hardcoded `Sec-Ch-Ua` `121` literal in `updateInterceptorRules` (context.go:509) with a value derived from the active profile UA's Chrome major, converging on the same UA-derived Client-Hints story godoll now produces (Plan 02). This closes the FINGERPRINT-01 "pin maps onto stealth.Profile" wiring and the rod-cli half of FINGERPRINT-03.

Purpose: Today createPage at context.go:395-440 builds a random fingerprint and never reads cfg.Stealth, so the user's pinned UA/platform never reach the page — the pin is wired-but-silent. This plan makes the pin actually take effect on the live page and removes rod-cli's duplicate hardcoded CH path.
Output: createPage uses the pinned profile; updateInterceptorRules emits a UA-derived Sec-Ch-Ua.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-configurable-fingerprint-consistency-validator/26-CONTEXT.md
@.planning/phases/26-configurable-fingerprint-consistency-validator/26-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/26-configurable-fingerprint-consistency-validator/26-01-SUMMARY.md
@.planning/phases/26-configurable-fingerprint-consistency-validator/26-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the resolved cfg.Stealth profile as the active stealth.Profile in createPage</name>
  <files>types/context.go</files>
  <read_first>
    - types/context.go (createPage :395-445 — em := stealth.NewEvasionManager(page), the random fg.Generate() + em.SetFingerprint(fp) at :400-407, em.Apply() :408; Context.config field :209; ctx.fingerprint :220; updateInterceptorRules prof selection :483-491)
    - ../godoll/stealth/evasion.go (Apply :43-127 — how em.profile drives scriptOverrideAttributes/timezone/userAgentData; the profile-set path. Confirm the EvasionManager exposes a way to set a *stealth.Profile, e.g. SetProfile, or that the profile field is settable; if only SetFingerprint exists, build the profile via stealth.FromFingerprint and overlay the pinned cfg.Stealth fields onto it)
    - ../godoll/stealth/profile.go (Profile fields :11-50; DefaultProfile :67)
    - types/config.go (StealthConfig identity fields + ResolveStealth — the resolved coherent identity this task consumes)
    - 26-01-SUMMARY.md, 26-02-SUMMARY.md (the exact resolved field names + any godoll helper signatures the prior plans settled on)
  </read_first>
  <action>
    In createPage, construct the active stealth.Profile from the resolved cfg.Stealth before applying evasion: build a base profile (stealth.DefaultProfile()), then overlay every non-zero cfg.Stealth identity field (UserAgent, Platform, Locale, Timezone, AcceptLanguage, Languages, Screen{W,H,DSF}, HardwareConcurrency, DeviceMemory, Vendor, SpoofClientHints) onto it. Set this profile as the EvasionManager's active profile using whatever setter godoll exposes (prefer a SetProfile-style method; if godoll only accepts a Fingerprint, set the profile via the available mechanism confirmed in 26-02-SUMMARY / godoll evasion.go). Keep the existing random fingerprint generation ONLY as the fallback/source for dimensions godoll still needs from a Fingerprint (WebGL VideoCard etc.), but the IDENTITY (UA/platform/CH/locale/timezone) must come from the pinned profile, not the random fp. Store the resolved profile on the Context (add a field, e.g. `profile *stealth.Profile`) so updateInterceptorRules (Task 2) and the future stealth-check action read the SAME active identity. Preserve the loud-failure behavior already present (em.Apply errors still warn to stderr, VALIDATE-03 carried-forward).
    Guard the no-pin path: when cfg.Stealth has no identity overrides, the overlaid profile equals DefaultProfile() so behavior is unchanged from today's default.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && go build ./... && grep -q 'cfg.Stealth' types/context.go && grep -q 'stealth.DefaultProfile' types/context.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` succeeds.
    - createPage builds the active stealth.Profile from cfg.Stealth (overlay on DefaultProfile) and sets it on the EvasionManager so injected JS reflects the pinned identity (grep `cfg.Stealth` in context.go).
    - The resolved profile is stored on the Context for reuse by the interceptor and stealth-check.
    - No-pin path: with an empty cfg.Stealth identity, the active profile equals DefaultProfile() (behavior unchanged) — express as a recorded reasoning check in the SUMMARY or a unit assertion.
    - Live-page verification deferred to Plan 05 harness: running the binary with `--user-agent "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7) ... Chrome/130.0.0.0 ..."` and reading `eval String(navigator.platform)` from the live page shows `MacIntel` (the pinned story), asserted in 26-05.
  </acceptance_criteria>
  <done>The config-pinned profile is the active stealth.Profile driving evasion; the no-pin default is preserved; the binary compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the interceptor Sec-Ch-Ua from the active profile UA (remove the 121 literal)</name>
  <files>types/context.go</files>
  <read_first>
    - types/context.go (updateInterceptorRules :460-519 — the prof selection :483-491, the chPlatform switch :501-508, the hardcoded Sec-Ch-Ua at :509, Sec-Ch-Ua-Mobile :510, Sec-Ch-Ua-Platform :511)
    - ../godoll/stealth/evasion.go (the secChUa/chromeMajorFromUA helpers added by Plan 02 — REUSE them rather than re-deriving; confirm their exported names in 26-02-SUMMARY)
    - 26-02-SUMMARY.md (the exact exported helper name(s) for UA->major and brand-string; if Plan 02 left them unexported in package stealth, call godoll's exported equivalent or re-derive with the SAME regexp `Chrome/(\d+)` and the SAME brand format)
  </read_first>
  <action>
    In updateInterceptorRules, change the prof selection (:483-491) to prefer the Context's pinned profile (set in Task 1) over the random-fingerprint-derived one, falling back to DefaultProfile only when none is set. Replace the hardcoded `headers["Sec-Ch-Ua"] = "...121..."` at :509 with a value built from the active profile UA's Chrome major: prefer calling godoll's exported brand-string builder (the secChUa/chromeMajorFromUA pair from Plan 02) so rod-cli and godoll share ONE injector shape; if those are unexported in godoll, replicate exactly — parse `Chrome/(\d+)` off prof.UserAgent via a package-level compiled regexp and format `"Not A(Brand";v="99", "Google Chrome";v="<major>", "Chromium";v="<major>"`. Keep the chPlatform switch (:501-508) and the Sec-Ch-Ua-Mobile/Platform lines as-is — only the version string changes. The Sec-Ch-Ua major must now equal the UA Chrome major (and the godoll JS userAgentData major from Plan 02) — that triple-agreement is the FINGERPRINT-02 invariant Plan 05 asserts.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && go build ./... && grep -vE '^\s*//' types/context.go | grep -c '"Google Chrome";v="121"' | grep -qx 0 && grep -q 'Chrome/' types/context.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` succeeds.
    - No `"Google Chrome";v="121"` literal remains in types/context.go non-comment code (negative grep returns 0).
    - The Sec-Ch-Ua header value is built from the Chrome major parsed off the active profile UA (via godoll's helper or an exact local replica using `Chrome/(\d+)`).
    - updateInterceptorRules prefers the pinned Context profile over the random-fp-derived profile.
    - Live-page verification deferred to Plan 05: with `--user-agent ...Chrome/130...`, the echoed `Sec-Ch-Ua` header read off a loopback server shows major `130` == UA major.
  </acceptance_criteria>
  <done>The rod-cli interceptor emits a UA-derived Sec-Ch-Ua with no `121` literal, using the pinned profile; the binary compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cfg.Stealth.UserAgent -> injected JS / HTTP headers | The pinned (user-controlled) UA/platform/locale strings reach the page via godoll JS injection and the rod-cli interceptor's ModifiedHeaders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Tampering | pinned UA/platform -> interceptor ModifiedHeaders | mitigate | The Sec-Ch-Ua value is built only from the digits-only Chrome major parsed off the UA (regexp `\d+`), so no attacker-controlled substring reaches the header. The User-Agent/Accept-Language pass-through already existed (no new sink); the consistency validator (Plan 01) has already rejected incoherent identities before this code runs. |
| T-26-08 | Tampering | pinned identity -> godoll JS injection | mitigate | godoll builds its JS via fmt.Sprintf with the constrained derived major (Plan 02 T-26-05); rod-cli passes the validated profile, not raw user input, into that path. |
| T-26-09 | Spoofing | mismatched-lie regression | mitigate | By making the pinned profile the SINGLE source feeding both JS and headers, the triple-surface agreement is structural; Plan 05's blocking consistency-invariant test gates any regression. |
| T-26-SC | Tampering | go module deps | mitigate | No new packages; reuses godoll + stdlib regexp. Any new dep needs a blocking checkpoint + audit. |
</threat_model>

<verification>
- `go build ./...` compiles.
- Negative grep: no `"Google Chrome";v="121"` in types/context.go non-comment lines.
- The active profile is sourced from cfg.Stealth (pin takes effect) and reused by the interceptor.
- Full live-page proof (navigator.platform/UA/Sec-Ch-Ua all one story) is asserted by the Plan 05 harness, which depends on this plan.
</verification>

<success_criteria>
- The config-pinned identity becomes the active stealth.Profile feeding JS + headers (FINGERPRINT-01 wiring complete).
- rod-cli's interceptor Sec-Ch-Ua is UA-derived with no `121` literal (rod-cli half of FINGERPRINT-03).
- No-pin default behavior is preserved.
</success_criteria>

<output>
Create `.planning/phases/26-configurable-fingerprint-consistency-validator/26-03-SUMMARY.md` when done
</output>
