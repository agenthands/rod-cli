---
context: phase
phase: 26-configurable-fingerprint-consistency-validator
task: 11
total_tasks: 16
status: in_progress
last_updated: 2026-06-24T10:22:40.487Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was hit through actual failure this session. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **Stale prebuilt binary** — the `tests/` suite execs a PREBUILT `../rod-cli` binary (`exec.Command(filepath.Abs("../rod-cli"))`), NOT freshly-compiled code. Mitigation: ALWAYS run `go build -o rod-cli .` before any `runCli`-based test, or you get false failures.
- [ ] CONSTRAINT: **`pkill -f` self-match** — `pkill -f "<pattern>"` matches the running shell when the pattern string appears in your own command line, killing the shell (exit 1, no output). Mitigation: use `pkill -9 -x rod-cli` (exact comm match), never `pkill -f "rod-cli ..."`.
- [ ] CONSTRAINT: **Unverified code-review fixes** — the gsd-code-fixer applied CR-01/CR-02/WR-01/WR-03 to the working tree but was interrupted before verifying or committing. The fixes BUILD but were NOT harness-verified. Mitigation: re-run the live harness (below) before committing or trusting them.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale-binary test | Running `go test ./tests/...` without rebuilding `./rod-cli` first → tests exercise old code, false pass/fail | blocking | `go build -o rod-cli .` immediately before any runCli test |
| pkill self-match | `pkill -f "rod-cli --session"` kills the orchestrator shell (pattern is in its own argv) | blocking | Use `pkill -9 -x rod-cli` (exact process name) |
| Committing godoll into rod-cli | `git add ../godoll/...` from rod-cli silently no-ops (separate repo); changes must stay in godoll's working tree | blocking | Never stage `../godoll/*` into rod-cli; godoll changes are uncommitted-by-design (consumed via `replace`) |
| Killing the user's browser | Blanket chromium kill would terminate the user's own interactive Chromium (PID ~15166, /usr/lib/chromium) | advisory | Only kill `-x rod-cli` / `-x headless_shell` / `-x chrome` test browsers |
| Full `go test ./...` | Exceeds Go's 10-min default timeout under browser load; includes pre-existing flaky TestStartServerWithPpid | advisory | Run targeted: `-run TestDetectionHarness`, `-run TestNetworkEvasionHeaders`, `./types ./actions`, godoll `./stealth` |

<current_state>
Autonomous run `/gsd-autonomous --from 26`. **Phase 26 is fully executed, verified PASSED (5/5 must-haves), and marked complete** (STATE advanced to phase 27). A code review (26-REVIEW.md, status issues_found: 2 critical, 5 warning, 4 info) ran, and the gsd-code-fixer APPLIED fixes for the 2 criticals + 2 warnings — but hit the session limit before verifying, committing, or writing REVIEW-FIX.md.

The fix changes are in the working tree (both rod-cli and ../godoll build cleanly) but are UNCOMMITTED and UNVERIFIED:
- `actions/stealth_check.go` — CR-01: webdriver verdict now accepts `"undefined"` (godoll masks it) AND `"false"` as PASS; only `"true"` is the tell.
- `tests/detection_test.go` — CR-01: strengthened stealth_check subtest to assert webdriver actually PASSES (not just output shape).
- `types/config.go` — CR-02 defense-in-depth + WR-03: validator rejects control chars/quotes in fingerprint strings; platform fail-open tightened.
- `types/context.go` — WR-01: corrected the stale profileFromStealth comment.
- `../godoll/stealth/js.go` — CR-02 primary: `json.Marshal` escaping of UA/platform/vendor/timezone/languages at the JS injection boundary (uncommitted by design).
</current_state>

<completed_work>
- Plans 26-01..26-05 executed, each committed (see HANDOFF.json for hashes).
- Regression fix 0d6a99d: Client-Hints coherent-by-default (profileFromStealth SpoofClientHints=true) — fixed Plan-03's silent default-CH regression; TestNetworkEvasionHeaders + TestDetectionHarness green.
- Phase 26 verified PASSED (573a93e), PROJECT.md evolved (3d7ed96).
- Code review committed (bf9dd8e).
- Code-fixer applied CR-01/CR-02/WR-01/WR-03 (working tree, uncommitted, build-passing, UNVERIFIED).
</completed_work>

<remaining_work>
1. **Verify + finalize code-review fixes** (next action). Then commit ONLY the 4 rod-cli fix files; leave godoll uncommitted.
2. **Phase 27** Canvas/WebGL/WebRTC Hardening (HARDEN-01 wire EvadeWebRTC/WithWebRTCLeakProtection; HARDEN-02 replace ApplyCanvasNoise no-op stub, stable-per-session noise).
3. **Phase 28** Human-Behavior Tuning (HUMANIZE-01).
4. **Phase 29** Best-Effort Live Validation (LIVEWAF-01).
5. **Milestone v1.6 lifecycle** (audit → complete → cleanup) — ONLY after 27-29 done. Note: `/gsd-audit-milestone` is premature now (3 of 6 phases remain).
</remaining_work>

<decisions_made>
- godoll changes stay UNCOMMITTED in ../godoll's working tree (consumed via `replace => ../godoll`; phases 24/25 pattern; avoids surprising cross-repo commits).
- Client-Hints ON by default (coherence; restores pre-v1.6 behavior).
- Skipped UI-SPEC for phase 26 (ui-plan-gate false-positives on browser-injection JS; no visual UI).
</decisions_made>

<blockers>
- Code-review fixes unverified — re-run the live harness before committing. No hard blockers otherwise.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full machine-readable state (commit hashes, uncommitted files, decisions).
2. `.planning/phases/26-configurable-fingerprint-consistency-validator/26-REVIEW.md` — the 11 findings; confirm CR-01/CR-02/WR-01/WR-03 are addressed and decide on the 4 Info items.
3. `.planning/phases/26-configurable-fingerprint-consistency-validator/deferred-items.md` — the CH-default coherence gap (resolved in-phase).

## Infrastructure State
- Branch: `main` (branching_strategy=none; commits go to main). Worktrees auto-degraded (#683: HEAD diverged from origin/HEAD) → sequential execution.
- godoll sibling repo at `../godoll`, branch `main`, has intentionally-uncommitted working-tree changes (phase 26 + carryover phase 25 proxy work).
- No daemons should be running after the pause; rebuild `./rod-cli` on resume.

<context>
The code-fixer's work looks substantively complete (CR-01 verdict fix verified by grep, CR-02 json.Marshal escaping verified by grep, config.go +78 lines for validator hardening). It just needs the verification loop + commit it never reached. Resume by FINISHING phase 26's fix finalization, then continue the autonomous milestone from phase 27.
</context>

<next_action>
Start with: `pkill -9 -x rod-cli` then `go build -o rod-cli .`, then `go test ./tests/ -run TestDetectionHarness -count=1` and confirm `./rod-cli stealth-check <url>` reports `webdriver=ok` on a default page (CR-01). Also `go test ./types/ ./actions/` and `(cd ../godoll && go test ./stealth/)`. If all green → write 26-REVIEW-FIX.md, commit the 4 rod-cli fix files (NOT godoll, NOT rod-cli.yaml/test_upload.txt/daemon_more_test.go), then resume the autonomous run from phase 27.
</next_action>
