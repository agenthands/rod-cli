---
phase: 26-configurable-fingerprint-consistency-validator
plan: 05
type: execute
wave: 3
depends_on: ["26-03", "26-04"]
files_modified:
  - tests/detection_test.go
autonomous: true
requirements: [FINGERPRINT-01, FINGERPRINT-02, FINGERPRINT-03, VALIDATE-01, VALIDATE-02]
must_haves:
  truths:
    - "The Client-Hints KNOWN-RED baseline at detection_test.go:202-230 is flipped to a required-GREEN assertion: the live Sec-Ch-Ua major equals the UA's Chrome major equals navigator.userAgentData brand version."
    - "A BLOCKING consistency-invariant subtest reads Sec-CH-UA, navigator.userAgentData, UA, navigator.platform, and WebGL from the LIVE page and asserts one consistent OS+version story."
    - "A pinned-identity subtest drives the binary with --user-agent (a non-default Chrome major plus OS) and confirms the live page reflects the pinned UA major, platform, and CH major (the pin took effect)."
    - "A stealth-check subtest drives the stealth-check command (plain and with --raw) and asserts the raw form is a single PASS/FAIL line with only failing signals (not a full-page dump)."
    - "All assertions read from the live page via eval / header-echo, never from a Go config field; zero network egress."
  artifacts:
    - path: "tests/detection_test.go"
      provides: "flipped CH assertion plus consistency-invariant plus pinned-identity plus stealth-check subtests"
      contains: "userAgentData"
  key_links:
    - from: "tests/detection_test.go"
      to: "the live daemon-reused browser"
      via: "evalDetect / header-echo httptest reads of Sec-Ch-Ua, userAgentData, platform, UA"
      pattern: "evalDetect"
---

<objective>
Make the harness gate the phase (validate-live-not-source): flip the Phase-26 Client-Hints KNOWN-RED baseline to a required-GREEN assertion, add a BLOCKING consistency-invariant subtest proving Sec-CH-UA, navigator.userAgentData, UA, navigator.platform and WebGL tell one OS+version story, add a pinned-identity subtest proving a --user-agent pin actually reaches the live page, and add a stealth-check subtest proving the --raw form is a single failing-signals-only line. Every assertion reads back from the live (daemon-reused) browser, never from a Go field, with zero network egress.

Purpose: This is the phase gate. Success criterion 1 (the consistency invariant) is explicitly a blocking harness test. This plan converts the work of Plans 01-04 into enforced, regression-netted truth and removes the FINGERPRINT-03 KNOWN-RED marker.
Output: Extended tests/detection_test.go with the flipped CH assertion plus three new subtests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-configurable-fingerprint-consistency-validator/26-CONTEXT.md
@.planning/phases/26-configurable-fingerprint-consistency-validator/26-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/26-configurable-fingerprint-consistency-validator/26-03-SUMMARY.md
@.planning/phases/26-configurable-fingerprint-consistency-validator/26-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the CH KNOWN-RED to required-green and add the blocking consistency-invariant subtest</name>
  <files>tests/detection_test.go</files>
  <read_first>
    - tests/detection_test.go (the file header convention :1-36 with the KNOWN-RED markers and the no-skip rule; evalDetect :46-57 and waitForDetectReady :62-74 helpers; webgl_not_software subtest :117-130; the CH KNOWN-RED block :202-230 with the header-echo httptest pattern :210-219 that reads the live Sec-Ch-Ua via goto plus document.body.innerText)
    - tests/cli_test.go (runCli :14-28 which prepends --no-banner, runs ../rod-cli, and requires global flags to precede the subcommand)
    - internal/detect/detect.js (the userAgent and webglVendor signals available via window.__detect for the invariant reads)
    - .planning/phases/26-configurable-fingerprint-consistency-validator/26-03-SUMMARY.md (confirms the rod-cli interceptor Sec-Ch-Ua is now UA-derived) and 26-02-SUMMARY.md (godoll userAgentData major derived)
  </read_first>
  <action>
    In tests/detection_test.go REPLACE the client_hints_known_red block (:207-230) with a required-GREEN subtest named client_hints_ua_derived. Using the existing header-echo httptest pattern: goto the echo server, read the live Sec-Ch-Ua header value from the echoed JSON body, parse its Google Chrome brand version, read the live UA via evalDetect(t, "userAgent") and parse its Chrome major with a Chrome digit regexp, and read the navigator.userAgentData Google-Chrome brand version via an eval. ASSERT all three majors are equal and non-empty using t.Errorf (not t.Logf) on mismatch. Remove the Phase-26 FINGERPRINT-03 KNOWN-RED comment marker and the t.Logf baseline so the assertion is now required-green. Do NOT add a t.Skip.
    ADD a new BLOCKING subtest named consistency_invariant (success criterion 1): from the LIVE page read the Sec-CH-UA major (header-echo), the navigator.userAgentData major and platform (eval), the navigator.userAgent OS token and Chrome major (evalDetect), navigator.platform (eval), and webglVendor/webglRenderer (evalDetect). Assert one consistent OS+version story: the three CH/UA/userAgentData majors are equal; the UA OS token, navigator.platform, and Sec-Ch-Ua-Platform agree (Windows UA implies platform Win32 implies CH platform Windows); and WebGL is not a software renderer (reuse the swiftshader/llvmpipe/software check). Fail with a message naming the disagreeing surfaces.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && go build . && grep -vE '^[[:space:]]*//' tests/detection_test.go | grep -c 'KNOWN-RED (Phase 26' | grep -qx 0 && grep -q 'consistency_invariant' tests/detection_test.go && grep -vE '^[[:space:]]*//' tests/detection_test.go | grep -c 't.Skip' | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - go build . compiles the test package (test file is syntactically valid).
    - The Phase-26 FINGERPRINT-03 KNOWN-RED marker is gone (negative grep on non-comment lines returns 0) and no t.Skip exists anywhere in the file.
    - client_hints_ua_derived asserts (t.Errorf) that the live Sec-Ch-Ua major equals the UA Chrome major equals the userAgentData brand version.
    - consistency_invariant exists as a blocking subtest reading Sec-CH-UA, userAgentData, UA, platform, and WebGL from the live page and asserting one OS+version story.
    - Behavior assertion: running the harness (go test ./tests/ -run TestDetectionHarness) with the Plan 03/04 binary built makes both subtests PASS for the default identity (UA Chrome/121 -> CH major 121 -> userAgentData 121 -> platform Win32).
  </acceptance_criteria>
  <done>The CH baseline is required-green and a blocking consistency-invariant subtest passes against the live page; no skips remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add the pinned-identity subtest (the --user-agent pin reaches the live page)</name>
  <files>tests/detection_test.go</files>
  <read_first>
    - tests/detection_test.go (evalDetect :46-57, waitForDetectReady :62-74, the header-echo pattern :210-219, the headful_optin row :236-248 showing the runCli("close") then global-flag-before-subcommand sequence)
    - tests/cli_test.go (runCli :14-28 — stealth flags apply at daemon SPAWN, so the test must runCli("close") first, then pass --user-agent as a GLOBAL flag BEFORE the goto subcommand)
    - .planning/phases/26-configurable-fingerprint-consistency-validator/26-03-SUMMARY.md (confirms cfg.Stealth.UserAgent becomes the active profile feeding JS + the interceptor)
  </read_first>
  <action>
    Add a subtest named pinned_identity_macos to TestDetectionHarness. Sequence: runCli("close") to drop any existing daemon (stealth flags only apply at spawn); pick a NON-default macOS UA with a non-default Chrome major, for example a Macintosh Intel Mac OS X user agent string ending Chrome/130.0.0.0 Safari/537.36; spawn the session pinned by passing the UA as a GLOBAL flag before the subcommand: runCli("--user-agent", <macUA>, "goto", ds.URL()); waitForDetectReady(t). Then read from the LIVE page: evalDetect(t, "userAgent") must contain Chrome/130 and must NOT contain HeadlessChrome; eval of navigator.platform must equal MacIntel; eval of the navigator.userAgentData Google-Chrome brand version must equal 130; and via the header-echo server the live Sec-Ch-Ua major must equal 130 and the Sec-Ch-Ua-Platform must be the macOS value. Assert each with t.Errorf naming the surface on mismatch. Finish by runCli("close") so the pinned session does not leak into later subtests, and document in the test comment that this proves FINGERPRINT-01 (pin took effect end-to-end) plus FINGERPRINT-03 (UA-derived CH) on a non-default identity. Keep zero network egress (only ds.URL() and the loopback header-echo httptest).
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && go build . && grep -q 'pinned_identity' tests/detection_test.go && grep -q 'Chrome/130' tests/detection_test.go && grep -q 'MacIntel' tests/detection_test.go</automated>
  </verify>
  <acceptance_criteria>
    - go build . compiles the test package.
    - pinned_identity_macos closes first, spawns with --user-agent before the subcommand, and reads back UA/platform/userAgentData/Sec-Ch-Ua from the LIVE page.
    - The subtest asserts the pinned non-default major (130) appears across UA, userAgentData, and Sec-Ch-Ua, and that navigator.platform is MacIntel — proving the pin reached the page and all surfaces agree.
    - The subtest closes the pinned session at the end so it does not bleed into other subtests.
    - Behavior assertion: go test ./tests/ -run TestDetectionHarness/pinned_identity passes with the Plan 03 binary.
  </acceptance_criteria>
  <done>The harness proves a --user-agent pin produces a coherent non-default identity on the live page; no daemon leak.</done>
</task>

<task type="auto">
  <name>Task 3: Add the stealth-check subtest (per-signal verdict + --raw single-line shape)</name>
  <files>tests/detection_test.go</files>
  <read_first>
    - tests/detection_test.go (evalDetect/waitForDetectReady; the existing software-renderer check at :117-130 for the expected WebGL verdict semantics)
    - tests/cli_test.go (runCli :14-28 — global flags before subcommand; --raw is a global flag)
    - actions/stealth_check.go (the output contract from Plan 04: human table default, single-line PASS/FAIL for --raw with only failing signals, structured json for --json)
    - .planning/phases/26-configurable-fingerprint-consistency-validator/26-04-SUMMARY.md (the exact raw line format and signal token spelling)
  </read_first>
  <action>
    Add a subtest named stealth_check to TestDetectionHarness. Sequence: goto the detect fixture (runCli("goto", ds.URL())) and waitForDetectReady; run out := runCli("stealth-check") (default human mode) and assert the output names multiple per-signal verdicts (contains at least the webdriver and webgl signal labels). Then run raw := runCli("--raw", "stealth-check") and assert the raw form is a SINGLE non-empty line (no embedded newline once trimmed) that starts with PASS or FAIL; if it starts with FAIL it contains only failing-signal tokens of the form name=FAIL(reason) and does NOT contain the full set of passing signals (assert it does NOT contain a passing-signal value dump — e.g. it must not contain the full UA string). Then run js := runCli("--json", "stealth-check") and assert it parses as JSON (json.Unmarshal into a map succeeds). Use t.Errorf with the captured output on any mismatch. This proves VALIDATE-01 (per-signal verdict from the live page) and VALIDATE-02 (token-efficient single-line --raw, no dump). Zero network egress (only the offline fixture).
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && go build . && grep -q 'stealth_check' tests/detection_test.go && grep -q 'stealth-check' tests/detection_test.go</automated>
  </verify>
  <acceptance_criteria>
    - go build . compiles the test package.
    - stealth_check drives runCli("stealth-check"), runCli("--raw","stealth-check"), and runCli("--json","stealth-check") against the offline fixture.
    - The subtest asserts the --raw output is a single trimmed line starting with PASS or FAIL with only failing-signal tokens and NO full-page/full-signal dump.
    - The subtest asserts the --json output parses as JSON.
    - Behavior assertion: go test ./tests/ -run TestDetectionHarness/stealth_check passes with the Plan 04 binary.
  </acceptance_criteria>
  <done>The harness proves stealth-check returns per-signal verdicts from the live page and that --raw is a single failing-signals-only line; --json parses.</done>
</task>

<task type="auto">
  <name>Task 4: Run the full detection harness to confirm the phase gate is green</name>
  <files>tests/detection_test.go</files>
  <read_first>
    - tests/detection_test.go (the whole TestDetectionHarness after Tasks 1-3)
    - .github/workflows/test.yml (the push CI job from Phase 24 that runs the harness, to confirm the new subtests run on the same gate)
  </read_first>
  <action>
    Build the binary (go build -o rod-cli .) and run the full detection harness so the flipped CH assertion, the consistency invariant, the pinned-identity, and the stealth-check subtests all execute against the live binary with zero network egress. If any subtest fails because of an ordering/daemon-leak issue (a pinned session bleeding into a later subtest), add the necessary runCli("close") guards rather than weakening assertions. This is a verification/glue task: do NOT relax any assertion to make it pass; a real failure here means Plans 01-04 left a coherence gap that must be reported in the SUMMARY (do not paper over it). Confirm `go test ./...` still passes for the rest of the suite (no regression from the test additions).
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && go build -o rod-cli . && go test ./tests/ -run TestDetectionHarness -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go build -o rod-cli . succeeds.
    - go test ./tests/ -run TestDetectionHarness -count=1 passes (the consistency_invariant, client_hints_ua_derived, pinned_identity_macos, and stealth_check subtests are all green).
    - go test ./... (full module) passes with no regression introduced by the new subtests.
    - No assertion was weakened or skipped to achieve green; any genuine coherence gap is reported in the SUMMARY instead of masked.
  </acceptance_criteria>
  <done>The full detection harness is green with the new blocking subtests; the phase gate passes with zero network egress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> live binary | The harness drives the real rod-cli binary and reads stealth signals back from the live page (no Go-field shortcut) |
| pinned --user-agent in test -> live headers/JS | The test pins a non-default UA, exercising the user-controlled-input path end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-13 | Spoofing | validate-from-source instead of live page | mitigate | Every new assertion reads via evalDetect / header-echo from the live daemon-reused browser; no assertion reads a Go config field. This is the phase's core anti-regression gate. |
| T-26-14 | Tampering | network egress in CI | mitigate | The harness uses only the offline internal/detect fixture and loopback httptest servers; the pinned-UA test introduces no external host. Zero egress preserved so CI stays deterministic. |
| T-26-15 | Repudiation | a silent stealth regression slipping past CI | mitigate | Flipping the KNOWN-RED to a hard t.Errorf and adding the blocking consistency_invariant subtest means a future UA/CH/platform mismatch fails the push CI job rather than logging a benign baseline. |
| T-26-SC | Tampering | go test deps | mitigate | No new third-party test deps; only stdlib (net/http/httptest, encoding/json, regexp) plus the existing internal/detect package. Any new dep requires a blocking checkpoint plus audit. |
</threat_model>

<verification>
- go build -o rod-cli . compiles.
- go test ./tests/ -run TestDetectionHarness -count=1 passes with the four new/flipped subtests green.
- No t.Skip and no Phase-26 KNOWN-RED marker remain in detection_test.go.
- go test ./... shows no regression.
- All reads are from the live page; zero network egress.
</verification>

<success_criteria>
- The consistency invariant is a passing BLOCKING harness test (success criterion 1; FINGERPRINT-01/02).
- The CH KNOWN-RED is flipped to required-green: Sec-CH-UA major equals UA major equals userAgentData version (FINGERPRINT-03).
- A pinned --user-agent produces a coherent non-default identity on the live page (FINGERPRINT-01).
- stealth-check returns per-signal verdicts and a single-line --raw form proven by the harness (VALIDATE-01/02).
</success_criteria>

<output>
Create `.planning/phases/26-configurable-fingerprint-consistency-validator/26-05-SUMMARY.md` when done
</output>
