---
phase: 28-human-behavior-tuning
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/humanize_tuning_test.go
autonomous: true
requirements: [HUMANIZE-01]
must_haves:
  truths:
    - "A test driving the real prebuilt rod-cli binary sets a slow typing speed and asserts measurably longer per-keystroke timing than the default path (criterion 2: a configured tuning value observably changes behavior)."
    - "A test asserts the default (no humanize flag) timing is unchanged / within the existing bound, demonstrating zero wall-clock regression from the tuning surface (criterion 3)."
    - "The tests build the binary first (go build -o rod-cli .) so they exercise freshly-compiled threading, not a stale binary."
  artifacts:
    - path: "tests/humanize_tuning_test.go"
      provides: "tunability proof (slow typing speed => longer timing) + zero-regression assertion (default timing unchanged), driving the real binary"
      contains: "typing-speed"
  key_links:
    - from: "tests/humanize_tuning_test.go"
      to: "actions/actions.go"
      via: "the real binary's threaded TypeWithHumanize honors --typing-speed-* so measured timing changes"
      pattern: "typing-speed"
---

<objective>
Prove HUMANIZE-01's observable-behavior criteria against the REAL binary: (2) a
configured tuning value measurably changes interaction behavior, and (3) the tuning
surface does not regress the harness wall-clock on the default path.

Purpose: Plan 01 wires the threading; this plan is the goal-backward evidence that
the wiring is live (not silently ignored) AND safe (default path unchanged). Models
the existing tests/humanize_test.go:59 timing assertion.
</objective>

<context>
@.planning/phases/28-human-behavior-tuning/28-CONTEXT.md
@.planning/phases/28-human-behavior-tuning/28-01-PLAN.md
@tests/humanize_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tunability + zero-regression timing test driving the real binary</name>
  <files>tests/humanize_tuning_test.go</files>
  <read_first>
    - tests/humanize_test.go (the existing humanized-typing timing model, :43-68, and how it drives the binary).
    - The runCli/testserver harness pattern used by the tests package.
  </read_first>
  <action>
  Add a test that: (a) builds rod-cli (or relies on the suite's build step — but ensure freshly compiled), (b) fills an input via the real binary with an explicit SLOW --typing-speed and measures elapsed per-keystroke timing, (c) asserts it is measurably longer than the default-path timing for the same text, and (d) asserts the default-path timing stays within the existing bound (zero regression). Keep the slow value bounded so the test itself stays fast. Respect the operational traps: go build -o rod-cli . before runCli; never pkill -f (use -x); never kill the user's Chromium; do not run full `go test ./...`.
  </action>
  <verify>go build -o rod-cli . ; go test ./tests/ -run TestHumanize -count=1</verify>
</task>

</tasks>

<must_have_truths>
See frontmatter. The test is the falsifiable evidence for criteria 2 + 3 — a knob
that is "set but silently ignored" must FAIL this test.
</must_have_truths>
