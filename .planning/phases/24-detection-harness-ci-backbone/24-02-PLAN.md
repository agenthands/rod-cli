---
phase: 24-detection-harness-ci-backbone
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - types/context.go
autonomous: true
requirements: [VALIDATE-03]
tags: [validate-03, loud-failure, stealth, evasion, stderr]

must_haves:
  truths:
    - "When fingerprint generation fails, a `warning:` line is written to stderr instead of the error being silently discarded."
    - "When `EvasionManager.Apply()` returns an error, a `warning:` line is written to stderr instead of `_ = em.Apply()` swallowing it."
    - "Warnings go to stderr only (stdout stays clean for `--raw`/piped output); the daemon does NOT hard-fail on an evasion error."
  artifacts:
    - path: "types/context.go"
      provides: "createPage() evasion path that surfaces fingerprint + Apply errors to stderr"
      contains: "os.Stderr"
  key_links:
    - from: "types/context.go createPage()"
      to: "os.Stderr"
      via: "fmt.Fprintf(os.Stderr, \"warning: ...\") on fingerprint + Apply error paths"
      pattern: "Fprintf\\(os\\.Stderr"
---

<objective>
Land VALIDATE-03: make the two currently-swallowed evasion errors in `types/context.go` `createPage()` observable. Today fingerprint-generation errors take a silent no-op path (`if err == nil`) and `EvasionManager.Apply()`'s error is discarded (`_ = em.Apply()`). Replace both with `fmt.Fprintf(os.Stderr, "warning: ...")` so a silent stealth no-op becomes observable — without hard-failing the daemon.

Purpose: VALIDATE-03 turns the "wired-but-silent" failure mode (the v1.5 onDOMNodeInserted lesson) into a visible warning. Because the e2e `runCli` helper folds stderr into its captured buffer, the harness in plan 24-03 can assert the warning is observable when evasion fails.
Output: A localized edit to `types/context.go` lines ~286-292. No new imports (`fmt` and `os` are already imported). No behavior change on the success path; no daemon abort on failure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-detection-harness-ci-backbone/24-CONTEXT.md
@.planning/phases/24-detection-harness-ci-backbone/24-PATTERNS.md

# The edit site + import block (fmt and os already imported at lines 5-6):
@types/context.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Surface fingerprint + Apply errors to stderr in createPage()</name>
  <read_first>
    - types/context.go lines 1-20 (import block — confirm `fmt` and `os` are already imported; no new import is needed)
    - types/context.go lines 281-327 (the `createPage()` body; the exact swallow site is lines 286-292)
    - .planning/phases/24-detection-harness-ci-backbone/24-PATTERNS.md lines 153-184 (the VALIDATE-03 edit pattern, stderr convention, and the "no log.* precedent → fmt.Fprintf(os.Stderr) is the convention-setting choice" note)
    - .planning/phases/24-detection-harness-ci-backbone/24-CONTEXT.md lines 26, 60 (do NOT hard-fail the daemon; warnings to stderr not stdout so they don't pollute `--raw`/piped results)
  </read_first>
  <files>types/context.go</files>
  <action>
In `createPage()` at the evasion block (currently lines 286-292), change the two error-swallowing paths to write a short `warning:` line to stderr while preserving current control flow (no hard-fail, no early return, success path unchanged).

Replace the fingerprint-generation block so the `err != nil` branch is no longer a silent no-op: on `fg.Generate()` error, write `fmt.Fprintf(os.Stderr, "warning: fingerprint generation failed: %v\n", err)`; on success (`fp != nil`) keep `ctx.fingerprint = fp` and `em.SetFingerprint(fp)` exactly as today.

Replace `_ = em.Apply()` with an `if err := em.Apply(); err != nil { fmt.Fprintf(os.Stderr, "warning: evasion Apply failed: %v\n", err) }`. `EvasionManager.Apply()` returns `error` (confirmed in godoll `stealth/evasion.go`).

CAUTION — variable shadowing: the outer `err` at the top of `createPage()` comes from `ctx.browser.Page(...)` and is checked at the end of the function (the `if err != nil { return nil, errors.Wrap(err, "create page failed") }` at ~line 323). The inner fingerprint `fp, err := fg.Generate()` currently shadows it inside the `if page != nil` block. Keep the inner fingerprint error in its own scope (use `:=` for the fingerprint generate, or a distinct name) so you do NOT clobber the outer page-creation `err` that the function returns. For the `Apply()` change, use a fresh `if err := em.Apply(); err != nil` block so its `err` is scoped to the `if` and cannot leak into the outer `err`. Verify the existing end-of-function `errors.Wrap(err, "create page failed")` still refers to the page-creation error after your edit.

Keep wording short and token-efficient (MEMORY: output noise). Do NOT add a structured logger or `log.*` — the repo has no such precedent; `fmt.Fprintf(os.Stderr, "warning: ...")` is the lightest convention-consistent choice (PATTERNS.md line 183). Do NOT change `os.Stdout`/stdout behavior — warnings go to stderr only so `--raw`/piped output stays clean.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && go build ./... && grep -c 'Fprintf(os.Stderr, "warning:' types/context.go && ! grep -q '_ = em.Apply()' types/context.go && echo OK</automated>
  </verify>
  <done>`types/context.go` compiles; `_ = em.Apply()` is gone; both the fingerprint-generation and `Apply()` error paths write a `warning:` line to `os.Stderr`; the outer page-creation `err` returned at end-of-function is unaffected; the daemon does not abort on evasion error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon process → operator stderr | diagnostic warnings cross to the operator's terminal / captured test buffer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Information Disclosure | warning text on stderr | mitigate | Warning strings carry only error context (no proxy creds, no secrets — none are in scope this phase); messages are short and contain only the godoll error `%v`. |
| T-24-05 | Repudiation | swallowed evasion no-op | mitigate | This IS the mitigation: previously-silent failures now leave an observable stderr trace, so a stealth no-op cannot ship unnoticed. |
</threat_model>

<verification>
- `go build ./...` compiles (no new import needed; `fmt`/`os` already present).
- `grep '_ = em.Apply()' types/context.go` returns nothing (the swallow is removed).
- `grep -c 'Fprintf(os.Stderr, "warning:' types/context.go` == 2 (both error paths surfaced).
- Existing tests still pass: `go test ./types/...` (no behavior change on the success path).
</verification>

<success_criteria>
- Fingerprint-generation failures and `EvasionManager.Apply()` failures each emit a `warning:` line to stderr.
- Stdout is unchanged (no `--raw`/pipe pollution); the daemon continues running on evasion error.
- The change is observable through `runCli` (which captures stderr), enabling the harness assertion in plan 24-03.
</success_criteria>

<output>
Create `.planning/phases/24-detection-harness-ci-backbone/24-02-SUMMARY.md` when done.
</output>
