---
phase: 24-detection-harness-ci-backbone
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - tests/detection_test.go
autonomous: true
requirements: [HARNESS-02]
tags: [detection-harness, e2e, eval-readback, known-red, stealth]

must_haves:
  truths:
    - "An e2e test boots the `internal/detect` fixture server, drives the real `../rod-cli` binary to it via `runCli`, and asserts each table-stakes signal by reading it back from the LIVE page via the `eval` command — never from a Go config field."
    - "The test extends (does not duplicate) the signals already covered by stealth_test.go (webdriver, plugins, UA) — it adds WebGL, permissions consistency, timezone, window.chrome, languages, screen."
    - "Signals known to be red at baseline (WebRTC ICE leak, hardcoded Client-Hints 121) are asserted at their CURRENT truth with `// KNOWN-RED (Phase NN: <REQ>)` markers — kept as executing assertions, never skipped — so CI is green against the documented baseline and each flips to required-green when its later phase fixes it."
  artifacts:
    - path: "tests/detection_test.go"
      provides: "e2e detection harness driving the live binary against the offline fixture"
      contains: "detect.New"
      min_lines: 60
  key_links:
    - from: "tests/detection_test.go"
      to: "internal/detect"
      via: "import + detect.New(); ds.Start(); defer ds.Close(); runCli(\"goto\", ds.URL())"
      pattern: "detect\\.New|ds\\.URL"
    - from: "tests/detection_test.go"
      to: "../rod-cli binary"
      via: "runCli(\"eval\", \"window.__detect.<signal>\") reads live-page verdicts"
      pattern: "runCli\\(\"eval\""
---

<objective>
Land HARNESS-02: a Go e2e test (`tests/detection_test.go`) that boots the `internal/detect` fixture server (plan 24-01), navigates the real `../rod-cli` binary to it via the existing `runCli` helper, and asserts each table-stakes stealth signal by reading the verdict back from the LIVE page via the `eval` command (`window.__detect.<signal>`) — never from a Go config field. Baseline-known-red signals (WebRTC leak, Client-Hints 121) are asserted at current truth with `// KNOWN-RED` markers so CI stays green on the documented baseline and flips to required-green when Phases 26/27 fix them.

Purpose: HARNESS-02 is the deterministic green-or-red gate that proves the SHIPPED binary's stealth (not godoll in isolation), reading from the live page per the v1.5 "validate-live-not-source" lesson. It also asserts the VALIDATE-03 warning (plan 24-02) is observable when evasion fails.
Output: `tests/detection_test.go` (package `tests`), reusing the existing `runCli` driver. Headless is the blocking path; headful is a local/opt-in matrix row gated by an env var.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-detection-harness-ci-backbone/24-CONTEXT.md
@.planning/phases/24-detection-harness-ci-backbone/24-PATTERNS.md

# Dependencies this plan builds on:
@.planning/phases/24-detection-harness-ci-backbone/24-01-SUMMARY.md
@.planning/phases/24-detection-harness-ci-backbone/24-02-SUMMARY.md

# The test patterns to mirror (runCli driver + body shape) and converge with (do NOT duplicate):
@tests/cli_test.go
@tests/stealth_test.go
@tests/network_evasion_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the e2e detection harness driving the live binary against the offline fixture</name>
  <read_first>
    - tests/cli_test.go lines 14-28 (the `runCli(args...)` helper — it is in `package tests`, call it directly; it prepends `--no-banner` and folds stderr INTO stdout, so VALIDATE-03 warnings are observable through its output)
    - tests/stealth_test.go (already asserts webdriver, plugins, UA — do NOT re-assert these three; extend with the not-yet-covered signals)
    - tests/network_evasion_test.go lines 1-45 (already asserts UA/Accept-Language/Sec-Ch-Ua headers; the Client-Hints story lives here — your KNOWN-RED CH check should not duplicate its header assertions)
    - internal/detect/server.go + detect.js (from plan 24-01: `detect.New()/Start()/URL()/Close()` and the `window.__detect.<signal>` global the page populates, gated by `window.__detect.ready`)
    - .planning/phases/24-detection-harness-ci-backbone/24-PATTERNS.md lines 99-149 (test body pattern, KNOWN-RED convention, runCli-folds-stderr note)
    - .planning/phases/24-detection-harness-ci-backbone/24-CONTEXT.md lines 20, 22 (headless = blocking gate, headful = opt-in env-gated row; assert current actual truth, mark KNOWN-RED, never skip the assertion)
  </read_first>
  <files>tests/detection_test.go</files>
  <action>
Create `tests/detection_test.go` in `package tests`. It reuses the existing `runCli` helper (same package — call directly; do NOT redefine it) and the `internal/detect` server from plan 24-01. Import `github.com/agenthands/rod-cli/internal/detect`.

Structure a single `TestDetectionHarness(t *testing.T)` (or a small set of focused sub-tests via `t.Run`) following the stealth_test.go shape:
  1. `ds, err := detect.New()` (fatal on err); `ds.Start()`; `defer ds.Close()`.
  2. `runCli("close")` for deterministic daemon state (every existing test does this first).
  3. `runCli("goto", ds.URL())` — navigate the real daemon-driven browser to the fixture; fatal on err with the output included.
  4. Wait for the page's async probes to settle: poll `runCli("eval", "window.__detect && window.__detect.ready")` until it reports ready (bounded retry loop, e.g. up to ~20 tries with a short sleep) so the WebRTC/permissions async probes have populated `window.__detect`. Fail with the captured output if never ready.
  5. For each table-stakes signal, `runCli("eval", "window.__detect.<signal>")` and assert the live-page value. READ FROM THE LIVE PAGE — never from a Go Config/Profile/Fingerprint field (the v1.5 validate-live-not-source rule, PITFALLS.md Pitfall 2).

Signal coverage — EXTEND, do not duplicate stealth_test.go. Add assertions for the signals stealth_test.go does NOT cover:
  - `webglVendor` / `webglRenderer`: assert NOT SwiftShader/llvmpipe/"software" (a software renderer is the headless WebGL tell). If CI's headless Chromium reports a software renderer at baseline, mark this `// KNOWN-RED` at current truth rather than failing (decide based on the observed baseline value the executor sees — record whichever is true).
  - `permissionsConsistent`: assert the `Notification.permission` vs `navigator.permissions.query` consistency verdict is the non-tell value.
  - `timezone`: assert it is a non-empty IANA zone string (`Intl.DateTimeFormat` resolved).
  - `windowChrome`: assert `window.chrome` is present (the headless tell is its absence).
  - `languages`: assert `navigator.languages` is non-empty.
  - `screen`: assert width/height are non-zero (headless zero-size tell).
You MAY include a thin webdriver/UA re-check ONLY if it reads `window.__detect` (the harness global) rather than re-running stealth_test.go's bare `navigator.webdriver` eval — but prefer to leave webdriver/plugins/UA to stealth_test.go and converge, not duplicate (PATTERNS.md line 101).

KNOWN-RED baseline signals (CONTEXT.md line 22, PATTERNS.md lines 142-147) — assert CURRENT actual truth, keep the assertion present (do NOT skip the test for these — they must execute), and mark each with a comment in this exact shape so a grep can find them and the later phase can flip them:
  - WebRTC ICE leak (informational `window.__detect.webrtcIce`): `// KNOWN-RED (Phase 27 HARDEN-01): WebRTC ICE leaks local IP; asserting baseline truth, flips to required-green when EvadeWebRTC is wired.` Assert whatever the baseline actually reports (leaked vs not) so CI is green now.
  - Client-Hints `121` mismatch: assert the current `Sec-CH-UA` truth via the header path, marked `// KNOWN-RED (Phase 26 FINGERPRINT-03): Client-Hints version hardcoded; flips to required-green when CH is derived from UA.` Do NOT duplicate network_evasion_test.go's full header dump — assert only the CH-version baseline truth.

These KNOWN-RED signals must each keep a REAL, executing assertion of current truth — do NOT mark them as skipped (no test-skip directive), so they stay visible in CI output and a later phase can flip them to required-green. The verify gate negative-greps the test for any skip directive to enforce this.

VALIDATE-03 observability check (ties to plan 24-02): add an assertion (or a documented sub-check) that demonstrates the evasion-warning path is observable through `runCli` output — i.e. that the harness CAN see a `warning:` stderr line if evasion fails, since `runCli` folds stderr into its buffer. If forcing an evasion failure is impractical in-test, at minimum assert the normal path produces NO spurious `warning:` line (proving the success path is clean and that the channel exists), and document in a comment that the warning surfaces here when `Apply()`/fingerprint generation fails.

Headless vs headful matrix (CONTEXT.md line 20): the test runs HEADLESS by default (the blocking CI gate). Gate an optional headful run behind an env var (e.g. `if os.Getenv("ROD_HEADFUL") == "1"`) as a local/opt-in row — do NOT add an always-on headful path to the blocking test (headful in CI needs xvfb and is slow/flaky). Drive headfulness through the same `runCli` surface (e.g. the `--headless` flag the binary already supports) only on the env-gated path.

Finish each test path with `runCli("close")` to release the daemon. Keep assertions reading from `window.__detect` (live page) so the harness proves the shipped binary, not source.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && go build -o rod-cli . && go vet ./tests/ && go test ./tests/ -run TestDetectionHarness -count=1 -v 2>&1 | tail -40 && grep -c "KNOWN-RED" tests/detection_test.go && grep -vc 'Skip(' tests/detection_test.go >/dev/null && ! grep -q '\.Skip(' tests/detection_test.go && echo OK</automated>
  </verify>
  <done>`tests/detection_test.go` exists; `go test ./tests/ -run TestDetectionHarness` passes GREEN against the current binary; it boots `detect.New()`, navigates the live binary, and asserts each extended table-stakes signal by reading `window.__detect.<signal>` from the live page; WebRTC + Client-Hints baseline-red signals carry `// KNOWN-RED (Phase NN: <REQ>)` markers and stay as executing assertions (not skipped); headful is env-gated, headless is default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live rod-cli binary | the test drives the real daemon over the CLI (`exec.Command`) — the same surface a real caller uses |
| live browser → offline fixture | loopback-only fetch of static embedded detection assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Tampering | test reads from Go state instead of live page | mitigate | All assertions read `window.__detect.<signal>` via the `eval` command against the live page — never a Go Config/Fingerprint field (the validate-live-not-source rule). |
| T-24-07 | Information Disclosure | KNOWN-RED WebRTC probe records a local IP | accept | Informational only, offline test context; the leak itself is fixed in Phase 27 (HARDEN-01) — this phase only records baseline truth. |
</threat_model>

<verification>
- `go build -o rod-cli .` produces the binary `runCli` drives.
- `go test ./tests/ -run TestDetectionHarness` passes green against the current binary (baseline).
- `grep -c "window.__detect" tests/detection_test.go` > 0 (assertions read the live-page global, not Go state).
- `grep -c "KNOWN-RED" tests/detection_test.go` >= 2 (WebRTC + Client-Hints baseline markers present).
- `grep -c '\.Skip(' tests/detection_test.go` == 0 (known-red signals stay visible as executing assertions, not skipped).
</verification>

<success_criteria>
- A deterministic, offline e2e test drives the real `rod-cli` binary against the fixture and produces a green-or-red result.
- Every table-stakes assertion reads back from the live page via `eval` (`window.__detect.<signal>`), extending — not duplicating — stealth_test.go/network_evasion_test.go.
- WebRTC + Client-Hints-121 baseline-red signals are asserted at current truth with `// KNOWN-RED (Phase NN: <REQ>)` markers and are never skipped; the suite is green against the documented baseline.
- The VALIDATE-03 evasion warning is observable through the harness's captured `runCli` output.
</success_criteria>

<output>
Create `.planning/phases/24-detection-harness-ci-backbone/24-03-SUMMARY.md` when done.
</output>
