---
phase: 24-detection-harness-ci-backbone
plan: 04
type: execute
wave: 3
depends_on: ["24-03"]
files_modified:
  - .github/workflows/test.yml
  - .gitignore
  - docs/cdp-footprint.md
autonomous: true
requirements: [HARNESS-03]
tags: [ci, github-actions, cleanup, gitignore, cdp-findings]

must_haves:
  truths:
    - "A new `.github/workflows/test.yml` runs `go test ./...` on push + PR to main, with go-version matching go.mod (1.25.x) — NOT release.yml's stale 1.23 — and zero network egress in the test run."
    - "The CI job builds the `rod-cli` binary (so `runCli` can drive it) and installs Chromium before running the harness."
    - "Stray build/test artifacts are gitignored and the tracked ones (`tests/rod/`, `tests/interaction_test.go.orig`) are removed from the repo; untracked ones (`rod-cli`, `test_rod`, `state.json`, `init_output.json`, `fix_test.patch`, `tests/coverage.out`, `tests/log/`) are deleted."
    - "A short CDP-footprint findings note documents the YES/NO ceiling on hiding the CDP tell (informational; no attempt to fix it)."
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "first test CI job — go test on push + PR to main, go 1.25.x"
      contains: "go-version"
    - path: ".gitignore"
      provides: "ignore rules for stray detection/build artifacts"
    - path: "docs/cdp-footprint.md"
      provides: "CDP-tell ceiling findings note (YES/NO)"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "tests/detection_test.go"
      via: "go test ./... runs the harness on push/PR"
      pattern: "go test"
---

<objective>
Land HARNESS-03 and close out the phase: create the repo's FIRST test CI workflow (`.github/workflows/test.yml`) that runs `go test ./...` (including the detection harness from plan 24-03) on push + PR to `main`, with go-version matching `go.mod` (1.25.x — NOT release.yml's stale 1.23) and zero network egress in the test run. Then do the cleanup standing up clean CI requires: gitignore the stray artifacts, remove the tracked junk (`tests/rod/` browser profile, `tests/interaction_test.go.orig`) and delete the untracked stray files. Finally, write the short CDP-footprint findings note documenting the YES/NO ceiling (informational only).

Purpose: HARNESS-03 makes the harness a real gate that runs on every push, baselined against the current binary so KNOWN-RED leaks (WebRTC, CH-121) surface as documented baseline rather than silent passes. The cleanup removes a 1185-file accidental browser-profile commit and assorted artifacts so CI runs on a clean tree.
Output: `.github/workflows/test.yml` (new), `.gitignore` (extended), removed stray artifacts, `docs/cdp-footprint.md` (new findings note).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-detection-harness-ci-backbone/24-CONTEXT.md
@.planning/phases/24-detection-harness-ci-backbone/24-PATTERNS.md

# Dependency: the harness the CI job runs:
@.planning/phases/24-detection-harness-ci-backbone/24-03-SUMMARY.md

# The only existing workflow (mirror checkout + setup-go shape); note its go-version is stale 1.23:
@.github/workflows/release.yml
# The existing ignore rules to consolidate with (already covers *.out, *.log):
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up stray build/test artifacts and extend .gitignore</name>
  <read_first>
    - .gitignore (already ignores `*.out`, `*.log`, `*.png`, `*.json` is commented-out — do NOT blanket-ignore `*.json` since `rod-cli.yaml`/configs and `state.json` semantics differ; ignore specific paths)
    - .planning/phases/24-detection-harness-ci-backbone/24-PATTERNS.md lines 216-220 (the stray-artifact list + "several already covered by globs — consolidate, don't duplicate")
    - .planning/phases/24-detection-harness-ci-backbone/24-CONTEXT.md line 28 (the authoritative artifact list)
  </read_first>
  <files>.gitignore</files>
  <action>
First, remove the TRACKED stray artifacts from git (these were committed by accident):
  - `git rm -r --cached tests/rod` then delete the working-tree dir (`rm -rf tests/rod`) — this is a 1185-file accidental Chromium user-data-dir commit (`net.Listen` rod profile). It must leave the repo.
  - `git rm --cached tests/interaction_test.go.orig` and delete it (`rm -f tests/interaction_test.go.orig`) — a stray merge/patch backup.

Then delete the UNTRACKED stray artifacts from the working tree (they are not in git, just remove the files): root `rod-cli`, `test_rod`, `state.json`, `init_output.json`, `fix_test.patch`, plus `tests/coverage.out` and `tests/log/` (directory). Use `rm -f` / `rm -rf` as appropriate; do NOT touch `tests/rod-cli.yaml` (that is a real test config, not stray).

Then extend `.gitignore` to keep these from coming back, CONSOLIDATING with existing globs (CONTEXT.md line 33 — do not duplicate rules already present). Note what is ALREADY covered: `*.out` already ignores `tests/coverage.out`; `*.log` already ignores log files. Add the entries NOT yet covered:
  - `/rod-cli` (root build binary — anchored so it doesn't ignore the source dir name)
  - `/test_rod`
  - `/state.json`
  - `/init_output.json`
  - `/fix_test.patch`
  - `tests/rod/` (the rod browser-profile dir created by test runs)
  - `tests/log/` (if not already covered by `*.log` — `*.log` matches files, not the `log/` dir, so add `tests/log/`)
  - `*.orig` (patch/merge backups like the removed `.orig`)
Group them under a clear comment block (e.g. `# Stray test/build artifacts (Phase 24 cleanup)`). Anchor the root-level binary/file ignores with a leading `/` so they only match repo-root, not nested paths. Do NOT uncomment the blanket `*.json` rule — it would ignore real config/state fixtures.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && test ! -e tests/rod && test ! -e tests/interaction_test.go.orig && test ! -e rod-cli && test ! -e state.json && test ! -e init_output.json && test ! -e fix_test.patch && git ls-files tests/rod | head -1 | grep -q . && echo "STILL TRACKED-FAIL" || echo "OK"</automated>
  </verify>
  <done>`tests/rod/` and `tests/interaction_test.go.orig` are removed from git AND the working tree; the untracked stray files (`rod-cli`, `test_rod`, `state.json`, `init_output.json`, `fix_test.patch`, `tests/coverage.out`, `tests/log/`) are deleted; `.gitignore` has anchored/consolidated rules preventing their return without duplicating existing `*.out`/`*.log` globs; `git status` shows no stray untracked artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Create the test CI workflow (go test on push + PR to main, go 1.25.x)</name>
  <read_first>
    - .github/workflows/release.yml lines 1-32 (mirror its `actions/checkout@v4` + `actions/setup-go@v5` shape; note its `go-version: '1.23'` is STALE — use 1.25.x to match go.mod)
    - go.mod line 3 (`go 1.25.1` — the CI go-version must match so `go test` compiles; PATTERNS.md line 210 flags the reconcile)
    - .planning/phases/24-detection-harness-ci-backbone/24-PATTERNS.md lines 188-212 (workflow adaptation: triggers, build binary, Chromium install, headless-only blocking gate)
    - .planning/phases/24-detection-harness-ci-backbone/24-CONTEXT.md lines 20, 27 (push + PR to main; headless blocking, headful NOT in the blocking job)
    - .planning/research/ARCHITECTURE.md line 229 (`rod-cli install` for Chromium; `go test ./...`)
  </read_first>
  <files>.github/workflows/test.yml</files>
  <action>
Create `.github/workflows/test.yml`:
  - `name: Test`
  - Trigger on push + PR to main: `on: { push: { branches: [main] }, pull_request: { branches: [main] } }` (CONTEXT.md line 27).
  - One `test` job on `runs-on: ubuntu-latest`. Steps:
    1. `actions/checkout@v4`.
    2. `actions/setup-go@v5` with `go-version: '1.25.x'` (match `go.mod`'s `go 1.25.1` — DO NOT copy release.yml's stale `1.23`, or the harness package won't compile under the 1.25 toolchain). Leave Go module/build caching to setup-go's default cache.
    3. Build the binary so `runCli`'s `../rod-cli` exists: `go build -o rod-cli .` at repo root (the e2e tests `exec.Command` an `../rod-cli` relative to `tests/`, which resolves to the repo-root binary).
    4. Install Chromium for the headless browser the daemon launches — run the binary's own installer step (e.g. `./rod-cli install` if that subcommand exists, per ARCHITECTURE.md line 229) OR rely on go-rod's auto-download on first launch; prefer an explicit install step so the first test isn't slowed/flaky by a mid-test download. Cache the Chromium download dir if practical.
    5. Run the suite HEADLESS (the blocking gate): `go test ./... -count=1`. Headless is the default test mode (plan 24-03); do NOT add an xvfb headful matrix row to this blocking job (CONTEXT.md line 20 — headful is local/opt-in only).
  - Zero network egress in the test run: the harness uses the offline `internal/detect` fixture, so the job must not depend on any third-party detection site. Do not add any step that curls/hits an external detection endpoint.
  Keep the workflow minimal and mirror release.yml's checkout/setup-go conventions.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && test -f .github/workflows/test.yml && grep -q "1.25" .github/workflows/test.yml && grep -Eq "branches:\s*\[\s*main" .github/workflows/test.yml && grep -q "go test" .github/workflows/test.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/test.yml'))" && echo OK</automated>
  </verify>
  <done>`.github/workflows/test.yml` exists and is valid YAML; triggers on push + PR to `main`; uses `go-version: 1.25.x` (matching go.mod, not 1.23); builds the binary, installs Chromium, and runs `go test ./...` headless; no external-network step.</done>
</task>

<task type="auto">
  <name>Task 3: Write the CDP-footprint findings note (informational YES/NO ceiling)</name>
  <read_first>
    - .planning/phases/24-detection-harness-ci-backbone/24-CONTEXT.md lines 24-25, 70 (CDP-footprint spike is INFORMATIONAL/non-blocking; document the ceiling, do NOT attempt to fix CDP detectability — that is v2 CDP-01)
    - .planning/research/PITFALLS.md lines 54-71 (Pitfall 3: CDP itself is detectable regardless of JS spoofing; rod-cli relies on Runtime/Network events for console/request logging)
    - internal/detect/detect.js (the informational `cdpTell` / `webrtcIce` probes from plan 24-01 that this note references)
  </read_first>
  <files>docs/cdp-footprint.md</files>
  <action>
Create `docs/cdp-footprint.md` — a short (roughly one screen) honest findings note answering the YES/NO question: can rod-cli meaningfully hide its CDP/`Runtime.enable` footprint given it relies on Runtime/Network CDP events for console and request logging? Cover:
  - The question and the answer (NO, not meaningfully within v1.6's JS-injection layer — the CDP transport is observable regardless of JS-property spoofing; PITFALLS.md Pitfall 3).
  - Why: `createPage()` subscribes to `RuntimeConsoleAPICalled` and `NetworkRequestWillBeSent`, which force Runtime/Network domain enablement — exactly the surface CDP-tell detectors probe.
  - What this phase does about it: ADDS informational, non-blocking CDP-tell probes to the harness (`window.__detect.cdpTell`) so rod-cli MEASURES its exposure rather than assuming spoofing covers it. It does NOT attempt a fix.
  - The honest ceiling: a JS-injecting CLI cannot fully control the CDP/TLS/IP layers; "undetectable" is not claimed. Reducing the CDP footprint (vs merely probing it) is deferred to v2 (CDP-01).
Keep it concise and token-efficient. This is the "short findings note" CONTEXT.md line 25 calls for — prose only, no code changes. (Per Claude's Discretion in CONTEXT.md line 32, `docs/cdp-footprint.md` is the chosen home for the note.)
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && test -f docs/cdp-footprint.md && grep -qi "CDP" docs/cdp-footprint.md && grep -qi "ceiling\|v2\|CDP-01\|non-blocking\|informational" docs/cdp-footprint.md && echo OK</automated>
  </verify>
  <done>`docs/cdp-footprint.md` exists and documents the YES/NO CDP-footprint ceiling: states CDP is detectable regardless of JS spoofing, that this phase only PROBES (informational, non-blocking) and does not fix it, and that reducing the footprint is deferred to v2 (CDP-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo + external network | the workflow checks out the repo and (only) downloads Chromium/Go modules; the test run itself is offline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | Information Disclosure | CI logs | mitigate | No secrets are used by the test job (no `secrets.*` referenced); the harness is offline and emits no credentials — proxy creds are out of scope this phase. |
| T-24-09 | Tampering | CI dependency download | accept | Chromium/Go-module downloads come from pinned actions (`checkout@v4`, `setup-go@v5`) and trusted upstreams; no `npm/pip/cargo` install, so no package-legitimacy gate applies. |
| T-24-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase — no slopcheck checkpoint required. |
</threat_model>

<verification>
- `.github/workflows/test.yml` is valid YAML, triggers on push + PR to main, uses go 1.25.x, runs `go test ./...`, has no external-network step.
- `git status` shows no stray untracked artifacts; `git ls-files tests/rod` returns nothing (the accidental browser-profile commit is gone).
- `.gitignore` consolidates new rules without duplicating existing `*.out`/`*.log` globs.
- `docs/cdp-footprint.md` documents the CDP ceiling (informational, deferred-fix to v2).
- After cleanup, `go test ./...` still passes locally against the current binary (baseline green, KNOWN-RED markers intact).
</verification>

<success_criteria>
- The repo's first test CI job runs the offline detection harness on every push + PR to main, go-version matching go.mod (1.25.x), with zero network egress in the test run — baselined against the current binary so KNOWN-RED leaks surface as documented baseline, not silent passes.
- The accidental `tests/rod/` browser-profile commit and `tests/interaction_test.go.orig` are removed from git; all listed stray artifacts are deleted and gitignored without duplicating existing globs.
- A concise CDP-footprint findings note records the honest YES/NO ceiling (probe, don't fix; reduction deferred to v2 CDP-01).
</success_criteria>

<output>
Create `.planning/phases/24-detection-harness-ci-backbone/24-04-SUMMARY.md` when done.
</output>
