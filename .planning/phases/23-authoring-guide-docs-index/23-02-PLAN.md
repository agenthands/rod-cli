---
phase: 23-authoring-guide-docs-index
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - docs/plugins/README.md
  - README.md
autonomous: true
requirements: [PDOC-05]
must_haves:
  truths:
    - "A reader can open docs/plugins/README.md and reach every plugin doc page and example from it"
    - "A reader starting at the top-level README.md reaches the plugin docs index in one click"
  artifacts:
    - path: "docs/plugins/README.md"
      provides: "Plugin docs index reaching every plugin doc + example"
      min_lines: 20
      contains: "authoring.md"
    - path: "README.md"
      provides: "One-click link into docs/plugins/ from the Documentation section"
      contains: "docs/plugins"
  key_links:
    - from: "docs/plugins/README.md"
      to: "docs/plugins/authoring.md"
      via: "relative link in the Getting Started group"
      pattern: "authoring\\.md"
    - from: "docs/plugins/README.md"
      to: "docs/plugins/examples/xss-scanner.md"
      via: "relative link in the Examples group"
      pattern: "examples/xss-scanner\\.md"
    - from: "README.md"
      to: "docs/plugins/README.md"
      via: "Documentation-section bullet linking to the plugin docs index"
      pattern: "docs/plugins"
---

<objective>
Create the plugin docs index `docs/plugins/README.md` reaching every plugin doc page and example, and add a one-click link into `docs/plugins/` from the top-level `README.md` `## Documentation` section (PDOC-05).

Purpose: Make all plugin documentation discoverable. The index is the hub that reaches authoring.md + the four reference pages + the three example docs; the top-level README routes readers to that hub in one click.

Output: `docs/plugins/README.md` (new index) and an edited `README.md` (one new bullet in the existing `## Documentation` list).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-authoring-guide-docs-index/23-CONTEXT.md

# The tutorial this index must reach (created in 23-01)
@docs/plugins/authoring.md

# Sibling pages the index must reach
@docs/plugins/lifecycle-hooks.md
@docs/plugins/state-api.md
@docs/plugins/cli-reference.md
@docs/plugins/examples/starter.md
@docs/plugins/examples/recipes.md
@docs/plugins/examples/xss-scanner.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the plugin docs index and link it from the top-level README</name>
  <files>docs/plugins/README.md, README.md</files>
  <read_first>
    - docs/plugins/cli-reference.md and docs/plugins/lifecycle-hooks.md — confirm exact sibling filenames and the established tone/structure (intro -> grouped lists -> cross-links) the index should match.
    - docs/plugins/examples/starter.md, docs/plugins/examples/recipes.md, docs/plugins/examples/xss-scanner.md — confirm the three example doc filenames the index must reach under examples/.
    - docs/plugins/authoring.md — the tutorial created in 23-01 the index must link as the first Getting Started item.
    - README.md (lines 15-21, the `## Documentation` section) — an existing bullet list (`- **[Name](path)**: description.`); the new plugin-docs bullet must match that exact bullet format.
  </read_first>
  <action>
    Part A — Create `docs/plugins/README.md`:

    Title it `# rod-cli Plugin Documentation` (H1) with a 1-2 sentence intro: this is the index for writing, loading, and running `rod-cli` plugins, pointing newcomers at the authoring tutorial.

    Then three grouped H2 sections with relative-link bullet lists. Use these exact link targets (all relative to docs/plugins/):

    `## Getting Started`
    - [Writing Your First Plugin](./authoring.md) — zero-to-running tutorial.

    `## Reference`
    - [Lifecycle Hooks](./lifecycle-hooks.md) — the four lifecycle hooks and their payloads.
    - [State / Context API](./state-api.md) — the `api` global accessors.
    - [Plugin CLI Reference](./cli-reference.md) — `plugin load`, `plugin list`, `plugin run`.

    `## Examples`
    - [Starter Template](./examples/starter.md) — copyable scaffold.
    - [Per-Hook Recipes](./examples/recipes.md) — single-hook recipes.
    - [XSS Scanner](./examples/xss-scanner.md) — flagship worked example.

    (Use a short trailing description on each bullet matching the sibling-doc tone. Wording/exact descriptions are at the executor's discretion per CONTEXT.md, but the three groups, the seven targets, and the link order above are fixed.)

    Every link MUST be relative and resolve from docs/plugins/: sibling pages as `./name.md`, examples as `./examples/name.md`. The index must reach all SEVEN docs: authoring.md, lifecycle-hooks.md, state-api.md, cli-reference.md, examples/starter.md, examples/recipes.md, examples/xss-scanner.md.

    Part B — Edit the top-level `README.md`:

    In the existing `## Documentation` section (a bullet list of `- **[Name](path)**: description.` entries), ADD ONE bullet linking to the plugin docs index, matching the existing bullet format exactly. Use:

    `- **[Plugin Development](docs/plugins/README.md)**: Writing, loading, and running rod-cli plugins — start here for the plugin docs index.`

    Place it as a new list item within the existing Documentation list (append after the last existing bullet). Do NOT rewrite or reorder the other bullets; add only this one line. The link target must be `docs/plugins/README.md` (relative from the repo root README) so the index is reachable in one click.

    Use the Edit tool for README.md (scoped insertion of the single bullet); use Write for the new docs/plugins/README.md.
  </action>
  <verify>
    <automated>test -f docs/plugins/README.md && grep -q "authoring.md" docs/plugins/README.md && grep -q "lifecycle-hooks.md" docs/plugins/README.md && grep -q "state-api.md" docs/plugins/README.md && grep -q "cli-reference.md" docs/plugins/README.md && grep -q "examples/starter.md" docs/plugins/README.md && grep -q "examples/recipes.md" docs/plugins/README.md && grep -q "examples/xss-scanner.md" docs/plugins/README.md && grep -q "docs/plugins/README.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/plugins/README.md` exists with three groups: Getting Started, Reference, Examples.
    - The index contains relative links to ALL SEVEN docs: authoring.md, lifecycle-hooks.md, state-api.md, cli-reference.md, examples/starter.md, examples/recipes.md, examples/xss-scanner.md.
    - Every relative link in docs/plugins/README.md resolves to an existing file (verified by the phase link-resolution check).
    - The top-level `README.md` `## Documentation` section contains a new bullet whose link target is `docs/plugins/README.md` — reachable in one click.
    - The other existing Documentation bullets in README.md are unchanged (only one line added).
  </acceptance_criteria>
  <done>
    docs/plugins/README.md is a grouped index reaching every plugin doc and example via resolving relative links, and README.md's Documentation section has a one-click Plugin Development bullet pointing at the index.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan adds one Markdown index file and inserts one bullet into the existing README. No code, no runtime surface, no new input crosses a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02 | Tampering | docs/plugins/README.md, README.md | accept | Documentation-only edits. The only integrity concern is broken/incorrect links, addressed by the phase link-resolution acceptance check rather than a runtime control. No new attack surface. |

No package installs in this plan; no `T-{phase}-SC` supply-chain threat applies (documentation only).
</threat_model>

<verification>
- `docs/plugins/README.md` exists and reaches all seven plugin docs via relative links.
- All relative links in docs/plugins/README.md resolve to real files.
- `README.md` `## Documentation` section links to `docs/plugins/README.md` (one click), with no other bullets altered.
</verification>

<success_criteria>
A reader can open `docs/plugins/README.md` and reach every plugin doc page and example from it; a reader starting at the top-level `README.md` finds a link into `docs/plugins/` and reaches the plugin documentation index in one click.
</success_criteria>

<output>
Create `.planning/phases/23-authoring-guide-docs-index/23-02-SUMMARY.md` when done.
</output>
