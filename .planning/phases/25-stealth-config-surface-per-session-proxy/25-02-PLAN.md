---
phase: 25-stealth-config-surface-per-session-proxy
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - types/context.go
  - types/context_test.go
autonomous: true
requirements: [PROXY-01, PROXY-02]
must_haves:
  truths:
    - "A user can route a session through an HTTP or SOCKS5 proxy via --proxy <url> with the scheme in the URL; egress is routed through godoll's ProxyConfig.ApplyToLauncher, not the bare launcher.Proxy (PROXY-01)."
    - "Proxy authentication via --proxy-auth user:pass succeeds through CDP-based handling (godoll SetupBrowserAuth) with no URL-embedded credentials and no 407/dialog hang (PROXY-02)."
    - "SOCKS5/HTTP authenticated proxies route through godoll's local authenticated relay (StartProxyRelay via ApplyToLauncher) and the relay is stopped when the session closes (PROXY-02)."
    - "Proxy credentials never appear in default stdout/stderr or the daemon log; the relay cleanup runs on Context.Close (credential safety)."
  artifacts:
    - path: "types/context.go"
      provides: "parseProxyConfig helper + ProxyConfig.ApplyToLauncher/SetupBrowserAuth wiring replacing bare launcher.Proxy; proxyCleanup stored on Context and called in Close"
      contains: "ApplyToLauncher"
  key_links:
    - from: "types/context.go launchBrowser"
      to: "godoll browser.ProxyConfig.ApplyToLauncher"
      via: "parsed ProxyConfig from cfg.Stealth.Proxy + cfg.Stealth.ProxyAuth replaces the bare browserLauncher.Proxy(cfg.Proxy) at lines 71-72"
      pattern: "ApplyToLauncher\\(browserLauncher\\)"
    - from: "types/context.go (browser instance)"
      to: "godoll browser.ProxyConfig.SetupBrowserAuth"
      via: "SetupBrowserAuth(browserInstance) called after NewBrowserE returns, before the first Page()/Navigate()"
      pattern: "SetupBrowserAuth\\("
    - from: "types/context.go Context.Close"
      to: "proxy relay cleanup func"
      via: "proxyCleanup() invoked on Close so StartProxyRelay is stopped per session"
      pattern: "proxyCleanup"
---

<objective>
Wire the first feature riding the config substrate: a **per-session HTTP/SOCKS5 proxy with CDP-based auth**, replacing the bare `launcher.Proxy(cfg.Proxy)` at `types/context.go:71-72` with godoll's proxy API. Parse `cfg.Stealth.Proxy` (+ `cfg.Stealth.ProxyAuth`) into a `browser.ProxyConfig`, apply it via `ApplyToLauncher` (which starts godoll's local authenticated relay when creds are present), register CDP auth via `SetupBrowserAuth`, and stop the relay on `Context.Close`. This is PROXY-01 and PROXY-02. Per-session binding is automatic — one daemon per `-s` session means one browser per proxy, no per-BrowserContext work.

Purpose: this validates the whole flag → config → godoll path end-to-end. The proxy is the smallest already-half-wired feature, so it proves the substrate from Plan 01 actually reaches the browser launch. Credentials must be handled via CDP `Fetch.continueWithAuth` (godoll `SetupBrowserAuth` uses `Browser.HandleAuth`), never URL-embedded (Chrome removed them, and SOCKS5 auth is unsupported by `--proxy-server`).

Output: proxy parsing + godoll wiring + relay-cleanup lifecycle in `types/context.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-stealth-config-surface-per-session-proxy/25-CONTEXT.md
@.planning/phases/25-stealth-config-surface-per-session-proxy/25-01-PLAN.md
@types/context.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse cfg.Stealth.Proxy/ProxyAuth into a godoll browser.ProxyConfig</name>
  <files>types/context.go, types/context_test.go</files>
  <read_first>
    - types/context.go (launchBrowser at line 32; the bare proxy site at lines 71-73: `if cfg.Proxy != "" { browserLauncher.Proxy(cfg.Proxy) }`; the godoll/browser import already present at line 12).
    - godoll browser.ProxyConfig at ../godoll/browser/proxy.go:18 — fields `Protocol` ("http"|"socks5"|"socks4"), `Address` ("host:port"), `Username`, `Password`. Helpers: `HasAuth()` (:39), `LauncherURL()` (:34, no creds), `ApplyToLauncher(l)` returns `(cleanup func(), err error)` (:47 — starts StartProxyRelay when HasAuth), `SetupBrowserAuth(b)` (:67 — loops Browser.HandleAuth with creds).
    - types/config.go StealthConfig (from Plan 01): `Stealth.Proxy` (URL with scheme), `Stealth.ProxyAuth` (`user:pass`).
  </read_first>
  <behavior>
    - Test: parseProxyConfig("http://127.0.0.1:8080", "") yields Protocol="http", Address="127.0.0.1:8080", no auth.
    - Test: parseProxyConfig("socks5://127.0.0.1:1080", "user:pass") yields Protocol="socks5", Address="127.0.0.1:1080", Username="user", Password="pass", HasAuth()==true.
    - Test: parseProxyConfig("", "") returns a nil config (no proxy → caller skips wiring).
    - Test: a proxy URL with embedded creds (http://user:pass@host:port) is rejected OR creds are stripped from the launcher URL — embedded-cred form must never reach Chrome's --proxy-server.
    - Test: an unparseable proxy URL returns a non-nil error (loud failure).
    - Test: parsing a ProxyAuth value of "user:pass:with:colons" splits on the FIRST colon only (password may contain colons).
  </behavior>
  <action>
    Add an unexported helper `parseProxyConfig(proxyURL, proxyAuth string) (*browser.ProxyConfig, error)` in types/context.go. Parse `proxyURL` with `net/url` (or manual scheme split): the scheme becomes `Protocol` (accept `http`, `https`→`http`, `socks5`, `socks4`), `host:port` becomes `Address`. If `proxyURL` is empty, return `(nil, nil)`. If it has embedded credentials (`url.User != nil`), do NOT carry them into `Address` — either reject with an error or drop them (embedded creds must never reach `--proxy-server`; Chrome removed support and it leaks); document the choice. Split `proxyAuth` on the FIRST `:` into Username/Password (password may legitimately contain `:`), and set them on the ProxyConfig. Return a loud error on an unparseable URL.

    Do NOT print the proxy URL or credentials anywhere. The helper is pure parsing — no logging.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && go build ./... && go test ./types/ -run 'ProxyConfig|ParseProxy' -count=1</automated>
  </verify>
  <done>parseProxyConfig maps scheme→Protocol and host:port→Address, splits proxyAuth on the first colon into Username/Password, rejects/strips embedded URL creds, returns nil for empty input and a loud error on a bad URL; types package builds and the parsing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace bare launcher.Proxy with ProxyConfig.ApplyToLauncher + SetupBrowserAuth, and store relay cleanup on Context</name>
  <files>types/context.go</files>
  <read_first>
    - types/context.go (the bare proxy block at lines 71-73; the godoll options + NewBrowserE block at lines 75-85; the Context struct at lines 107-123; NewContext at 125; Close at 433-446; closeBrowser at 263-279).
    - godoll ProxyConfig.ApplyToLauncher (../godoll/browser/proxy.go:47) returns a cleanup func that must be called to stop the relay (nil when no relay); SetupBrowserAuth (../godoll/browser/proxy.go:67) must be called AFTER the browser exists and BEFORE the first Page()/Navigate().
    - parseProxyConfig from Task 1.
  </read_first>
  <action>
    In `launchBrowser` (types/context.go), REPLACE the bare proxy block at lines 71-73 (`if cfg.Proxy != "" { browserLauncher.Proxy(cfg.Proxy) }`) with the godoll proxy path:
      1. `proxyCfg, err := parseProxyConfig(cfg.Stealth.Proxy, cfg.Stealth.ProxyAuth)` — return the error on failure.
      2. If `proxyCfg != nil`: `cleanup, err := proxyCfg.ApplyToLauncher(browserLauncher)` (this sets `--proxy-server` for the no-auth case, or starts a local authenticated relay and points Chrome at it for the auth case — godoll handles the SOCKS5/HTTP-auth gap). Return the error on failure. Capture `cleanup` so it can be stopped later.
      3. After `browser.NewBrowserE(ctx, opts)` returns `browserInstance` (line 80), if `proxyCfg != nil && proxyCfg.HasAuth()` call `proxyCfg.SetupBrowserAuth(browserInstance)` — this registers the persistent CDP auth handler (godoll loops `Browser.HandleAuth(user, pass)`) BEFORE any Page()/Navigate(), so an authenticated proxy never 407s or pops a hanging dialog.

    Because `launchBrowser` returns only `(*rod.Browser, error)` today, thread the relay cleanup out so `Context.Close` can stop it. Add a `proxyCleanup func()` field to the `Context` struct (lines 107-123). Set it from `initial()` (lines 150-175) where `launchBrowser` is called — either change `launchBrowser` to also return the cleanup, or store it on the Context inside the launch path. In `closeBrowser` (or `Close` at 433-446), invoke `ctx.proxyCleanup()` if non-nil (guard against double-call; nil it after) so the per-session relay is stopped when the session closes. The relay is per-session because the daemon is per-session.

    Remove or stop relying on the old `cfg.Proxy` bridge for the launch path (the authoritative source is now `cfg.Stealth.Proxy`). Do NOT log the proxy URL or credentials. The godoll relay logs to its own `log.Printf` (`[ProxyRelay] ...` with the upstream Address but NOT credentials — Address-only is acceptable); do not add any rod-cli-side credential logging.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/rod-cli && go build ./... && go vet ./... && go test ./types/ -run 'Proxy|LaunchBrowser|Close' -count=1</automated>
  </verify>
  <done>The bare `launcher.Proxy(cfg.Proxy)` is gone; launchBrowser parses `cfg.Stealth.Proxy`/`ProxyAuth`, applies via `ApplyToLauncher`, calls `SetupBrowserAuth` for the auth case before first navigation, stores the relay cleanup on Context, and `Close`/`closeBrowser` stops the relay; no credentials are logged; package builds and vets clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → upstream proxy | When `--proxy-auth` is set, godoll starts a local `127.0.0.1:0` CONNECT relay; Chrome connects to the relay with NO auth, the relay adds `Proxy-Authorization: Basic <b64>` to the upstream. Credentials live only in the relay process memory + the upstream CONNECT request, never in Chrome's `--proxy-server` argv. |
| proxy egress vs WebRTC/DNS | HTTP egress goes through the proxy; the parallel WebRTC/DNS stack does NOT (Phase 27 HARDEN-01 concern — cross-referenced, out of scope). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Information Disclosure | URL-embedded proxy creds reaching --proxy-server | mitigate | parseProxyConfig (Task 1) rejects/strips embedded `user:pass@` and never lets creds into `LauncherURL()`/`--proxy-server`; auth goes via CDP `SetupBrowserAuth` only. |
| T-25-06 | Denial of Service | 407 / native auth dialog hangs the headless daemon | mitigate | `SetupBrowserAuth` registers the CDP auth handler BEFORE the first Page()/Navigate() (Task 2), so the auth challenge is answered programmatically — no hanging dialog. |
| T-25-07 | Resource leak | per-session relay not stopped on close | mitigate | `proxyCleanup` stored on Context and invoked in `closeBrowser`/`Close` (Task 2); the relay is process-scoped to the per-session daemon. |
| T-25-08 | Information Disclosure | proxy creds in rod-cli stdout/stderr/daemon.log | mitigate | No rod-cli-side logging of the proxy URL/creds; godoll's relay logs the upstream Address only, not credentials. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; zero new third-party Go modules (godoll/browser already in go.mod, verified in STACK.md). No [ASSUMED]/[SUS] packages. |

WebRTC/DNS leak past the proxy is owned by Phase 27 (HARDEN-01) — this plan routes HTTP egress but does not prevent the WebRTC real-IP leak. Cross-referenced so the gap is explicit, not silent.
</threat_model>

<verification>
- `go build ./...` and `go vet ./...` clean; bare `launcher.Proxy(` no longer present in launchBrowser's proxy path.
- A no-auth proxy routes via `ApplyToLauncher` (sets `--proxy-server`); an auth proxy starts the relay and registers CDP auth before navigation.
- `Context.Close` stops the relay (no leaked listener).
- No proxy credential string appears in rod-cli's own output or daemon log.
- End-to-end egress + isolation + auth-against-a-fixture is asserted in Plan 03.
</verification>

<success_criteria>
- PROXY-01: `--proxy <http://...|socks5://...>` routes the session's egress through godoll's proxy API, bound to the per-session daemon's browser (automatic per-session binding).
- PROXY-02: `--proxy-auth user:pass` authenticates via CDP (`SetupBrowserAuth` → `Browser.HandleAuth`), with no URL-embedded credentials and no 407/dialog hang.
- SOCKS5-with-auth (and HTTP-auth) route through godoll's local authenticated relay (`StartProxyRelay` via `ApplyToLauncher`), stopped on session close.
</success_criteria>

<output>
Create `.planning/phases/25-stealth-config-surface-per-session-proxy/25-02-SUMMARY.md` when done.
</output>
