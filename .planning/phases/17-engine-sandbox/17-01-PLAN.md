---
wave: 1
depends_on: []
files_modified:
  - internal/plugin/engine.go
autonomous: true
---

# Plan 17-01: Engine Sandbox Setup

## Objective
Implement the plugin engine sandbox and script loader using the `goja` JavaScript engine.

## Requirements Covered
- PLUG-01
- PLUG-02

## Tasks

<task>
<read_first>
- internal/plugin/engine.go
</read_first>
<action>
Create `internal/plugin/engine.go` to define a `PluginEngine` struct wrapping a `goja.Runtime`.
Add an `Init()` method that instantiates the runtime.
Add a `LoadScript(path string) error` method that reads a file and executes it in the `goja.Runtime`.
</action>
<acceptance_criteria>
- `internal/plugin/engine.go` exists and compiles.
- `goja.Runtime` is successfully instantiated.
- `LoadScript` correctly reads and parses JS files.
</acceptance_criteria>
</task>

## Verification
<must_haves>
<truths>
- Engine initializes properly inside the daemon process.
- A generic script can be parsed and executed via file path.
</truths>
</must_haves>

## Artifacts this phase produces
- `internal/plugin/engine.go`
- `PluginEngine`
- `Init`
- `LoadScript`
